---
description: Write the handoff file so a fresh session can continue with /continue
---
Reap finished background sessions: from an up-to-date lane clone (e.g. `~/workspace/.lanes/web-everything/lane-54`; its HEAD must equal origin/main; the primary checkout is stale) run `node scripts/conveyor/session-reaper.mjs` as a FOREGROUND call with timeout 600000 (it is slow: one `gh` call per session), never backgrounded. It only stops sessions that are done/failed or whose target item/PR is confirmed resolved/merged. Do not hand-run `claude stop`. Report the session count from `claude agents --json` before and after. NEVER overwrite, shorten or reword /Users/nicolasgilbert/workspace/.operations/handoff/handoff-webeverything-rules.md (the operator's durable standing rules; the 500-word limit below does not apply to it). Only APPEND any operator correction or preference given this session that is not already there, as a new dated line, and reference it in the handoff as "rules file: N rules". Also include in the handoff a "NEXT ITEMS" list: the first dispatchable lines of the `## Priority order` in epic #3383's card (prototype branch), each with its list line number and what blocks the ones that are waiting; never take next items from `run.mjs suggest-next`. Then overwrite /Users/nicolasgilbert/workspace/.operations/handoff/handoff-webeverything.md (a file outside the repo and outside ~/.claude, because the repo's guard blocks edits in the primary checkout and the built-in .claude-path gate prompts on every edit under ~/.claude; do not write into the repo; the tracked ops/* branch home is card #3779, not built yet). Contents, under 500 words, no secrets: the goal; environment gotchas; the plan as a compact list of PR -> disposition -> status, with statuses taken from live `gh` output at write time; what is in flight and where its output will appear (PR numbers, branches, lanes); decisions waiting on the operator; standing rules. Reply with the file path and one sentence.
