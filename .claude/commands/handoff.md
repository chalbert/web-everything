---
description: Write the handoff file so a fresh session can continue with /continue
---
Reap finished background sessions: from an up-to-date lane clone (e.g. `~/workspace/.lanes/web-everything/lane-54`; its HEAD must equal origin/main; the primary checkout is stale) run `node scripts/conveyor/session-reaper.mjs` as a FOREGROUND call with timeout 600000 (it is slow: one `gh` call per session), never backgrounded. It only stops sessions that are done/failed or whose target item/PR is confirmed resolved/merged. Do not hand-run `claude stop`. Report the session count from `claude agents --json` before and after. Then overwrite /Users/nicolasgilbert/.claude/handoff-webeverything.md (a file outside the repo, because the repo's guard blocks edits in the primary checkout; do not write into the repo). Contents, under 500 words, no secrets: the goal; environment gotchas; the plan as a compact list of PR -> disposition -> status, with statuses taken from live `gh` output at write time; what is in flight and where its output will appear (PR numbers, branches, lanes); decisions waiting on the operator; standing rules. Reply with the file path and one sentence.
