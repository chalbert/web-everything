---
description: Review the backlog and pick & work the next item (routes to the next-backlog-item skill)
---

Invoke the `next-backlog-item` skill to review the backlog, present a ranked shortlist, and — once I say go — work the chosen item. If I name an item below (e.g. `100` or `100-requirement-as-code`), skip the selection step and work that item directly.

$ARGUMENTS
