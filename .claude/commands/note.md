---
description: Capture a one-off observation into the cross-session learnings pool (routes to the capture-learning skill)
---

Invoke the `capture-learning` skill to shape what I say below into a learnings-pool entry and append it via
`we:scripts/conveyor/learnings-drop.mjs`. This is the write side only — capture, never adjudicate: it must
never route to `we:backlog/` or agent memory.

$ARGUMENTS
