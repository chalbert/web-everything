---
description: Continue the orchestration work from the saved handoff
---
FIRST read /Users/nicolasgilbert/workspace/.operations/handoff/handoff-webeverything-rules.md (the operator's durable standing rules: obey every one; you never overwrite it, you only append new operator feedback to it the moment it is given). Its rule 1 governs "next items": they come ONLY from the `## Priority order` of epic #3383's card on the prototype branch, never from `run.mjs suggest-next`; name the list line of each dispatch. Then read /Users/nicolasgilbert/workspace/.operations/handoff/handoff-webeverything.md and follow it. Do not trust the PR statuses in that file: re-derive live state first with `gh pr list --repo chalbert/web-everything --state open --json number,title,labels,mergeStateStatus` and the same for `--repo chalbert/frontierui`, and check lane-pool status. Then reap finished background sessions: from an up-to-date lane clone (e.g. `~/workspace/.lanes/web-everything/lane-54`; its HEAD must equal origin/main; the primary checkout is stale) run `node scripts/conveyor/session-reaper.mjs` as a FOREGROUND call with timeout 600000 (it is slow: one `gh` call per session), never backgrounded. It only stops sessions that are done/failed or whose target item/PR is confirmed resolved/merged. Do not hand-run `claude stop`. Report the session count from `claude agents --json` before and after. Also read the prototype tracker: epic #3383 card `backlog/3383-a-background-mechanical-dispatcher-replaces-the-interactive.md` on branch `lane/mechanical-dispatcher` (`git show origin/lane/mechanical-dispatcher:backlog/3383-...`; keep it current with /prototype-tracker). (Old prototype PR #1853 is CLOSED; there is no PR for the prototype.) Rule: prototype (#3383) work is committed straight to lane/mechanical-dispatcher with no PR of its own; everything else goes to main via a PR. Build the "Needs you" list ONLY from the operator-queue script: run `node scripts/operations/operator-queue.mjs` from that same up-to-date lane clone (PR #2334 has merged; the primary checkout is stale and lacks the script). Never ask the operator to review a PR the script lists as NOT READY; that is agent work. The "Needs you" list is that script's NEEDS YOU section printed verbatim and NOTHING else: the operator reviews only `review:human` PRs, so never list a PR without that label, and never add your own confirmations, approvals, chores, decisions or questions (decisions live in the docket). Empty means say nothing about Needs you. A PR without review:human is never operator work. Then reply in under 100 words: what changed since the handoff, and what you will do next. Then do it. Keep turns few and replies short. Keep no state in your head: anything to carry over goes back into the handoff file at the end of the session.
