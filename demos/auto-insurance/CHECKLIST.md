# Auto Insurance (exercise app B) — verification checklist

> Auto-generated by `npm run check:demos -- --write-checklist`. Edit only below the
> `## Demo-specific` marker — everything above it is regenerated from the demo's metadata
> (`src/_data/demos.json` + `conformance.json` + parsed routes).

Served by Vite on `:3000`. Entry: <http://localhost:3000/demos/auto-insurance/index.html>.

Mounted under the base path **`/demos/auto-insurance/`** and driven by the shipping **Router** block,
so the routing/reload checks below guard the base-path regression class (#317/#318).

## Routing & base path (the regression guard)

- [ ] **Cold load** of the entry URL redirects into route space (not a bare origin-root path).
- [ ] **Reload (Cmd-R)** on each deep route re-renders — no 404, no blank page. Routes: `/book`, `/quotes`, `/underwriting`, `/claims`.
- [ ] **Deep-link paste** of a route in a fresh tab lands on that view.
- [ ] **Tab navigation** updates the URL to the base-qualified path and sets `aria-current="page"`.
- [ ] **Back / forward** moves between visited routes and keeps the active tab in sync.
- [ ] The address bar **never** shows an origin-root path (e.g. `http://localhost:3000/book`).

### Server-side fallback probe (no browser)

```bash
npm run check:demos -- --live   # asserts entry + every deep route reload returns 200
```

## Standards surfaces (from `conformance.json` — the app is the forcing function for these)

- [ ] **data-table** — the policy book table.
- [ ] **pagination** — windowing the 4k book.
- [ ] **collection-operations** — sort / page the book.
- [ ] **selection** — selecting a policy (via the master-detail coordinator).
- [ ] **master-detail** — policy row → detail coordination.
- [ ] **router** — client-side module navigation.
- [ ] **lifecycle** — the policy status machine (Web Lifecycle).
- [ ] **status-indicator** — policy state + UW finding chips.
- [ ] **decision-trace** — the underwriting decision as a DecisionRecord.
- [ ] **audit-trail** — per-policy immutable history; transitions auto-log + timeline.
- [ ] **stepper** — the multi-step quote wizard (locked progression + per-step gate).
- [ ] **tree-select** — the coverage hierarchy builder (role=tree, cascade selection).

## Demo-specific

_Hand-authored notes, gotchas, and demo-specific checks go here — preserved across regen._
