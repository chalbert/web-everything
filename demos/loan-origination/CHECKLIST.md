# Loan Origination (exercise app A) — verification checklist

> Auto-generated by `npm run check:demos -- --write-checklist`. Edit only below the
> `## Demo-specific` marker — everything above it is regenerated from the demo's metadata
> (`src/_data/demos.json` + `conformance.json` + parsed routes).

Served by Vite on `:3000`. Entry: <http://localhost:3000/demos/loan-origination/index.html>.

Mounted under the base path **`/demos/loan-origination/`** and driven by the shipping **Router** block,
so the routing/reload checks below guard the base-path regression class (#317/#318).

## Routing & base path (the regression guard)

- [ ] **Cold load** of the entry URL redirects into route space (not a bare origin-root path).
- [ ] **Reload (Cmd-R)** on each deep route re-renders — no 404, no blank page. Routes: `/pipeline`, `/application`, `/processing`, `/underwriting`, `/admin`.
- [ ] **Deep-link paste** of a route in a fresh tab lands on that view.
- [ ] **Tab navigation** updates the URL to the base-qualified path and sets `aria-current="page"`.
- [ ] **Back / forward** moves between visited routes and keeps the active tab in sync.
- [ ] The address bar **never** shows an origin-root path (e.g. `http://localhost:3000/pipeline`).

### Server-side fallback probe (no browser)

```bash
npm run check:demos -- --live   # asserts entry + every deep route reload returns 200
```

## Standards surfaces (from `conformance.json` — the app is the forcing function for these)

- [ ] **data-table** — the loan pipeline table.
- [ ] **pagination** — windowing the 5k pipeline.
- [ ] **collection-operations** — sort / filter / page the pipeline.
- [ ] **selection** — selecting a loan row to inspect (now via the master-detail coordinator).
- [ ] **router** — client-side navigation between app views (module bar, detail deep-links).
- [ ] **lifecycle** — the loan application's guarded, role-scoped status machine.
- [ ] **status-indicator** — semantic status chips for state + finding.
- [ ] **audit-trail** — the loan's immutable history; lifecycle transitions auto-log + a timeline.
- [ ] **decision-trace** — the proof-of-compliance trace as a normalized DecisionRecord.
- [ ] **master-detail** — the pipeline row → trace detail coordination.

## Demo-specific

- New route? Add it to **`MODULES`** in `app.ts` **and** to the loan-origination regex in
  `routerDemoFallback` (`vite.config.mts`) — otherwise its reload 404s.
- `route:link` and programmatic redirects must use **`routePath('/x')`** (base-qualified); the router
  does not prepend `base` to link/redirect targets, only to route patterns.
