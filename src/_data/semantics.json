[
  {
    "term": "Viewport Presence",
    "definition": "Whether an element is currently within a reference frame (the viewport or a scrolling ancestor), surfaced as enter/leave transitions. The shared in/out-of-view trigger, owning the observe-vocabulary (reference / margin / coverage, mapping to IntersectionObserver root / rootMargin / threshold) but never the UX decision a consumer takes on it.",
    "usage": "Standardized by the Viewport Presence Intent. Composed by Collection Operations <code>advance:auto</code>, Prefetch <code>eagerness:viewport</code>, and the visibility-gated trait so the IntersectionObserver trigger has one home, not three."
  },
  {
    "term": "Mock Contract",
    "definition": "A portable, interaction-bearing artifact that defines how an HTTP service responds during development: consumer interactions whose response shape is anchored on OpenAPI and whose dev-time behavior (latency, fault, conditional, response-kind, auth) lives in a layer on top. Because it is data, not code, any conformant server can serve it and any drift-checker can verify it.",
    "usage": "Standardized by the Mock Contract Protocol (Web Cases). The standard artifact of the mock & service-proxy dev-service — the server is a swappable provider. One contract, two uses: drive a mock and drift-check the real service."
  },
  {
    "term": "Consumer Interaction",
    "definition": "A single consumer expectation in a Mock Contract: a request to match paired with the response the consumer depends on. Pact/BDCT terminology — it makes the artifact contract-bearing rather than shape-describing, since a real consumer's dependency on an interaction is what a contract verifies.",
    "usage": "The unit of the Mock Contract Protocol's <code>interactions[]</code>. Drives a mock (serve the response) and a drift-check (replay against the real provider)."
  },
  {
    "term": "Response-Kind",
    "definition": "How a Mock Contract interaction is fulfilled: <code>mock</code> (serve the defined response), <code>proxy</code> (forward to the real backend), or <code>record</code> (proxy and capture the live response to seed a mock). Mixed per-interaction on one service.",
    "usage": "The <code>kind</code> field of a Mock Contract interaction. Why the proxy is a mode of the one schema, not a sibling standard."
  },
  {
    "term": "Contract Drift",
    "definition": "Divergence between a recorded consumer interaction and the real service's current behavior — the provider stopped honouring an interaction a consumer depends on. Distinct from shape non-conformance: schema-validation proves a payload is well-formed, contract-verification proves the interaction still holds.",
    "usage": "Detected by the Web Cases drift-check, which replays a Mock Contract's interactions against the live service. The dev-time half of proving the seams still hold."
  },
  {
    "term": "Action",
    "definition": "The abstract concept of a user trigger that initiates a process or side-effect (e.g., 'Save', 'Delete'). It decouples the trigger (Button) from the handler.",
    "usage": "Standardized by Action Intent."
  },
  {
    "term": "Background Task",
    "definition": "A long-running operation that has gone async — handed off from an in-view loader past its escalation threshold to a persistent surface, where it continues while the user is free to navigate away.",
    "usage": "Standardized by Background Task Intent. The receiving end of the Loader Intent's <code>escalation: async</code> handoff; hosted in an app-shell region (Layout Intent <code>dock</code>)."
  },
  {
    "term": "beforetoggle",
    "definition": "A cancelable event fired before a visibility change. Call preventDefault() to cancel the transition.",
    "usage": "Matches Popover API pattern. Fired by ViewEngine on all View blocks. Event detail contains <code>oldState</code> and <code>newState</code> ('hidden' or 'visible')."
  },
  {
    "term": "Anchor",
    "definition": "The reference element a floating element positions itself relative to. Also called the anchor element.",
    "usage": "Standardized by Anchor Intent (CSS Anchor Positioning <code>anchor-name</code>/<code>position-anchor</code>). The Anchored Element tethers to it."
  },
  {
    "term": "Anchored Element",
    "definition": "The floating element (tooltip, popover, menu) positioned relative to an anchor element. Maps to CSS Anchor Positioning's 'anchored element'.",
    "usage": "Standardized by Anchor Intent; positioned by a Positioning Provider via the Web Positioning plug."
  },
  {
    "term": "Anti-Flicker Threshold",
    "definition": "The paired entry-delay and minimum-hold contract that prevents a loading indicator from flashing on fast operations: the indicator is only shown once the wait passes an entry threshold (≈50ms), and once shown is held for a minimum duration (≈100ms) even if the operation resolves sooner. Both values are configurable defaults.",
    "usage": "Standardized by Loader Intent (the <code>timing</code> entry threshold + the <code>holdFloor</code> minimum hold)."
  },
  {
    "term": "Placement",
    "definition": "The preferred side and alignment of an anchored element relative to its anchor, on a 12-way grid (side × start/center/end) plus an automatic option.",
    "usage": "The <code>placement</code> dimension of the Anchor Intent. Mirrors CSS <code>position-area</code> and Floating UI placement."
  },
  {
    "term": "Collision",
    "definition": "Detection of viewport or clipping-ancestor overflow, and the fallback response that keeps an anchored element visible.",
    "usage": "The <code>collision</code> dimension of the Anchor Intent: an ordered pipeline of <code>flip</code>/<code>shift</code>/<code>hide</code>/<code>none</code>."
  },
  {
    "term": "Flip",
    "definition": "A collision strategy that moves the anchored element to the opposite side of the anchor when the preferred placement overflows.",
    "usage": "A value of the Anchor Intent <code>collision</code> pipeline. Maps to CSS <code>position-try</code> and Floating UI <code>flip()</code>."
  },
  {
    "term": "Tether",
    "definition": "The live binding that keeps an anchored element positioned relative to its anchor as the page scrolls, resizes, or mutates.",
    "usage": "Established by a Positioning Provider when <code>tracking: 'follow'</code>; returns a cleanup that detaches the tether."
  },
  {
    "term": "Positioning Provider",
    "definition": "A registered engine that computes placement for anchored elements, satisfying the CustomPositioner contract (native CSS Anchor, Floating UI adapter, etc.).",
    "usage": "Registered in CustomPositioningRegistry and resolved through the injector chain. See Web Positioning."
  },
  {
    "term": "aria-current",
    "definition": "ARIA attribute indicating the current item within a set. For navigation, aria-current='page' identifies the current page link.",
    "usage": "Set by RouteLinkBehavior on the link matching the current URL. Screen readers announce it as 'current page'."
  },
  {
    "term": "aria-sort",
    "definition": "ARIA attribute on a column (or row) header indicating the table's current sort state: ascending, descending, none (sortable but not currently sorted), or other (a non-standard order). Only one header is sorted at a time.",
    "usage": "The <a href=\"/blocks/data-table/\">Data Table block</a> projects the Collection Operations <code>sortDirection</code> onto it verbatim; the active sort key's <code>&lt;th&gt;</code> carries the direction, other sortable headers carry <code>none</code>."
  },
  {
    "term": "sortable table",
    "definition": "The WAI-ARIA APG recipe for a table whose columns can be reordered: a native <code>&lt;table&gt;</code> with <code>&lt;th scope=\"col\"&gt;</code> headers, each sortable header wrapping its label in a <code>&lt;button&gt;</code>, and the active column carrying <code>aria-sort</code>. Distinct from <code>role=\"grid\"</code>, which adds cell-level keyboard navigation.",
    "usage": "The accessible-today pattern the <a href=\"/blocks/data-table/\">Data Table block</a> realizes for the sort stage of Collection Operations."
  },
  {
    "term": "filter match",
    "definition": "How a set of active filter predicates combine: <code>all</code> (AND — a row must satisfy every predicate) or <code>any</code> (OR — a row satisfies at least one). The predicates themselves are call-site data, not part of the standard.",
    "usage": "The <code>filterMatch</code> dimension of the <a href=\"/intents/collection-operations/\">Collection Operations intent</a>, realized by the Data Table block's filter stage."
  },
  {
    "term": "group summary",
    "definition": "The aggregate value shown for each group when a collection is grouped — count, sum, average, minimum, or maximum — named after the SQL aggregate functions, the nearest cross-platform standard (the web has none).",
    "usage": "The <code>groupSummary</code> dimension of the <a href=\"/intents/collection-operations/\">Collection Operations intent</a>; the <a href=\"/blocks/data-table/\">Data Table block</a> renders it in a <code>scope=\"rowgroup\"</code> summary row per group."
  },
  {
    "term": "Behavior",
    "definition": "A reusable functionality pattern attached to a component via composition rather than inheritance.",
    "usage": "Implemented via Web Behaviors."
  },
  {
    "term": "Blocking",
    "definition": "The UX state where user interaction is prevented to ensure data integrity during a critical process.",
    "usage": "Defined in Loader Intent (Blocking Profile)."
  },
  {
    "term": "Block",
    "definition": "A modular, visual UI component that follows strict interoperability standards.",
    "usage": "Standardized by Web Blocks."
  },
  {
    "term": "Breakpoint",
    "definition": "Viewport width thresholds that trigger layout changes. In Web Everything, these are tokenized to ensure consistency across JS and CSS.",
    "usage": "Standardized by Breakpoint Intent."
  },
  {
    "term": "Case",
    "definition": "A raw HTML file representing a specific usage scenario. Serves as the Single Source of Truth for documentation examples and E2E test fixtures.",
    "usage": "Located in `src/cases/`."
  },
  {
    "term": "commandfor/command",
    "definition": "Declarative trigger pattern from Invoker Commands API. Button references target by ID (commandfor) and specifies action (command). Custom commands use -- prefix.",
    "usage": "Standard pattern (Baseline Jan 2026). Example: <code>&lt;button commandfor=\"panel-1\" command=\"--view-show\"&gt;</code>"
  },
  {
    "term": "Component",
    "definition": "An encapsulated custom element with its own shadow DOM and lifecycle."
  },
  {
    "term": "Implicit Template",
    "definition": "The convention where a <component> definition's own children are its render source, with no nested <template> wrapper required. A lone direct-child <template> remains the explicit, inert form; both lower to identical output.",
    "usage": "The <component> element's authoring default (DC-11). See the Component block."
  },
  {
    "term": "Form-Associated Custom Element",
    "definition": "A custom element that participates in forms via static formAssociated + attachInternals() — gaining form/labels access, :disabled from a fieldset, and form-lifecycle callbacks.",
    "usage": "Declared on a <component> with the form-associated attribute (DC-12). Value & validation wiring composes with Web Validation."
  },
  {
    "term": "Default ARIA Semantics",
    "definition": "The accessibility role/state an element exposes by default via ElementInternals (internals.role, internals.aria*), overridable per-instance by a role= or aria-* attribute.",
    "usage": "Declared on a <component> with default-role=\"…\" (DC-13). Borrows the platform's ElementInternals ARIAMixin vocabulary."
  },
  {
    "term": "Atomic Move",
    "definition": "Relocating a connected DOM node via Element.moveBefore() without disconnecting it, preserving live state (focus, media playback, iframes, shadow tree). Custom elements opt in by defining connectedMoveCallback.",
    "usage": "Declared on a <component> with the preserve-on-move attribute (DC-15). Mirrors the platform moveBefore()/connectedMoveCallback contract."
  },
  {
    "term": "Composite Widget",
    "definition": "A UI component managing focus for its children (e.g., Listbox, Grid) via specific keyboard patterns like Roving Tabindex.",
    "usage": "Standardized by Composite Widget Protocol."
  },
  {
    "term": "content-visibility",
    "definition": "CSS property for lazy rendering. 'hidden' skips rendering but keeps element in accessibility tree. 'auto' renders only when near viewport.",
    "usage": "Standard (Baseline). Preferred for <code>view:show</code> as it provides best performance while maintaining accessibility."
  },
  {
    "term": "Context",
    "definition": "A mechanism for hierarchical data flow and service provision across component trees, independent of the DOM structure.",
    "usage": "Implemented by Web Contexts."
  },
  {
    "term": "Dependency Injection",
    "definition": "A design pattern where a component's dependencies are provided to it, rather than created by it. Enables loose coupling and easy testing.",
    "usage": "Implemented by Web Injectors."
  },
  {
    "term": "Directive",
    "definition": "Structural markers (loops, conditionals) and comment logic that manipulate the DOM structure.",
    "usage": "Implemented by Web Directives."
  },
  {
    "term": "Directionality",
    "definition": "The handling of text direction (LTR vs RTL) and its impact on layout, icons, and interaction logic.",
    "usage": "Core consideration for internationalization."
  },
  {
    "term": "disclosure",
    "definition": "A design pattern where a button controls the visibility of a content section. The button uses aria-expanded to communicate state.",
    "usage": "Implemented by NavSectionBehavior for collapsible navigation sections. Follows the W3C APG Disclosure Navigation pattern."
  },
  {
    "term": "Elevation",
    "definition": "The simulated Z-axis depth of an element, typically conveyed via shadows or layering, indicating hierarchy.",
    "usage": "Managed by Surface Intent."
  },
  {
    "term": "Fit",
    "definition": "The strategy for forcing content into a container (Cover, Contain, Fill) preserving aspect ratio.",
    "usage": "Used across Media and Layout blocks."
  },
  {
    "term": "Focus",
    "definition": "The orchestration of keyboard navigation and active element state. Includes trapping, restoring, and cycling focus.",
    "usage": "Managed by Interaction Intent."
  },
  {
    "term": "Forgivable",
    "definition": "Error tolerance level where the operation failed but the application state remains valid.",
    "usage": "A core principle of Reliability Intent."
  },
  {
    "term": "hidden/visible",
    "definition": "Binary visibility states used in toggle events. Preferred over 'open/closed' or 'active/inactive' for consistency with Popover API.",
    "usage": "Standard event detail shape: <code>{ oldState: 'hidden', newState: 'visible' }</code>"
  },
  {
    "term": "hidden=\"until-found\"",
    "definition": "HTML attribute value that hides content but allows browser search (Ctrl+F) to find and reveal it.",
    "usage": "Standard (Interop 2025). Excellent for accordions, FAQs, and searchable expandable content."
  },
  {
    "term": "Iconography",
    "definition": "Visual symbols standards including sizing scales, stroke weights, and positioning strategies to ensure optical balance.",
    "usage": "Standardized by Icon Intent."
  },
  {
    "term": "if/else (view)",
    "definition": "Conditional rendering that adds/removes content from DOM. Clean unmount with no state retention.",
    "usage": "Implemented by ViewIfDirective. Use for expensive components, auth gates, or when cleanup is needed. Contrast with <code>view:show</code> (ViewShowBehavior) which toggles visibility only."
  },
  {
    "term": "inert",
    "definition": "HTML attribute that makes element and descendants non-interactive. Removed from tab order and accessibility tree for interaction.",
    "usage": "Standard. Use for preview states, disabled sections, or content behind modals."
  },
  {
    "term": "Input",
    "definition": "Standardization of data entry interfaces, including affordances, privacy masking, virtual keyboard types, and visual status rendering. Status renders the DisplayDecision from Validation Intent (see OP-21).",
    "usage": "Standardized by Input Intent. The <code>status</code> dimension visually presents validity state from Validation Intent; there is one source of truth for field state."
  },
  {
    "term": "Interaction",
    "definition": "The handling of input modalities (pointer, touch, gamepad, keyboard) and the feedback loops required for each.",
    "usage": "Standardized by Interaction Intent."
  },
  {
    "term": "keep-alive",
    "definition": "Feature that preserves component state when hidden instead of destroying it. Supports max cache limit with LRU eviction.",
    "usage": "Inspired by Vue KeepAlive. Essential for tabs where users expect form state to persist."
  },
  {
    "term": "Keyboard Shortcuts",
    "definition": "A system for registering and handling key combinations that works with Shadow DOM and standard OS conventions.",
    "usage": "Standardized by Web Blocks."
  },
  {
    "term": "Layered Architecture",
    "definition": "The organization of protocols from abstract intents down to concrete implementation details.",
    "usage": "Core philosophy of Web Everything."
  },
  {
    "term": "Layout",
    "definition": "The application shell structure and region mechanics (Drawers, Rails, Panels) that define the macro-layout.",
    "usage": "Standardized by Layout Intent."
  },
  {
    "term": "Lifecycle",
    "definition": "The standardized state machine (Idle -> Loading -> Success/Error) that drives the UI. It separates the 'Process' (Time) from the 'Outcome' (Data).",
    "usage": "Resource Loaders and Actions must expose their lifecycle capabilities."
  },
  {
    "term": "Loading State",
    "definition": "The visualization of pending operations, defining how the UI reacts (Blocking, Skeleton, Spinner) to latency.",
    "usage": "Standardized by Loader Intent."
  },
  {
    "term": "Messaging",
    "definition": "The system for prioritizing and displaying communication to the user, from subtle toasts to blocking alerts.",
    "usage": "Standardized by Message Intent."
  },
  {
    "term": "Modal",
    "definition": "A dialog or overlay that forces user interaction before returning to the main content. Defines stacking, scrolling, and dismissal behavior.",
    "usage": "Standardized by Modal Intent."
  },
  {
    "term": "Module",
    "definition": "A self-contained unit of functionality or UI that expects a specific host context.",
    "usage": "Building block of Web Architecture."
  },
  {
    "term": "Motion",
    "definition": "The physics, latency tolerance, and transition behaviors that define the 'feel' of the application.",
    "usage": "Standardized by Motion Intent."
  },
  {
    "term": "name (exclusive groups)",
    "definition": "Attribute that groups elements for mutual exclusivity. Only one element with the same name value can be active/visible at a time.",
    "usage": "Standard pattern from HTML (radio buttons, <code>&lt;details name&gt;</code> accordion). Preferred over 'group' attribute."
  },
  {
    "term": "Navigation",
    "definition": "The structure of application flow (Hierarchical, Lateral, Linear) and the mechanism for transitioning between views.",
    "usage": "Standardized by Navigation Intent."
  },
  {
    "term": "navigation landmark",
    "definition": "A nav element that identifies a section containing navigation links. Use aria-label when multiple landmarks exist on the same page.",
    "usage": "NavListBehavior attaches to nav elements. The HTML nav element creates an implicit navigation ARIA landmark."
  },
  {
    "term": "outlet",
    "definition": "A named insertion point for routed or projected content. Enables router integration and parent-controlled content projection.",
    "usage": "Term from React Router and Angular. Use <code>view:outlet=\"name\"</code> for named outlets."
  },
  {
    "term": "Polyfill",
    "definition": "Code that implements a feature on web browsers that do not support the feature.",
    "usage": "Provided by Web Plugs."
  },
  {
    "term": "Positioning",
    "definition": "Declarative strategies for placing elements relative to the viewport or other elements.",
    "usage": "Standardized by Layout (regions) and the Anchor Intent (tethered surfaces). Element-relative anchoring is delegated to a Positioning Provider via the Web Positioning plug."
  },
  {
    "term": "Prefetching",
    "definition": "The strategy of loading resources before they are required, balanced against bandwidth and battery constraints.",
    "usage": "Standardized by Prefetch Intent."
  },
  {
    "term": "Registry",
    "definition": "A system for defining and looking up capabilities or components via a global or scoped lookup mechanism.",
    "usage": "Implemented by Web Registries."
  },
  {
    "term": "Reliability",
    "definition": "Strategies for error recovery, retry policies, and maintaining a viable state during failure conditions.",
    "usage": "Standardized by Reliability Intent."
  },
  {
    "term": "Resource Action",
    "definition": "A behavioral directive that attaches asynchronous mutation lifecycle management to UI triggers.",
    "usage": "Part of Web Blocks."
  },
  {
    "term": "Resource Loader",
    "definition": "A structural pattern used to handle asynchronous data fetching and state display in-place.",
    "usage": "Part of Web Blocks."
  },
  {
    "term": "Roving Tabindex",
    "definition": "A focus management pattern where `tabindex=\"0\"` moves to the currently active item, while others are set to `-1`.",
    "usage": "Used in Composite Widgets. One of the two strategies of the Focus Delegation Intent (the other being Virtual Focus)."
  },
  {
    "term": "Virtual Focus",
    "definition": "A focus management pattern where DOM focus stays on a controller element (e.g. a text input or listbox) while the visually active item is tracked via `aria-activedescendant`. Unlike real focus, the active item must be scrolled into view manually — browsers do not auto-scroll `aria-activedescendant` targets.",
    "usage": "The `virtual` strategy of the Focus Delegation Intent; required for comboboxes where typing must continue uninterrupted. Complements Roving Tabindex."
  },
  {
    "term": "Selection Follows Focus",
    "definition": "A pattern where moving focus to an item also activates or selects it (single-select listboxes, auto-activated tabs), as opposed to focus moving independently of selection (multi-select, menus, comboboxes), where selection commits on an explicit key.",
    "usage": "The `selectionFollowsFocus` dimension of the Focus Delegation Intent; composes with the Selection Intent's commitment model."
  },
  {
    "term": "Selection",
    "definition": "The behavior of choosing items from a set, defining cardinality (Single vs Multi) and required state.",
    "usage": "Standardized by Selection Intent."
  },
  {
    "term": "Skeleton",
    "definition": "A placeholder shaped like the content that will (or may) load, shown during an indeterminate wait. It sets the user's expectation of the incoming content's nature and limits layout shift when real content arrives; preferably subtly animated to indicate ongoing work. Distinct from a loader (an abstract activity animation such as a spinner) in that it mirrors the content's shape.",
    "usage": "Standardized by Loader Intent (the <code>skeleton</code> value of the <code>indicator</code> dimension, typically over the <code>replacement</code> strategy)."
  },
  {
    "term": "show/hide (view)",
    "definition": "Toggle visibility without removing from DOM. Element remains in accessibility tree and preserves state.",
    "usage": "Implemented by ViewShowBehavior (<code>view:show</code>) for expression-bound visibility, or ViewBehavior (<code>view</code> attr) for command-driven toggling. Use for frequent toggles where state preservation matters."
  },
  {
    "term": "State Management",
    "definition": "Standard primitives for observability, data binding, and reaction to data changes (Signals, Stores).",
    "usage": "Implemented by Web States."
  },
  {
    "term": "Surface",
    "definition": "The definition of visual depth (Z-Axis), material qualities (Opaque, Transparent), and their interactive properties.",
    "usage": "Standardized by Surface Intent."
  },
  {
    "term": "switch/case/default (view)",
    "definition": "Multi-branch conditional rendering where one case is shown based on expression value. More declarative than multiple if statements.",
    "usage": "Implemented by ViewSwitchDirective. Matches Angular ngSwitch, Solid Switch/Match. Use <code>view:switch</code> on parent, <code>view:case</code>/<code>view:default</code> on children."
  },
  {
    "term": "tablist/tab/tabpanel",
    "definition": "ARIA roles for accessible tab interfaces. Container has role=tablist, triggers have role=tab, content has role=tabpanel.",
    "usage": "Required by WAI-ARIA Tabs Pattern. Must include aria-selected, aria-controls, aria-labelledby."
  },
  {
    "term": "Time & Date",
    "definition": "The unified handling of temporal selection and display across different input modalities (Calendar, Dial, Text).",
    "usage": "Standardized by Temporal Intent."
  },
  {
    "term": "toggle (event)",
    "definition": "Event fired after a visibility change completes. Non-cancelable notification of completed state transition.",
    "usage": "Matches Popover API and <code>&lt;details&gt;</code> element pattern. Event detail contains <code>oldState</code> and <code>newState</code>."
  },
  {
    "term": "Token",
    "definition": "A parsed unit of meaning within a text node or attribute, often used for data binding.",
    "usage": "Handled by Web Tokens."
  },
  {
    "term": "Trace",
    "definition": "A record of an execution path through the application. Composed of multiple 'Spans' (units of work).",
    "usage": "Standardized by Web Traces."
  },
  {
    "term": "Tracer",
    "definition": "An engine responsible for recording execution flow (Traces) and context. It can be injected into any scope.",
    "usage": "Standardized by Web Traces."
  },
  {
    "term": "Tracing Type",
    "definition": "A categorization tag for traces (e.g., 'Network', 'Render', 'UserInteraction') allowing different tracers to subscribe to specific domains.",
    "usage": "Standardized by Web Traces."
  },
  {
    "term": "Trait",
    "definition": "A granular, composable capability mixed into a component class (e.g., `withSortable`).",
    "usage": "Standardized by Web Traits."
  },
  {
    "term": "trigger",
    "definition": "An element that initiates an action on another element. In tabs: tab-trigger. In dropdowns: dropdown-trigger.",
    "usage": "Common term across UI libraries. With Invoker Commands, triggers use <code>commandfor</code> attribute."
  },
  {
    "term": "Type-Ahead",
    "definition": "A keyboard navigation pattern where typed characters accumulate into a buffer that moves focus to the first item whose label matches; the buffer resets after an idle timeout. Distinct from filtering — items are never removed.",
    "usage": "Standardized by Type-Ahead Intent and the Type-Ahead block. Consumed by Droplist and composes with Composite Widget."
  },
  {
    "term": "Typography",
    "definition": "The abstraction of visual text hierarchy (Headline, Body, Caption) from semantic HTML elements.",
    "usage": "Standardized by Typography Intent."
  },
  {
    "term": "Validation",
    "definition": "The lifecycle of data integrity checking, error message placement, and submission blocking.",
    "usage": "Standardized by Validation Intent."
  },
  {
    "term": "view",
    "definition": "A region of content whose visibility is managed. Framework-agnostic term covering show/hide, structural rendering, and command-driven toggling.",
    "usage": "Foundation module providing ViewEngine (shared core), ViewBehavior (<code>view</code> attr), ViewShowBehavior (<code>view:show</code>), ViewIfDirective (<code>view:if</code>), ViewSwitchDirective (<code>view:switch</code>). Higher-level components (Tabs, Router) build on these."
  },
  {
    "term": "ViewEngine",
    "definition": "Shared utility providing hidden mode management, toggle events (beforetoggle/toggle), exclusive group coordination, and View Transitions integration.",
    "usage": "Not a block itself. Imported by View blocks and higher-level components (Router uses it directly). Handles HOW to hide/show; blocks handle WHEN."
  },
  {
    "term": "Virtual Element",
    "definition": "An element implementation using markers (e.g., comments) to avoid introducing redundant container nodes in the DOM.",
    "usage": "Used elsewhere to preserve CSS Grid/Flex relationships."
  },
  {
    "term": "Web Case",
    "definition": "See 'Case'.",
    "usage": "See 'Case'."
  },
  {
    "term": "route",
    "definition": "A URL pattern to view mapping. Defines which content displays for a given URL path.",
    "usage": "Standard term from all major routers. Pattern syntax follows URLPattern API: <code>/users/:id</code>, <code>/files/*</code>."
  },
  {
    "term": "guard",
    "definition": "A predicate/policy attached to a <a href=\"#term-guarded-region\">region</a>, evaluated at a lifecycle event of that region, whose deny-branch resolves to a member-specific outcome. The route-navigation case (Angular <code>canActivate</code>/<code>canDeactivate</code>) is one member; the concept is scale-invariant across the document, routes, modals, and elements.",
    "usage": "Standardized by the <a href=\"/projects/webguards/#protocol-guard\">Guard protocol</a> (Web Guards), which fixes only the seam — region + event + predicate + <a href=\"#term-guard-provider\">guard provider</a> — and leaves each member's <a href=\"#term-deny-outcome\">deny-outcome</a> to that member. Surface vocabulary: <code>route:guard=\"requireAuth\"</code> (<a href=\"#term-entry-guard\">entry guard</a>), <code>route:guard:leave=\"confirmUnsaved\"</code> (<a href=\"#term-exit-guard\">exit guard</a>)."
  },
  {
    "term": "loader",
    "definition": "Async function that fetches data before a route activates. Result available in route context.",
    "usage": "Term from React Router 6+ and Remix. Used as <code>route:loader=\"loadUser\"</code>. Data accessible via <code>@route.data</code>."
  },
  {
    "term": "Navigation API",
    "definition": "Modern browser API (Baseline 2024) replacing History API. Provides navigation events, traversal info, and interception capabilities.",
    "usage": "Primary navigation mechanism. Use <code>navigation.addEventListener('navigate', ...)</code> for route interception."
  },
  {
    "term": "URLPattern",
    "definition": "Web standard API (Baseline 2025) for matching URLs against patterns with named groups and wildcards.",
    "usage": "Use for route pattern matching. Syntax: <code>/users/:id</code> for named params, <code>*</code> for wildcards, <code>:param?</code> for optional."
  },
  {
    "term": "prefetch",
    "definition": "Strategy for loading route resources before navigation. Can load on hover, when visible, or eagerly.",
    "usage": "Use <code>route:prefetch=\"hover\"</code> on links. Integrates with Speculation Rules API when available."
  },
  {
    "term": "scroll restoration",
    "definition": "Browser or custom control over scroll position after navigation. Navigation API provides native support.",
    "usage": "Configure via <code>route:config scroll=\"auto|manual|none\"</code>. 'auto' uses browser behavior, 'manual' for custom control."
  },
  {
    "term": "SSR Contract",
    "definition": "The specification of what server output MUST look like for a given Web Everything primitive, and what the client expects during hydration.",
    "usage": "Each project defines its SSR Contract in a dedicated section on its project page."
  },
  {
    "term": "Hydration",
    "definition": "The process by which client-side JavaScript attaches interactivity to server-rendered HTML without re-rendering the DOM.",
    "usage": "Described in each project's SSR Contract section."
  },
  {
    "term": "Progressive Enhancement Baseline",
    "definition": "The minimum functionality available when JavaScript is disabled or not yet loaded. Server-rendered content should be usable at this baseline.",
    "usage": "Defined per project in SSR Contracts."
  },
  {
    "term": "Hydration Marker",
    "definition": "A DOM annotation (comment node, data attribute, or JSON script block) that enables the client to associate server-rendered content with its reactive bindings.",
    "usage": "Used by Web Expressions SSR for expression rebinding. Formats: comment markers, <code>data-expr</code> attributes, JSON hydration maps."
  },
  {
    "term": "Declarative Shadow DOM",
    "definition": "The <code>&lt;template shadowrootmode&gt;</code> pattern that allows shadow DOM content to be included in server-rendered HTML and parsed by the browser without JavaScript.",
    "usage": "Used by Web Components SSR Contract for custom elements with shadow DOM."
  },
  {
    "term": "Conformance Level",
    "definition": "The tier at which a validation implementation conforms to the Validation Intent contract. Level 0: Intent-aware. Level 1: observable states and events (swappable). Level 2: canonical shapes and DI concerns (combinable).",
    "usage": "Standardized by Validation Intent. Conformance is defined against observable contract, never computation strategy."
  },
  {
    "term": "Validity State",
    "definition": "The four parallel regions of a validation control's state: Validity (merged + version token), Interaction (dirty/touched/focused/submitted), Commit (committed/rejected/held), Display (silent/showing-{severity}).",
    "usage": "Defined in Validation Intent per-level vocabulary. Each region is independently observable and transitions emit events."
  },
  {
    "term": "Validity Pending",
    "definition": "An async validation state carrying a version/generation token. The token is the input's snapshot version at dispatch time; results from old versions are discarded (stale-resolve prevention).",
    "usage": "Composes Loader Intent's pending lifecycle. Cross-cutting requirement for all async validation systems (RHF #1688, Final Form #420)."
  },
  {
    "term": "Commit Policy",
    "definition": "The validation-level dimension that defines data persistence rules: error rejects (blocks submission), warning/info commit, caution holds until user confirms.",
    "usage": "Standardized by Validation Intent <code>level</code> dimension. Separates severity/tone from data-gating behavior."
  },
  {
    "term": "Error Summary",
    "definition": "A form-level aggregate list of all validation errors, displayed at the top (GOV.UK pattern). Ordered by DOM, linked to fields, focused on submit-blocked, with verbatim parity to inline messages.",
    "usage": "Optional feature <code>validation.feature.error-summary</code> at Conformance Level 1+. Combines with inline messages for dual-model a11y and discoverability."
  },
  {
    "term": "Reward Early Punish Late",
    "definition": "A display policy: until a field errors, show validation lazily (on blur). Once errored, flip to eager (on change) for immediate feedback. Revert to lazy on fix. A dynamic behavior (not a flat execution value) driven by interaction state.",
    "usage": "Emerges naturally from Validation Intent's DisplayDecision gating by visibility intent. Research: Konjević, Smashing Magazine. Prevents annoying re-validation while encouraging rapid correction."
  },
  {
    "term": "Dirty Check",
    "definition": "The concern that determines whether current field value differs from its baseline (pristine state). Algorithms: strict ===, deep equal, normalized (trim/case), custom comparators.",
    "usage": "A decomposed DI concern (<code>customValidation:dirty-check</code>). Swappable per control or app-wide. Required for interaction state tracking."
  },
  {
    "term": "Change Detection",
    "definition": "The concern that detects when a field value has changed: event listeners, signal subscriptions, polling, MutationObserver. Triggers value-input events.",
    "usage": "A decomposed DI concern (<code>customValidation:change-detection</code>). The event-driven vs state-derived architectural choice collapses into this concern + inter-element-comms; the other 6 concerns are architecture-agnostic."
  },
  {
    "term": "Severity Level",
    "definition": "The unified semantic axis shared by Validation (<code>level</code>), Feedback (<code>severity</code>), and Message (<code>tone</code>): error (failure), caution (critical advisory), warning (advisory), info (informational), success (confirmation).",
    "usage": "Defined across intents. Allows validation messages, toasts, and alerts to speak the same language. Replaces the triplicated vocabulary (feedback.severity / message.tone / validation.level)."
  },
  {
    "term": "Dependency Graph",
    "definition": "A <code>dependsOn</code> relation at group scope: field B depends on field A. When A changes, B is revalidated reactively. Enables cross-field and conditional validation without imperative handlers.",
    "usage": "Optional feature <code>validation.feature.cross-field</code>. Required for password-confirm, date ranges, required-if patterns. Fixes RHF #trigger and Angular updateValueAndValidity revalidation complexity."
  },
  {
    "term": "Recovery Handler",
    "definition": "A service registered in <code>CustomRecoveryHandlerRegistry</code> that decides whether and how to recover from a mechanism failure. Implements <code>tryRecover(error, context)</code> returning a <code>RecoveryResult</code> or <code>null</code> to decline. Handlers are tried in registration order; the first non-null result wins.",
    "usage": "Web Reliability. Distinct from validation error handling — recovery handlers respond to system/operation failures (timeouts, server errors), not input invalidity."
  },
  {
    "term": "Recovery Outcome",
    "definition": "The instruction a recovery handler returns to the consumer: <code>retry</code> (re-execute after optional delay), <code>queued</code> (stored for later replay), <code>fallback</code> (surface fallback content), or <code>abort</code> (propagate error immediately).",
    "usage": "Web Reliability — <code>RecoveryResult.outcome</code>. Maps to Reliability Intent dimensions: retry → recovery.automatic/manual; fallback → fallback.skeleton/empty/previous; abort → tolerance.terminal."
  },
  {
    "term": "Mechanism Failure",
    "definition": "An unexpected system or operation error — network unreachable, server crash, database timeout, computation failure. Distinguished from input invalidity (Validation domain): the user's data was correct; the mechanism broke.",
    "usage": "Web Reliability, Reliability Intent. Determines which domain owns the error: mechanism failures → Reliability; input errors → Validation."
  },
  {
    "term": "Change Tracking",
    "definition": "The observability concern of detecting, representing, and attributing mutations to state over time. A facet of observability in general — not limited to form dirty-state — that recurs across stores, signals, documents, collections, the DOM, and network sync, and that the field solves with plural strategies (snapshot diff, Proxy, signals, patches, CRDTs, event sourcing).",
    "usage": "The framing term for the Change Tracking standard (research topic <code>change-tracking-observability</code>). Generalizes the validation <code>customValidation:dirty-check</code> and <code>customValidation:change-detection</code> concerns. Goal: configurable preferred strategy + interop between multiple strategies in one app."
  },
  {
    "term": "Change Record",
    "definition": "The normalized representation of a single mutation that every change-tracking strategy maps into: <code>path</code> (RFC 6901 pointer), <code>op</code> (RFC 6902 vocabulary), optional <code>oldValue</code>/<code>newValue</code>, <code>source</code> (attribution), <code>timestamp</code>, and optional <code>version</code> causal token. A lossy lingua franca for observation — not a replacement for any strategy's native form.",
    "usage": "Provisional interop target (research topic <code>change-tracking-observability</code>); shape to be finalized in the Change Tracking protocol. <code>oldValue</code> is carried because RFC 6902 ops are not self-inverting."
  },
  {
    "term": "Change Source",
    "definition": "The origin of a mutation carried on a Change Record so cross-layer consumers can discriminate cause: <code>user</code> input, <code>program</code> mutation, <code>server-sync</code>, <code>replay</code>/<code>undo</code>, or <code>external</code>. May carry a W3C Trace Context <code>traceparent</code> linking the change to the initiating span.",
    "usage": "The attribution axis of Change Tracking. Generalizes validation's <code>value-input</code> (user) vs <code>baseline-change</code> (program/server) distinction; mirrors the CloudEvents <code>source</code> envelope field."
  },
  {
    "term": "Change Strategy",
    "definition": "A registered mechanism that turns a tracked target into a stream of Change Records, satisfying the <code>CustomChangeStrategy</code> contract (<code>track</code>, optional <code>diff</code>, optional <code>applyInverse</code>). Capability is feature-detected by which optional methods are present. Examples: native-signals, snapshot-diff, proxy, patch, crdt, event-sourced.",
    "usage": "Change Tracking Protocol (Web States). The native store/signal subscription strategy is the zero-config default; library strategies are opt-in adapters behind the same contract."
  },
  {
    "term": "Change Strategy Registry",
    "definition": "The <code>CustomChangeStrategyRegistry</code> resolved through the injector chain that selects the active Change Strategy per scope, so different subtrees of one app can track changes with different strategies simultaneously (e.g. a form on snapshot-diff, a collaborative document on a CRDT).",
    "usage": "Change Tracking Protocol (Web States). Generalizes the validation <code>customValidation:change-detection</code> concern from one form to all observable state. Nearest-scope strategy wins."
  },
  {
    "term": "Render Strategy",
    "definition": "A registered mechanism that turns a parsed element tree plus a change source into committed DOM updates, satisfying the <code>CustomRenderStrategy</code> contract (<code>mount</code>, optional <code>update</code>, optional <code>dispose</code>). Capability is feature-detected by which optional methods are present — a strategy without <code>update</code> is mount-once. The four registered strategies are declarative-static (the native-first default), vdom, fine-grained, and imperative. The sibling of <a href=\"#change-strategy\">Change Strategy</a>: change strategies detect what mutated, render strategies commit how the DOM reflects it.",
    "usage": "Render Strategy Protocol (Web Components). A technical contract, not a UX preference — surfaces under /protocols/, never /intents/. The JSX adapter and the declarative &lt;component&gt; element are consumers that target a chosen strategy."
  },
  {
    "term": "Render Strategy Registry",
    "definition": "The <code>CustomRenderStrategyRegistry</code> resolved through the injector chain that selects the active Render Strategy per scope, so different subtrees of one app render with different strategies simultaneously (a static header on declarative-static, a live grid on fine-grained). Mirrors the resolution rule of <a href=\"#change-strategy-registry\">Change Strategy Registry</a> — nearest-scope wins.",
    "usage": "Render Strategy Protocol (Web Components). The registry is open, not a fixed enum: a future platform target (DOM Parts / Template Instantiation) registers as another provider without touching the contract."
  },
  {
    "term": "Render Input",
    "definition": "The payload a Render Strategy realizes — a <code>RenderInput</code> tagged union keyed by strategy id, where each variant carries that strategy's own shape: a parsed template + binding map (declarative-static), a <code>render()</code> thunk returning a vtree (vdom), a reactive computation (fine-grained), or an imperative builder (imperative). One union keeps the registry signature non-generic (mirroring how <a href=\"#change-strategy\">Change Strategy</a> takes a wide <code>target</code> rather than <code>CustomChangeStrategy&lt;T&gt;</code>); the registered provider validates the variant it receives.",
    "usage": "Render Strategy Protocol (Web Components). The <code>strategy</code> discriminant equals the provider's <code>readonly key</code>, so resolution and input validation share one id space."
  },
  {
    "term": "Lowering",
    "definition": "Compiling a higher-level render-strategy expression of control-flow into a lower-level declarative-static primitive — e.g. JS <code>items.map(…)</code> into <code>&lt;template is=\"for-each\"&gt;</code>, or a ternary into <code>&lt;template is=\"if\"&gt;</code>. The inverse of <a href=\"#lifting\">Lifting</a>. Structural control-flow lowers reversibly; the eager <code>{expr}</code> ⇄ reactive <code>{{ expr }}</code> boundary is lossy.",
    "usage": "Render Strategy Protocol cross-strategy compiler. Distinct from same-strategy spelling conversion (Axis-1), which is reversible by construction."
  },
  {
    "term": "Lifting",
    "definition": "The inverse of <a href=\"#lowering\">Lowering</a> — recovering a higher-level render-strategy expression (JS <code>items.map(…)</code>) from a declarative-static directive (<code>&lt;template is=\"for-each\"&gt;</code>). Structural directives lift cleanly; lifting cannot recover whether a <code>{{ }}</code> path was authored as an eager value or a tracked binding, so that boundary is declared lossy.",
    "usage": "Render Strategy Protocol cross-strategy compiler."
  },
  {
    "term": "Change Adapter",
    "definition": "A thin bridge that maps a third-party change source (Immer/Mutative patches, Yjs/Automerge ops, a JSON Patch stream) into the canonical Change Record contract, or between two strategies' formats. Because mappings are lossy, bridges are adapters rather than identities.",
    "usage": "Change Tracking Protocol (Web States). Enables a signals graph → JSON-Patch stream → CRDT document pipeline with the Change Record as the hand-off."
  },
  {
    "term": "Baseline",
    "definition": "The pristine reference snapshot a diff or dirty strategy compares the current value against to derive change. Re-captured on a <code>baseline-set</code> event (initial load, save, server sync). For SSR, the serialized initial store state is the post-hydration baseline.",
    "usage": "Change Tracking Protocol (Web States); reconciles with the Validation Intent's existing baseline notion behind <code>customValidation:dirty-check</code>."
  },
  {
    "term": "Structural Diff",
    "definition": "Change detection by comparing two immutable snapshots (baseline vs current) and deriving the set of Change Records between them. Sampled coverage — intermediate states between snapshots are invisible — but library-agnostic and trivially correct; the natural fallback when no interception is available.",
    "usage": "A Change Strategy supplying the optional <code>diff(baseline, current)</code> method. Contrast with interception strategies (Proxy, signals) that capture every write as it happens."
  },
  {
    "term": "Change Patch",
    "definition": "A serializable, optionally invertible set of Change Records aligned to JSON Patch (RFC 6902). Forward patches apply a change; inverse patches (carrying <code>oldValue</code>, since RFC 6902 ops are not self-inverting) revert it, enabling undo/replay. Immer and Mutative emit forward + inverse patch pairs.",
    "usage": "The transport-friendly representation produced by patch and snapshot Change Strategies. The <code>change-reverted</code> event applies an inverse Change Patch."
  },
  {
    "term": "Localization",
    "definition": "Turning a single UI into many locales: composing messages from static text plus formatted values, resolving keys against catalogs, walking locale fallback chains, and adapting writing direction. Distinct from native value formatting, which the platform already provides via <code>Intl</code>.",
    "usage": "The domain of Web Intl, specified by the Localization Protocol and declared by the Locale Intent."
  },
  {
    "term": "Locale Negotiation",
    "definition": "Expanding a requested locale into an ordered fallback chain of available locales (e.g. <code>fr-CA</code> → <code>fr</code> → <code>en</code>), so a missing translation degrades gracefully to a more general locale rather than failing.",
    "usage": "Performed by the <code>LocaleNegotiation</code> interface; the Localization Protocol walks the resulting chain when resolving a key."
  },
  {
    "term": "Fallback Chain",
    "definition": "The ordered list of locales tried in turn when resolving a message key, produced by Locale Negotiation. Resolution stops at the first locale for which a Translation Provider returns a message.",
    "usage": "Walked by the Localization Protocol's resolution pattern; the outermost fallback is typically the application's default locale."
  },
  {
    "term": "Message Catalog",
    "definition": "A keyed set of message sources for one locale (and optionally one namespace). The data a Translation Provider reads to resolve a key into a Message Function.",
    "usage": "Loaded eagerly or lazily by a Translation Provider. Authored as JSON / PO / XLIFF / ARB / Fluent and normalized at build time."
  },
  {
    "term": "MessageFormat 2.0",
    "definition": "The Unicode message-formatting syntax (UTS #35 Part 9, published 2024) for composing messages with variable interpolation and plural/gender selection. Forward-compatible with the TC39 <code>Intl.MessageFormat</code> proposal.",
    "usage": "The default message syntax of the Localization Protocol; compiled by a MessageFormatter into a Message Function. Abbreviated MF2."
  },
  {
    "term": "Message Function",
    "definition": "A resolved message represented as <code>(params?) =&gt; string</code> — a function that formats a message with the given parameters using native <code>Intl</code>. The common shape for runtime (MF2) and compile-time (tree-shaken) messages alike.",
    "usage": "Returned by <code>CustomTranslationProvider.resolve()</code>; typed as <code>MessageFn</code>."
  },
  {
    "term": "Translation Provider",
    "definition": "A registered source that resolves a message key for a locale to a Message Function, or declines with <code>null</code>. A native <code>Intl</code> MF2 formatter, lazy-namespace catalog, or library adapter (FormatJS, i18next, Lingui, Paraglide).",
    "usage": "Satisfies the CustomTranslationProvider contract; registered in CustomTranslationProviderRegistry and resolved through the injector chain. First provider that resolves a key owns the message."
  },
  {
    "term": "Bidi",
    "definition": "Bidirectional text handling — laying out content that mixes left-to-right and right-to-left scripts per the Unicode bidirectional algorithm. The reason layout flows by inline start/end rather than physical left/right.",
    "usage": "Driven by the Locale Intent <code>direction</code> dimension via the <code>dir</code> attribute, <code>:dir()</code>, and CSS logical properties. <code>direction: auto</code> defers to the bidi algorithm."
  },
  {
    "term": "Locale Switching",
    "definition": "Changing the application's active locale at runtime, and the delivery strategy governing how the change reaches the UI: synchronously from preloaded catalogs (<code>instant</code>), via an on-demand catalog fetch behind a loading state (<code>async</code>), or through a full per-locale document load (<code>reload</code>).",
    "usage": "The <code>switching</code> dimension of the Translation Intent. Orthogonal to which locale is active (the Locale Intent). <code>async</code> composes with the Loader Intent; <code>reload</code> with URL-based locale routing."
  },
  {
    "term": "Live Region",
    "definition": "A DOM region whose updates are announced to assistive technology without moving focus, via `aria-live` (`polite`/`assertive`) or an implicit role (`status`, `alert`, `log`). The region must already exist (empty) when the page loads so the AT registers it; text is then written into it on events.",
    "usage": "Standardized by the Live Region Status Intent. Announces loading, result counts, no-results, and errors. Distinct from the Message/Feedback intents, which own notification content and priority."
  },
  {
    "term": "Windowing",
    "definition": "Rendering only the visible window of a large collection (plus a small overscan) and recycling rows as the user scrolls, rather than placing every item in the DOM. Also called virtualization. Distinct from the Virtual Element (comment-marker) concept.",
    "usage": "The `windowed` rendering mode of the Windowed Collection Intent. Requires Set Size / Position in Set to preserve accessibility, and must not unmount the focused item."
  },
  {
    "term": "Set Size / Position in Set",
    "definition": "The `aria-setsize` (total item count) and `aria-posinset` (1-based absolute index) attributes that let assistive technology announce an item's position in a collection — e.g. 'item 42 of 5000' — even when not all items are present in the DOM.",
    "usage": "Required on every rendered item under Windowing; the index is the item's absolute position in the full collection, never its DOM order."
  },
  {
    "term": "Truncation",
    "definition": "Visually cutting text that overflows its box (`clip`, `ellipsis`, or `fade`) while keeping the full string as the element's text content. Must not slice mid-grapheme, and must provide a recovery path (tooltip/title or expand) to read the full value.",
    "usage": "The `truncation` dimension of the Typography Intent. Preserves WCAG 2.5.3 Label in Name because the accessible name stays the full text; front-load distinguishing words so they survive the cut."
  },
  {
    "term": "Clearable Value",
    "definition": "An 'X' affordance that resets a field's value. Shown only when a value is present, carries the accessible name 'Clear', is keyboard-reachable, and returns focus to the field after clearing. Decoupled from Escape so clearing is a deliberate action, not an accidental side-effect of dismissing a popup.",
    "usage": "The `clear` affordance of the Input Intent. Reused by search fields, comboboxes, tag inputs, and filters."
  },
  {
    "term": "Option Group",
    "definition": "A labeled subset of options within a selection control. Native: `<optgroup label>` (label required, no nesting). Custom: `role=\"group\"` + `aria-labelledby` referencing a visible heading; the heading is not a focusable option.",
    "usage": "The `grouping` dimension of the Selection Intent. Mirrors how menus, command palettes, and faceted lists organize choices."
  },
  {
    "term": "Declarative Custom Element",
    "definition": "A custom element whose definition — tag name, template, and shadow options — is expressed entirely in HTML markup, requiring no authored JavaScript class.",
    "usage": "The output of the <code>&lt;component&gt;</code> element in Web Components. Distinct from Declarative Shadow DOM, which declares an instance's shadow content but not a reusable element definition."
  },
  {
    "term": "Component Definition",
    "definition": "The <code>&lt;component name&gt;</code> declaration block — a name, a <code>&lt;template&gt;</code>, and shadow/registration options — that registers a Declarative Custom Element.",
    "usage": "Authored once per element type. The deterministic build-time AST transform lowers each Component Definition to a class plus customElements.define()."
  },
  {
    "term": "Definition Element",
    "definition": "The <code>&lt;component&gt;</code> element itself: a transient authoring element that reads a Component Definition, registers the custom element, then removes itself from the DOM leaving no wrapper node.",
    "usage": "Web Components. Shares the self-removal lifecycle of a Transient Component, but its purpose is registration rather than rendering."
  },
  {
    "term": "Trait Selection",
    "definition": "The act of choosing which traits to compose and what dimension values they take when constructing a component. Local, total, and developer-owned — expressed as attributes on the substrate (e.g. <code>selection=\"model=single\"</code>) or as a list of <code>with*</code> factories in TypeScript. Distinct from Ambient Intent, which is the designer-owned behavioral-preference channel propagated via DI.",
    "usage": "The structural-construction channel of the Droplist family. Structural dimensions (<code>model</code>, <code>editable</code>) never travel via Ambient Intent — they are always stated locally. Behavioral dimensions (<code>focusStrategy</code>, <code>collision</code>) may fall back to Ambient Intent when not stated."
  },
  {
    "term": "Ambient Intent",
    "definition": "A designer- or product-owned <em>behavioral</em> preference (focus strategy, collision handling, truncation) propagated down a DOM subtree via DI. Partial, overridable, and never structural — Ambient Intent cannot set <code>model</code> or <code>editable</code>; those are Trait Selection.",
    "usage": "The behavioral-preference channel in the Droplist family and other composable systems. Set via <code>injector.set('customContexts:droplistIntent', { focusStrategy: 'virtual', … })</code>; resolved per-dimension as <code>explicit ⊕ ambient ⊕ default</code>. The Loader Intent's <code>loaderIntent</code> provider is the original precedent."
  },
  {
    "term": "Droplist",
    "definition": "The abstract family — the substrate that every list-based selection surface (Dropdown, Multi-dropdown, Autocomplete, Tree-select, native <code>&lt;select&gt;</code>) conforms to. Not itself a shippable widget. (A menu button is not a member — it invokes commands via a role=menu owned by the Menu block; only its trigger shape is shared.)",
    "usage": "Standardized by the Droplist block. Documents the trait surface map, the two-channel resolution rule, and the preset table that each concrete member resolves into."
  },
  {
    "term": "Dropdown",
    "definition": "One concrete member of the Droplist family — single-select, button-triggered, non-filtering. Structural identity: <code>selection=\"model=single\"</code> with type-ahead.",
    "usage": "Standardized by the Dropdown block. A form-associated custom element so <code>&lt;form&gt;</code>/<code>FormData</code>/reset/validity work like the native <code>&lt;select&gt;</code> baseline."
  },
  {
    "term": "history disposition",
    "definition": "What a navigation contributes to session history: <code>push</code> (new entry), <code>replace</code> (overwrite current), or <code>none</code> (no entry). Determines what the browser Back button does after the navigation.",
    "usage": "The <code>history</code> dimension of the Navigation Intent. Maps to the Navigation API <code>navigate(url, { history })</code> option."
  },
  {
    "term": "deep link",
    "definition": "A URL that encodes enough application state (path + search params) to reconstruct a specific view directly — bookmarkable, shareable, and surviving a page refresh.",
    "usage": "The <code>url</code> value of the Navigation Intent's <code>persistence</code> dimension. Contrast <code>memory</code> (ephemeral, in-memory state) and <code>session</code> (carried in History <code>state</code>, survives traversal but not a fresh share)."
  },
  {
    "term": "view transition",
    "definition": "An animated transition played as one view replaces another during navigation, driven by the View Transitions API. Its <em>direction</em> (forward vs back) is a navigation concern; its <em>feel</em> (easing, reduced-motion) is a motion concern.",
    "usage": "The <code>transition</code> dimension of the Navigation Intent owns direction (<code>directional|fade|none</code>); the Motion intent's <code>physics</code> owns feel. Browser-initiated Back navigations carry no transition type, so direction is derived from history-traversal direction."
  },
  {
    "term": "leave guard",
    "definition": "Logic that can interrupt a navigation away from the current view — proceeding freely, prompting for confirmation (e.g. unsaved changes), or blocking until a condition clears.",
    "usage": "The <code>guard</code> dimension of the Navigation Intent (<code>free|confirm|blocked</code>). Grounded in the Navigation API <code>navigate</code> event (<code>preventDefault</code>/<code>intercept</code>) and <code>beforeunload</code>. Distinct from a route entry <code>guard</code> (canActivate); a leave guard is canDeactivate. The Router exposes it as <code>route:guard:leave</code>."
  },
  {
    "term": "scroll sentinel",
    "definition": "A zero-height marker element placed at the boundary of a scrollable list. When it enters the viewport, an IntersectionObserver fires and the next slice is requested — the native-first grounding for auto-advance (infinite scroll) without scroll-position math.",
    "usage": "The Pagination block wires <code>advance:auto</code> (the Collection Operations <code>advance</code> dimension) to a sentinel observed by <a href=\"https://developer.mozilla.org/en-US/docs/Web/API/IntersectionObserver\">IntersectionObserver</a>. Rendering of the grown list delegates to Windowed Collection; the <code>loadingMore</code> lifecycle to Loader. Only meaningful with <code>pageMode:append</code> (append + auto = infinite scroll)."
  },
  {
    "term": "result-range label",
    "definition": "A status label that names the visible slice of a paged collection — e.g. ‘showing 21–40 of 500’. Requires a known total, so it composes only over an offset/page resource protocol; a cursor protocol has no total and forces it off.",
    "usage": "The <code>rangeLabel</code> dimension of the Collection Operations Intent, surfaced by the Pagination block's <code>withRangeLabel</code> trait. Recommended page-size options, defaults, and how the label degrades when the total is unavailable are unverified — owned by <a href=\"/backlog/060-pagination-range-labels-page-size/\">backlog #060</a>."
  },
  {
    "term": "Reorder",
    "definition": "A user interaction that mutates the <em>order</em> of items in a collection while leaving the item set unchanged. Distinct from a computed sort (which derives order from a comparator) and from data transfer (which moves a payload between zones).",
    "usage": "Standardized by the <a href=\"/intents/reorder/\">Reorder Intent</a> and realized by the <a href=\"/blocks/reorderable-list/\">Reorderable List</a> block. The manual counterpart to the <a href=\"/intents/collection-operations/\">Collection Operations</a> <code>sort</code> dimension — a list is hand-ordered or sorted, never both at once."
  },
  {
    "term": "Reorderable List",
    "definition": "A list whose item order is user-mutable by both pointer drag and keyboard, with live-region announcement and a pluggable commit strategy. Relocates items atomically with <code>Element.moveBefore()</code> so a moved item keeps its focus, animation, and connection state.",
    "usage": "The <a href=\"/blocks/reorderable-list/\">Reorderable List</a> block; implements the <a href=\"/intents/reorder/\">Reorder Intent</a>. Pairs each item with <code>preserve-on-move</code> for the atomic relocation."
  },
  {
    "term": "Drag Handle",
    "definition": "A scoped affordance within an item that initiates a drag, as opposed to the whole item being draggable. Leaves the rest of the item interactive (links, buttons) while still allowing it to be moved.",
    "usage": "The <code>handle</code> value of the Reorder Intent's <code>grab</code> dimension, surfaced by the Reorderable List block's <code>withDragHandle</code> trait. Marked in markup with <code>[data-reorder-handle]</code>."
  },
  {
    "term": "Keyboard Reorder",
    "definition": "The keyboard-operable path for reordering — roving tabindex over items plus grab/move/drop keys plus a polite live-region announcement of each move. It is a mandatory part of the reorder contract, not an enhancement; the deprecated <code>aria-grabbed</code>/<code>aria-dropeffect</code> are not used.",
    "usage": "The <code>keyboard</code> value of the Reorder Intent's <code>movement</code> dimension, surfaced by the Reorderable List block's <code>withKeyboardReorder</code> trait. Composes <a href=\"/intents/focus-delegation/\">Focus Delegation</a> and <a href=\"/intents/live-region-status/\">Live Region Status</a>."
  },
  {
    "term": "Reorder Commit Strategy",
    "definition": "The strategy that decides where a new order is written after a reorder — ephemeral (session only), localStorage, or written back to the source. Distinct from the validation <a href=\"/semantics/#term-commit-policy\">Commit Policy</a>, which governs when form field edits are written.",
    "usage": "The <code>commit</code> dimension of the Reorder Intent, surfaced by the Reorderable List block's <code>withCommitStrategy</code> trait behind the cancelable <code>reorder-commit</code> event. Defaults to ephemeral."
  },
  {
    "term": "Tooltip",
    "definition": "A small, transient, non-interactive surface that appears on hover or keyboard focus of a trigger and supplements it with an accessible description. Wired via <code>aria-describedby</code> (it describes; it does not name), never as the sole carrier of essential information, and never holding focusable content. Must satisfy WCAG 1.4.13 — hoverable, persistent, and dismissible.",
    "usage": "Delivered by the <a href=\"/blocks/tooltip/\">Tooltip block</a>, composing the <a href=\"/intents/anchor/\">Anchor</a>, <a href=\"/intents/surface/\">Surface</a>, and <a href=\"/intents/typography/\">Typography</a> intents. Distinct from a Toggletip."
  },
  {
    "term": "Toggletip",
    "definition": "A click- or tap-revealed, re-summonable hint. Unlike a <a href=\"/semantics/#term-tooltip\">Tooltip</a> it has no persistent describing relationship with its trigger, so its content is announced through a live region rather than <code>aria-describedby</code>. Triggered by activation (not hover/focus), it is a separate concern from the tooltip.",
    "usage": "Cross-referenced by the <a href=\"/blocks/tooltip/\">Tooltip block</a> as an explicitly-out-of-scope sibling; announced via the <a href=\"/intents/live-region-status/\">Live Region Status</a> intent."
  },
  {
    "term": "Slider",
    "definition": "A control for choosing a numeric value by position along a track. The single-thumb case is the native <code>&lt;input type=\"range\"&gt;</code>; the block enhances only the gaps the native control can't express. Distinct from a numeric/stepper field — the slider is the continuous-along-a-track member.",
    "usage": "Delivered by the <a href=\"/blocks/slider/\">Slider block</a>, realizing the <a href=\"/intents/selection/\">Selection Intent</a>'s <code>range</code> variant and composing the <a href=\"/intents/input/\">Input</a> and <a href=\"/intents/locale/\">Locale</a> intents."
  },
  {
    "term": "Dual-Thumb Range",
    "definition": "A from–to range modelled as two coordinated thumbs over a single track, each with its own <code>aria-valuenow/min/max</code>, kept ordered (the low thumb cannot cross the high thumb) and reported as a pair. The platform has no single dual-thumb range element, so it is the standard composition for 'from–to'.",
    "usage": "The concrete realization of the <a href=\"/intents/selection/\">Selection Intent</a>'s <code>range</code> variant in the <a href=\"/blocks/slider/\">Slider block</a>: <code>constraints.min</code> / <code>constraints.max</code> become the two thumb bounds."
  },
  {
    "term": "Segmented Control",
    "definition": "A single-select choice rendered as a row of adjacent, always-visible buttons (the iOS/macOS segmented control or 'pill' toggle group). The <a href=\"/intents/selection/\">Selection Intent</a>'s <code>single</code> model in a horizontal, live-immediacy variant — exactly one option pressed, choosing commits immediately. Distinct from a multi-select toggle-button group and from Tabs (which disclose panels rather than set a value).",
    "usage": "Delivered by the <a href=\"/blocks/segmented-control/\">Segmented Control block</a>, composing <a href=\"/intents/selection/\">Selection</a>, <a href=\"/intents/focus-delegation/\">Focus Delegation</a> (roving, horizontal), and the Typography / Density intents."
  },
  {
    "term": "Compound Child",
    "definition": "A sub-element authored inside a parent block that acts as one of the parent's options, mapping to the parent's value model — e.g. <code>&lt;segment&gt;</code> in a segmented control, <code>&lt;option&gt;</code> in a native <code>&lt;select&gt;</code>. Under the JSX adapter the same authoring lowers to an <code>options</code> array rather than rendered child components. This is a shared authoring-and-lowering <em>paradigm</em>, not a UX intent: it has no user-facing dimensions of its own, so it lives as a semantics term that the <a href=\"/intents/selection/\">Selection Intent</a> and option-bearing blocks cross-reference, rather than as an intent or (yet) a protocol. It is a sibling of — not subordinate to — Selection's <code>grouping</code> dimension, which arranges options into labeled sets (<code>&lt;optgroup&gt;</code> / <code>role=group</code>) rather than declaring them.",
    "usage": "Exemplified directly by the <a href=\"/blocks/segmented-control/\">Segmented Control block</a> (<code>&lt;segment&gt;</code> children) and native <code>&lt;select&gt;</code> (<code>&lt;option&gt;</code> children). Tabs is a related-but-distinct shape: it binds via <code>tab-trigger</code>/<code>tab-panel</code> attributes on arbitrary elements rather than positional <code>&lt;tab&gt;</code> children. Promotion to a first-class protocol was considered and deferred (no proven multi-vendor interop demand); the one real conformance contract — how authored children lower to an <code>options</code> array under the JSX adapter — is specified by the <a href=\"#term-compound-child-lowering\">Compound-Child Lowering</a> term (with the <a href=\"#term-child-materialization-dialect\">Child-Materialization Dialect</a> axis governing <code>options</code> vs <code>compound</code> output), the latent protocol seed."
  },
  {
    "term": "Compound-Child Lowering",
    "definition": "The conformance contract behind the <a href=\"#term-compound-child\">Compound Child</a> paradigm: how a parent block's positional children lower to an <code>options</code> array under the JSX adapter instead of rendering as child components. <strong>Field mapping</strong> — a child's <code>value</code> attribute &rarr; <code>value</code>, its text content &rarr; <code>label</code>, <code>disabled</code> &rarr; <code>disabled</code>; a <code>selected</code>/<code>checked</code>/<code>pressed</code> child lifts to the parent's <code>value</code> (an array when the parent is <code>model=multiple</code>). Grouping (<code>&lt;optgroup&gt;</code>) and hierarchy (tree children) nest as a second structural axis (<code>{ group, options }</code> / <code>{ …, children }</code>). <strong>Round-trip</strong> is reversible for plain-text children; it turns lossy once a child carries rich markup — a flat <code>label</code> string cannot hold it — which the <a href=\"#term-child-materialization-dialect\">Child-Materialization Dialect</a> resolves (a <code>render</code> slot under the <code>options</code> form, or the lossless <code>compound</code> form). Tabs is <em>out of scope</em>: it binds via <code>tab-trigger</code>/<code>tab-panel</code> attributes on arbitrary elements, not positional children, so it keeps structural children and does not collapse to an <code>options</code> array.",
    "usage": "Specifies the one real contract named in <a href=\"#term-compound-child\">Compound Child</a>, exercised by the <a href=\"/blocks/segmented-control/\">Segmented Control block</a> (<code>&lt;segment&gt;</code>) and the Droplist family (already an <code>options</code> array via <code>&lt;template for-each&gt;</code>); worked authoring&hArr;JSX examples live in <a href=\"/backlog/281-specify-the-compound-child-to-options-array-lowering-under-t/\">backlog #281</a>. Writable as an adapter convention today, before any render-strategy protocol. Graduates to a first-class <code>compound-child</code> protocol only if independent-vendor interop demand on the child-authoring shape is proven; its natural owner would then be the render-strategy / component-compiler path, not Selection."
  },
  {
    "term": "Child-Materialization Dialect",
    "definition": "A structural JSX-authoring preference governing how a parent's compound children materialize: <code>options</code> (a flat data prop, <code>options={[…]}</code>) vs <code>compound</code> (subcomponents, <code>&lt;Parent.Option&gt;</code> authored as children). Both are faithful lowerings of the <em>same</em> authored HTML; the difference is the child tree's <em>shape</em>, which makes this axis orthogonal to the attribute-spelling JSX dialect (#235, <code>html</code>/<code>react</code>) that leaves the tree identical. The <code>compound</code> form round-trips rich-markup children losslessly (its children are JSX); the <code>options</code> form needs a <code>render</code> slot for the same case (see <a href=\"#term-compound-child-lowering\">Compound-Child Lowering</a>).",
    "usage": "A <em>soft</em> authoring preference, not a protocol — it offends a convention rather than breaking interop (the #150 protocol-vs-preference test) — so any enforcement lowers into an incumbent linter, never a Web Everything sheriff. The adapter is <strong>default-less</strong>: it resolves the dialect from a development-preference config the project extends in a chain (team &rarr; business line &rarr; company &rarr; a base config offered online); <code>options</code> is what a native-/static-first base config sets at the root, not a constant baked into the adapter. Input accepts either form regardless of the codegen choice (round-trip), mirroring how <code>jsxToHtml</code> ingests either attribute dialect. Alignment task: fold the adapter's baked <code>DEFAULT_DIALECT</code> into the base config — the same config-extends-platform-default migration tracked in #243."
  },
  {
    "term": "guarded region",
    "definition": "The scope a <a href=\"#term-guard\">guard</a> protects — scale-invariant across the document (the outermost region), a route, a modal, a panel, or a single element. A guard hooks a region's <em>cancelable</em> lifecycle event (a close button, route change, <code>beforeunload</code>), never raw DOM removal: imperative teardown that bypasses a guardable trigger cannot be caught.",
    "usage": "Defined by the <a href=\"/projects/webguards/#protocol-guard\">Guard protocol</a> (Web Guards) as the first element of its fixed seam (region + event + predicate + provider). Same intent applies whether the region is the whole document, a route, or a modal."
  },
  {
    "term": "guard provider",
    "definition": "The swappable policy engine that resolves a <a href=\"#term-guard\">guard</a>'s predicate — <code>CustomGuardProvider.evaluate(region, event, context): Promise&lt;GuardDecision&gt;</code>. Resolved through the injector chain (default platform provider &rarr; project override &rarr; custom plug); the provider contract is the protocol's <em>only</em> lock. Unlike the synchronous, client-side positioning provider it shares a pattern with, a guard provider crosses a trust boundary: it is <strong>async</strong>, <strong>server-authoritative</strong> (the front-end decision is a UX mirror, never enforcement), and <strong>revocable</strong> (an optional <code>subscribe()</code> invalidation signal). The <code>forbid</code>(403)/<code>cloak</code>(404) disclosure decision lives here, surfaced as <code>GuardDecision.reason</code> — not in the UX-only member intent.",
    "usage": "Held in <code>CustomGuardRegistry</code> (<code>window.customGuards</code>); the project selects/overrides it via the platform config it extends (flavors), the tool carrying no default. Standardized by the <a href=\"/projects/webguards/#protocol-guard\">Guard protocol</a> (Web Guards)."
  },
  {
    "term": "deny-outcome",
    "definition": "What a <a href=\"#term-guard\">guard</a>'s deny-branch resolves to. Deny-outcome families are member-specific and deliberately <em>not</em> unified: an <a href=\"#term-exit-guard\">exit guard</a>'s is a user-mediated <code>confirm</code> (stay/discard); an <a href=\"#term-entry-guard\">entry guard</a>'s is <code>hide | redirect | forbid | cloak</code>. This divergence is exactly why guard members are siblings over one provider seam, not one merged intent.",
    "usage": "Each member intent owns its deny-outcome; the <a href=\"/projects/webguards/#protocol-guard\">Guard protocol</a> fixes the provider seam but says nothing about the deny branch."
  },
  {
    "term": "entry guard",
    "definition": "The Guard member that gates <em>entering</em> a region — <em>may I activate/enter this?</em> Predicate = authorization/precondition; <a href=\"#term-deny-outcome\">deny-outcome</a> = <code>hide | redirect | forbid (403) | cloak (404)</code>, where the 403-vs-404 choice (revealing whether the resource exists) is a security call made behind the <a href=\"#term-guard-provider\">guard provider</a>. The routing-and-rendering face of access control.",
    "usage": "The access-control member (<a href=\"/backlog/178-access-control-authorization-gate/\">#178</a>) of the <a href=\"/projects/webguards/#protocol-guard\">Guard protocol</a>. Prior art: Angular <code>canActivate</code>, Vue route enter, CASL <code>&lt;Can&gt;</code> (rendering)."
  },
  {
    "term": "exit guard",
    "definition": "The Guard member that gates <em>tearing down</em> a region the user is in — <em>may I leave/dismiss this?</em> Predicate = loss-of-state (dirty form, unsaved editor, in-flight task); <a href=\"#term-deny-outcome\">deny-outcome</a> = a user-mediated <code>confirm</code> (stay/discard). Arms on the cancelable dismissal trigger at any scale: document (<code>beforeunload</code>), same-document route (Navigation API <code>intercept</code>), in-app dismissal (modal/panel close).",
    "usage": "The exit-guard member (<a href=\"/backlog/273-exit-guard-intent-confirm-before-tearing-down-a-region-with-/\">#273</a>) of the <a href=\"/projects/webguards/#protocol-guard\">Guard protocol</a>; successor of the navigation-guard intent (#129). Prior art: Angular <code>canDeactivate</code>, Vue <code>beforeRouteLeave</code>, React <code>useBlocker</code>, SvelteKit <code>beforeNavigate</code>."
  },
  {
    "term": "Notification",
    "definition": "A transient, non-blocking, in-page message that appears, is accessibly announced, persists for a bounded time (or until dismissed), and is removed. The general concept — temporary, dismissable, queued, severity-weighted, announced — of which a <em>toast</em> or <em>snackbar</em> is one <em>presentation</em>. Distinct from the OS-level Web Notifications API (system notifications outside the tab), from a persistent background-task surface, and from an inline (in-flow) message banner.",
    "usage": "Realized by the <a href=\"/blocks/notification/\">Notification block</a>, which implements the <a href=\"/intents/feedback/\">Feedback Intent</a>. Presentation styles (toast, snackbar, banner-position) belong to the implementation, not the standard."
  },
  {
    "term": "Notification Region",
    "definition": "The top-layer host element that displays notifications and is the live region for their announcements. Placed declaratively once per app; mounts to the platform top layer via the Popover API (<code>popover=\"manual\"</code>), falling back to a Web Portals mount. Reads the Feedback Intent's <code>placement</code> and <code>stackLimit</code> to position and bound the visible set.",
    "usage": "The Component half of the <a href=\"/blocks/notification/\">Notification block</a>; its injectable controller is the imperative half (<code>show</code>/<code>dismiss</code>). Emits <code>notification-shown</code> / <code>notification-dismissed</code>."
  },
  {
    "term": "Breadcrumb",
    "definition": "A structured trail showing the user's location within a site or data hierarchy, as an ordered list of links from root to current page. Answers \"where am I?\" — distinct from a navigation menu (\"where can I go?\"), tabs (\"which panel?\"), or a stepper (\"how far through this process?\"). Realized as a <code>nav[aria-label]</code> landmark wrapping an ordered list, the current crumb a non-link carrying <code>aria-current=\"page\"</code>, with presentational (never announced) separators.",
    "usage": "Realized by the <a href=\"/blocks/breadcrumb/\">Breadcrumb block</a>, which implements the <a href=\"/intents/navigation/\">Navigation Intent</a> and composes the Typography Intent for crumb-label truncation. Overflow collapses the middle behind an ellipsis disclosure, keeping root + current visible."
  },
  {
    "term": "Dialog (family substrate)",
    "definition": "The abstract substrate every dialog-based surface conforms to — a top-layer surface that interrupts or overlays the page, owning the shared modal-surface machinery (focus containment, escape/outside/backdrop dismissal, scroll-lock, modal/non-modal mode, top-layer mount). Not a shippable widget; its concrete members are distinct configurations of the Modal Intent's <code>placement</code> over this one substrate: centered modal (center), drawer (start/end), bottom sheet (bottom). The droplist-family pattern applied to dialogs (<code>droplist : dropdown :: dialog : drawer</code>).",
    "usage": "The <a href=\"/blocks/dialog/\">Dialog block</a> (abstract), realized on native <code>&lt;dialog&gt;</code> + the Popover API; implements the <a href=\"/intents/modal/\">Modal Intent</a> and composes Focus Containment, Surface, and Motion. Concrete members (<a href=\"/backlog/360-drawer-sheet-block/\">drawer</a> #360, sheet, modal) extend it rather than re-implementing the machinery."
  },
  {
    "term": "Drawer",
    "definition": "The edge-anchored, sliding concrete member of the dialog family — a panel entering from a viewport inline edge (navigation drawer, filter panel, detail pane). It extends the abstract dialog substrate (inheriting focus containment, dismissal, backdrop, scroll-lock, mode, top-layer) and only fixes the configuration to an edge: the Modal Intent's <code>placement: start | end</code> (RTL-aware), with slide motion and swipe-to-dismiss added. Sibling of the centered modal (center) and bottom sheet (bottom).",
    "usage": "Realized by the <a href=\"/blocks/drawer/\">Drawer block</a>, a concrete member of the <a href=\"/blocks/dialog/\">dialog family</a> (<code>droplist : dropdown :: dialog : drawer</code>). It re-implements none of the dialog machinery — a drawer IS a dialog at an inline edge."
  },
  {
    "term": "Carousel",
    "definition": "A set of equivalent slides presented one (or a few) at a time, advanced by controls, pagination markers, swipe, or optional autoplay. Native-first on a CSS scroll-snap container, with the emerging CSS carousel pseudo-elements (<code>::scroll-button()</code>, <code>::scroll-marker</code>) as native controls where shipped. Distinct from tabs (labelled panels) and from paginated data (Collection Operations).",
    "usage": "Realized by the <a href=\"/blocks/carousel/\">Carousel block</a>; composes the Motion, Live-Region Status, and Navigation intents. Autoplay is opt-in and accessibility-gated (pause control, pause-on-hover/focus, honours prefers-reduced-motion) per WCAG 2.2.2 + the WAI-ARIA APG carousel pattern."
  },
  {
    "term": "Content-Addressed Id",
    "definition": "The version identity of a Module-as-a-Service served artifact: an opaque hash digest over <em>every</em> byte-determining input — the authored definition, the transform/compiler version, the request params (form/target/strategy), and the folded provenance. Same id ⇒ byte-identical artifact (cacheable forever); any input moving mints a new id. Omitting a byte-determining input is the one excluded (broken) rule — it serves drift.",
    "usage": "The identity invariant of the <a href=\"/projects/webadapters/#protocol-maas-versioning\">MaaS Served-Artifact Versioning Protocol</a> (ratified in #088, realized by #389). The terminal rung of the pin ladder resolves to the content-addressed URL, the only rung served <code>immutable</code>."
  },
  {
    "term": "Pin Ladder",
    "definition": "The grammar by which a consumer pins a Module-as-a-Service artifact: a floating-tag → exact-semver → content-hash ladder, each rung resolving <em>down</em> to the immutable content-addressed URL (the npm <code>tag → version → tarball</code> shape). Floating and exact pins are revalidatable short-TTL redirects; the content-hash rung is terminal and never revalidates. WE supports pinning at any rung and mandates none.",
    "usage": "Part 2 of the <a href=\"/projects/webadapters/#protocol-maas-versioning\">MaaS Served-Artifact Versioning Protocol</a>. Unit of version is dual — a human semver on the definition and the derived <a href=\"/glossary/#content-addressed-id\">content-addressed id</a> coexist (human contract vs. integrity contract)."
  },
  {
    "term": "API-Contract Version",
    "definition": "A version number that bumps <em>only on a public-API change</em> of a runtime dependency, decoupled from that package's release semver — exposed by the runtime as <code>__API_VERSION__</code>. A served artifact declares a compatibility <em>range</em> over it (e.g. <code>&gt;=2 &lt;3</code>) against its bare-specifier runtime (e.g. <code>@frontierui/jsx-runtime</code>); 'satisfies' means the resolved runtime's <code>__API_VERSION__</code> falls in the range.",
    "usage": "Part 4 of the <a href=\"/projects/webadapters/#protocol-maas-versioning\">MaaS Served-Artifact Versioning Protocol</a> — a consumer↔provider seam-contract (the #092 shape). WE owns the range format + what 'satisfies' means; Frontier UI owns the value (<code>__API_VERSION__</code>) and the opt-in <code>maas-check</code> validator (#390). Advertise-only by default; the validator is consumer-opt-in, never mandated."
  },
  {
    "term": "Consumption Edge",
    "definition": "A directed <em>consumes</em> edge in the provider–consumer graph: a record that one consumer depends on one provider, surfaced by WE introspection at the DI seam (<code>Injector.consumptionEdges()</code>, opt-in via <code>Injector.trackConsumption</code>). Bidirectional by construction — the same edge answers both 'what do I depend on?' and 'who depends on me?', the latter being what makes impact analysis possible. Build-derived edges are <em>confirmed</em>; a runtime drift agent may add <em>potential</em> edges until a trace confirms them.",
    "usage": "Part 1 of the <a href=\"/projects/webregistries/#protocol-provider-consumer-graph\">Provider–Consumer Graph Protocol</a> (#401), populated by the introspection extension in #400. The graph is build-time auto-derived (no live agent), the #092 most-permissive ingestion default."
  },
  {
    "term": "Consumer-Driven Projection",
    "definition": "The net-new, consumer side of a provider↔consumer seam-contract: the consumer declares the subset of a protocol's dimensions/capabilities it actually depends on (<code>consumerRequires</code>), rather than the whole protocol. The provider side is <em>reused</em> from the existing protocol conformance contract (the provider declares its tier). Compatible ⇔ the consumer's required subset is contained in what the provider's tier supplies; a drift is any change removing a required member.",
    "usage": "Part 2 of the <a href=\"/projects/webregistries/#protocol-provider-consumer-graph\">Provider–Consumer Graph Protocol</a> (#401, #092 Fork 2). The only new schema at the seam — it deliberately reuses <a href=\"/protocols/\">protocols.json</a> rather than coining a parallel Pact-style format. Feeds drift detection, impact analysis, and continuous verification on one definition."
  },
  {
    "term": "Vega-Lite L1 Profile",
    "definition": "The thin semantic core of the <a href=\"/projects/webcharts/\">Web Charts</a> standard: a chart declared as <code>data &rarr; mark &rarr; encoding</code> (Vega-Lite's vocabulary, borrowed) restricted to a minimal L1 subset — basic marks, position/color/size channels, and scales/axes/legends. Selections, transforms, and view composition are additive L2+ tiers a conformant L1 renderer may ignore. It describes <em>what the data means</em>, never how it is drawn.",
    "usage": "Standardized by Web Charts (#105, scaffolded under #570). The semantic plane is kept strictly separate from the presentation/theme plane (which consumes <a href=\"/projects/webtheme/\">Web Theme</a> tokens), so one spec re-themes without edits. Authored as a profile schema on the <a href=\"/projects/webcharts/#vega-lite-l1\">webcharts project page</a> (#572)."
  },
  {
    "term": "Chart Encoding (semantic plane)",
    "definition": "The mapping from a data field to a visual channel in a <a href=\"/projects/webcharts/#vega-lite-l1\">Vega-Lite L1</a> chart — e.g. <code>color: { field: 'category' }</code> means <em>encode category by hue</em>, not <code>#3b82f6</code>. The encoding is semantic; the concrete palette, scale range, and animation are the presentation/theme plane, resolved separately. A field's <em>measurement type</em> (quantitative / temporal / ordinal / nominal) drives the default scale and the accessibility-derivable axis/legend labels.",
    "usage": "The core distinction in the Web Charts semantic/presentation split (#572). Because color/size are semantic, the same chart spec re-themes for free and an a11y description is derivable from the encoding + measurement types — the conformance suite scores accessibility as a first-class axis (#570 slice d)."
  }
]
