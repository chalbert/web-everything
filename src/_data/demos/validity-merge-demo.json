{
  "id": "validity-merge-demo",
  "name": "Validity Merge Playground",
  "summary": "The runnable home for the runtime customValidityMerge plug (#215) — the real window.customValidityMerge registry driving a form-associated <validity-merge-field>. Toggle the four named sources (native/schema/async/manual) live and watch them collapse into one MergedValidity pushed to native ElementInternals.setValidity, so the merged state drives :invalid / :user-invalid for free. Swap source-reduction ↔ last-write-wins with zero control edits. A runtime-conformance section asserts the plug's invariants (strictest-wins, pending-beats-valid, strategy swap, native validity binding, per-scope resolution).",
  "kind": "playground",
  "description": "<h2>Concept</h2><p>A live surface for the runtime <a href=\"/plugs/customvaliditymergeregistry/\"><code>customValidityMerge</code></a> plug (<a href=\"/backlog/215-validity-merge-runtime-plug-element-internals/\">#215</a>) — the live counterpart to the dependency-free <code>validity-merge/</code> model (<a href=\"/backlog/212-validity-merge-registry-strategy-plane/\">#212</a>). The real <code>window.customValidityMerge</code> registry, resolved per-scope through the injector chain, drives a form-associated <code>&lt;validity-merge-field&gt;</code> control. Toggle each of the four named <strong>sources</strong> and watch them collapse into one <code>MergedValidity</code> handed to native <a href=\"https://developer.mozilla.org/en-US/docs/Web/API/ElementInternals/setValidity\"><code>ElementInternals.setValidity</code></a>.</p><h3>Surface fixed, math swappable</h3><p>Per <a href=\"/backlog/004-validation-engine-open-design-points/\">#004</a> OP-1 the standard fixes the <code>MergedValidity</code> surface, not the merge math. Swap the strategy — <code>source-reduction</code> (native-first, strictest-wins) ↔ <code>last-write-wins</code> — and the <em>same</em> sources collapse differently with <strong>zero control edits</strong>.</p><h3>:user-invalid for free</h3><p>Because the merged state flows through <code>setValidity()</code>, the control participates in native <code>:invalid</code> / <code>:user-invalid</code> styling and form-submission blocking — the answer to the webvalidation open question: the UA owns the user-interaction gate, the control never re-exposes touched/dirty.</p><h3>Verified contract</h3><p>The runtime-conformance section asserts the live plug against its contract: strictest-wins, pending-beats-valid, the strategy swap, the native validity binding, and per-scope (nearest-wins) resolution. The strategies + orchestrator are the <strong>same source</strong> the #212 unit suite runs.</p>",
  "status": "draft",
  "liveUrl": "http://localhost:3000/demos/validity-merge-demo.html",
  "projects": [
    "webvalidation"
  ]
}
