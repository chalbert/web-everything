{
  "id": "text-interpolation",
  "name": "Text Interpolation Demo",
  "summary": "Text node expression pipeline with {{mustache}} and [[polymer]] syntax, named contexts, pipes, nested paths, and template instantiation — all rendered without FOUC.",
  "description": "<h2>Concept</h2><p>This demo showcases the complete text node expression pipeline: text node parsers detect <code>{{ }}</code> and <code>[[ ]]</code> syntax, expression parsers evaluate content, and InterpolationTextNode renders resolved values into the DOM.</p><h3>Key Features</h3><ul><li><strong>Basic Interpolation:</strong> <code>{{name}}</code> renders state values from injector-provided contexts.</li><li><strong>Nested Paths:</strong> <code>{{user.profile.name}}</code> with dot-notation resolution.</li><li><strong>Named Contexts:</strong> <code>{{@theme.primary}}</code> resolves values from named injector contexts.</li><li><strong>Pipe Expressions:</strong> <code>{{name | uppercase}}</code> applies filter functions via the pipe parser.</li><li><strong>Multiple Expressions:</strong> Several expressions in the same text node.</li><li><strong>Polymer Syntax:</strong> <code>[[count]]</code> using double-square-bracket parser.</li><li><strong>Mixed Syntax:</strong> Both <code>{{ }}</code> and <code>[[ ]]</code> on the same page.</li><li><strong>Template Instantiation:</strong> Expressions inside <code>&lt;template&gt;</code> elements, cloned at runtime to avoid FOUC.</li></ul><h3>Technical Approach</h3><p>The pipeline flows: Text → CustomTextNodeParser → UndeterminedTextNode → CustomTextNodeRegistry upgrade → InterpolationTextNode → CustomExpressionParserRegistry → Resolved Value. Parsers are registered via bootstrap ('mustache' for <code>{{ }}</code>, 'polymer' for <code>[[ ]]</code>).</p>",
  "status": "active",
  "liveUrl": "http://localhost:3000/demos/text-interpolation-demo.html",
  "projects": [
    "webexpressions"
  ]
}
