{
  "id": "status-indicator",
  "name": "Status Indicator Intent",
  "status": "draft",
  "summary": "Canonical display of a domain entity's lifecycle state — a semantic status chip/badge plus the available next transitions — so an entity's current state, and what can happen to it next, read consistently across every surface. The visual member of the Web Lifecycle protocol.",
  "dimensions": {
    "tone": {
      "description": "How the current state maps to the canonical semantic `tone` (#1427; drives colour/icon, never bespoke per-screen). Synonyms normalized: success (≡ positive), warning (≡ caution). `critical` is a distinct severity token and `progress` is an intent-local lifecycle tone (neither is in the shared severity palette, #1427 roster).",
      "values": [
        "neutral",
        "info",
        "progress",
        "success",
        "warning",
        "critical"
      ]
    },
    "shape": {
      "description": "The visual metaphor for the status token",
      "values": [
        "badge",
        "pill",
        "dot",
        "text"
      ]
    },
    "affordance": {
      "description": "Whether the available next transitions are surfaced alongside the status",
      "values": [
        "display-only",
        "actionable"
      ]
    }
  },
  "description": "<h3>What Status Indicator Is</h3><p class=\"text-sm text-gray-600 mb-2\">The canonical, accessible presentation of <strong>where an entity is in its lifecycle</strong> &mdash; the loan that is <code>underwriting</code>, the claim that is <code>approved</code>, the order that is <code>shipped</code>. It is the visual member of the <a href=\"/projects/weblifecycle/\">Web Lifecycle</a> protocol: it reads the same lifecycle provider and renders the current state plus, optionally, the moves available from here.</p><p class=\"text-sm text-gray-600 mb-2\"><strong>It owns the display, not the rules.</strong> Which states exist and which transitions are legal belong to the lifecycle definition; whether the actor may fire one belongs to <a href=\"/projects/webguards/\">Web Guards</a>. This intent only standardizes how the state is shown and announced &mdash; so a status stops being a hand-coloured <code>&lt;span&gt;</code> per screen.</p><h3>Tone</h3><p class=\"text-sm text-gray-600 mb-2\">A status maps to a semantic severity, not an ad-hoc colour. The tone (not the hex) is the intent; the theme resolves it.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>neutral</code>: an inert/draft state. <code>info</code>: informational, in-system. <code>progress</code>: actively moving through the flow (intent-local tone). <code>success</code> (≡ positive): a successful terminal/near-terminal state. <code>warning</code> (≡ caution): needs attention (conditions, on-hold). <code>critical</code>: declined/failed/expired (distinct severity token).</li></ul><h3>Shape</h3><p class=\"text-sm text-gray-600 mb-2\">The visual token: <code>badge</code> (boxed label), <code>pill</code> (rounded), <code>dot</code> (a coloured dot + text, for dense tables), or bare <code>text</code>.</p><h3>Affordance</h3><p class=\"text-sm text-gray-600 mb-2\"><code>display-only</code> shows the state; <code>actionable</code> also surfaces the available next transitions (from the provider) as controls. A11y: a state change is announced via <a href=\"/intents/live-region-status/\">Live Region Status</a> rather than re-defining an announcement here.</p>",
  "events": [
    "transition"
  ]
}
