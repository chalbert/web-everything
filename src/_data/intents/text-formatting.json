{
  "id": "text-formatting",
  "name": "Text-Formatting Controls",
  "requiresCapabilities": [],
  "status": "draft",
  "summary": "The formatting-controls axis (ratified #590 Fork 5): which format operations a surface exposes, where the controls sit, and how they overflow. UX-only and editor-independent — the same axis governs a comment box, a rich-text surface, or a markdown field — so it earns its own home apart from the rich-text surface intent. Composes droplist (grouped op menus), popover (floating bubble), and button (single ops). The technical mechanic (execCommand vs a command registry vs a custom serializer) is NOT here — it is a Technical Configurator concern.",
  "dimensions": {
    "exposure": {
      "description": "Which formatting operations are surfaced. minimal = inline emphasis + link; standard = + lists, headings, blockquote (the recommended default); full = + block insertion, tables, media; custom = an author-declared op set.",
      "values": [
        "minimal",
        "standard",
        "full",
        "custom"
      ]
    },
    "placement": {
      "description": "Where the controls live relative to the surface. toolbar = a persistent bar (the discoverable default); floating = a selection bubble anchored to the current range; inline = in-content affordances (slash menu / markdown shortcuts); contextual = a right-click / long-press menu.",
      "values": [
        "toolbar",
        "floating",
        "inline",
        "contextual"
      ]
    },
    "overflow": {
      "description": "What happens when the controls exceed the available width. overflow-menu = collapse extras behind a \"more\" affordance (the most-permissive default — nothing is lost); scroll = a horizontally scrollable strip; wrap = flow onto a second row.",
      "values": [
        "overflow-menu",
        "scroll",
        "wrap"
      ]
    }
  },
  "description": "<h3>What This Is</h3><p class=\"text-sm text-gray-600 mb-2\">The <strong>formatting-controls axis</strong> ratified in <a href=\"/backlog/590-graduate-rich-text-editing-the-webediting-project/\">#590</a> (Fork 5): <em>which</em> format operations a surface exposes, <em>where</em> the controls sit, and <em>how</em> they overflow. It is <strong>UX-only and editor-independent</strong> — the same axis governs a plain comment box, a rich-text surface (see <a href=\"/intents/rich-text/\">rich-text</a>), or a markdown field — which is why it earns its own home rather than being baked into the editing surface.</p><h3>Dimensions (UX-only)</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>exposure</code>: <code>minimal</code> (inline emphasis + link) &rarr; <code>standard</code> (<strong>recommended default</strong> — + lists, headings, blockquote) &rarr; <code>full</code> (+ block insertion, tables, media) &rarr; <code>custom</code> (author-declared op set).</li><li><code>placement</code>: <code>toolbar</code> (persistent bar — the discoverable default) vs <code>floating</code> (selection bubble) vs <code>inline</code> (slash menu / markdown shortcuts) vs <code>contextual</code> (right-click / long-press).</li><li><code>overflow</code>: <code>overflow-menu</code> (collapse behind a &ldquo;more&rdquo; affordance — the most-permissive default, nothing is lost) vs <code>scroll</code> (horizontal strip) vs <code>wrap</code> (second row).</li></ul><h3>Composition</h3><p class=\"text-sm text-gray-600 mb-2\">Controls compose existing intents: <a href=\"/intents/disclosure/\">droplist</a>-style grouped op menus, <a href=\"/intents/anchor/\">anchor</a>ed <code>floating</code> bubbles, and single-op buttons. A control reflects the current selection state (active / inactive / disabled) and applies an operation to the active range.</p><h3>UX-only &rarr; the mechanic is a Configurator card</h3><p class=\"text-sm text-gray-600 mb-2\">The <em>how</em> a format operation is realized — legacy <code>execCommand</code> vs a declarative command registry vs a custom serializer round-trip — is a technical strategy, <strong>not</strong> part of this intent. It is authored as a <strong>Technical Configurator</strong> domain, per intent-UX-only&rarr;configurator.</p>",
  "events": [
    "format-apply",
    "format-state-change"
  ]
}
