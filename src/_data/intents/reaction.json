{
  "id": "reaction",
  "name": "Reaction Intent",
  "status": "draft",
  "summary": "The reaction interaction (toggle + aggregate + roster) — the consumer end of the expressive-symbol seam. Adopts Atlassian ReactionSummary as the data-contract baseline (count · users[] · reacted · optimisticallyUpdated); not a protocol; multi-user sync is a composed webrealtime concern, never baked in (#370 Fork 5).",
  "dimensions": {
    "reactionSet": {
      "description": "Which symbols are offerable — the convergent sentiment default, a project named-set, or the full all-emojis picker (default-less core; sentiment flavor default).",
      "values": [
        "sentiment",
        "named-set",
        "all-emojis"
      ]
    },
    "roster": {
      "description": "How the who-reacted list surfaces — just the aggregate count, or a keyboard-reachable, display-limited roster of names.",
      "values": [
        "count-only",
        "names"
      ]
    },
    "optimistic": {
      "description": "Whether your own toggle updates immediately before the round-trip confirms, or waits for confirmation.",
      "values": [
        "on",
        "off"
      ]
    }
  },
  "description": "<h3>What This Is</h3><p class=\"text-sm text-gray-600 mb-2\">The <strong>reaction interaction</strong> — affix a symbol to content, toggle your own, aggregate a count, and show a who-reacted roster. The consumer end of the provider→consumer seam (decision <a href=\"/backlog/370\">#370</a> Fork 5): it <em>composes</em> the <a href=\"/intents/expressive-symbol/\">expressive-symbol</a> glyph, it never re-renders it.</p><h3>The contract — <code>ReactionSummary</code> (baseline)</h3><p class=\"text-sm text-gray-600 mb-2\">Adopts Atlassian's <code>ReactionSummary</code> as the contract baseline: <code>count</code> (the aggregate) · <code>users[]</code> (the who-reacted roster, with a display limit) · <code>reacted</code> (toggle-your-own) · <code>optimisticallyUpdated</code>. <strong>Not a protocol</strong> — a data contract with no swappable-vendor/engine-interop story (minimize lock-in). <strong>Multi-user sync is composed, never baked in</strong>: \"others see your reaction\" is a separate real-time concern driven through <a href=\"/intents/live-region-status/\">live-region-status</a> + a webrealtime protocol.</p><h3>Dimensions</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>reactionSet</code>: which symbols are offerable — <code>sentiment</code> (the convergent default set), a project <code>named-set</code>, or <code>all-emojis</code> (the full-picker escape). Default-less core; <code>sentiment</code> is the flavor default (Fork 6).</li><li><code>roster</code>: how the who-reacted list surfaces — <code>count-only</code> (just the aggregate) or <code>names</code> (a keyboard-reachable roster, display-limited).</li><li><code>optimistic</code>: whether your own toggle updates immediately (<code>on</code>) before the round-trip confirms, or waits (<code>off</code>).</li></ul><h3>Accessibility (load-bearing)</h3><p class=\"text-sm text-gray-600 mb-2\">Each reaction is an <code>aria-pressed</code> toggle with a <strong>composed accessible name</strong> — emoji name + count + your-reacted-state (\"thumbs up, 5 reactions, you reacted, activate to remove\"), not a bare glyph. Announce <strong>your local toggle only</strong> via live-region-status — never every remote tick (which would be a screen-reader flood). The roster is keyboard-reachable.</p><h3>vs. the status badge</h3><p class=\"text-sm text-gray-600 mb-2\">A reaction count chip resembles the <code>surface</code>-tokened badge of <a href=\"/backlog/354\">#354</a>, but means \"N people reacted,\" not \"this state\" — they <strong>compose, they do not merge</strong>.</p>"
}
