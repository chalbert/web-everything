{
  "id": "pagination",
  "name": "Custom Pagination",
  "status": "draft",
  "summary": "Standard for iterating over resource collections (Cursor, Offset, Page).",
  "description": "<h3>Design Philosophy</h3><p>Every API handles lists differently: <code>page/limit</code>, <code>offset/limit</code>, or <code>cursor/after</code>. This standard normalizes the iteration process into a simple <code>next()</code> protocol, so a UI consumer — the <a href=\"/intents/collection-operations/\">Collection Operations</a> intent's <code>page</code> dimension — can drive any list without knowing the underlying strategy.</p><h3>Strategy &amp; UX Consequences</h3><p>The strategy is a <strong>technical</strong> data-fetch choice, but it is not UX-neutral: it dictates which pagination affordances the <a href=\"/intents/collection-operations/\"><code>page</code> dimension</a> can offer. The deciding factor is whether a <code>total</code> is available.</p><ul><li><code>offset</code> / <code>page</code> — recompute a window against the full set, so a <code>total</code> is available: this <strong>enables jump-to-page</strong> (numbered, <code>pageMode: paged</code>) and a <strong><code>rangeLabel</code></strong> ('showing 21–40 of 500'). Trade-off: under concurrent inserts/deletes the window shifts, so rows can be skipped or duplicated across pages.</li><li><code>cursor</code> — fetches the next N rows after a stable key, so it is stable under writes, but it <strong>exposes no <code>total</code></strong> and only <code>next()</code>/<code>previous()</code>. It therefore <strong>cannot drive jump-to-page or <code>rangeLabel</code></strong>, and forces the intent to <code>pageMode: append</code> or prev/next.</li></ul><p><strong>Cross-layer constraint:</strong> jump-to-page and <code>rangeLabel</code> require a known total, so they pair only with an <code>offset</code>/<code>page</code> strategy — a <code>cursor</code> strategy forces <code>append</code>/prev-next. This is the same fact the <a href=\"/intents/collection-operations/\">Collection Operations</a> intent states from the UX side, kept consistent here on the protocol side.</p><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">interface CustomPagination&lt;T&gt; {\n    // The current slice of data\n    readonly items: T[];\n\n    // Metadata\n    readonly hasNext: boolean;\n    readonly hasPrevious: boolean;\n    readonly total?: number;       // present for offset/page; absent for cursor\n\n    // Navigation\n    next(): Promise&lt;CustomPagination&lt;T&gt;&gt;;\n    previous(): Promise&lt;CustomPagination&lt;T&gt;&gt;;\n}\n\ntype CustomPaginationDefinition =\n    // total available -> drives jump-to-page + rangeLabel; window shifts under writes\n    | { strategy: 'offset'; limit?: number }\n    | { strategy: 'page'; limit?: number }\n    // no total, next()/previous() only -> forces append / prev-next; stable under writes\n    | { strategy: 'cursor'; limit?: number; key?: string };</code></pre>"
}
