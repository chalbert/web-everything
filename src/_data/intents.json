[
  {
    "id": "motion",
    "name": "Motion Intent",
    "status": "concept",
    "summary": "Protocol defining the application's physics, latency tolerance, and transition behavior.",
    "dimensions": {
      "physics": {
        "description": "The animation model governing transitions and micro-interactions",
        "values": ["natural", "immediate", "reduced"]
      }
    },
    "description": "<h3>Physics Contract</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>natural</code>: Mass-based physics (springs), context-aware transitions.</li><li><code>immediate</code>: <strong>&lt; 16ms</strong> latency. Disables decorative motion for high-performance workflows.</li><li><code>reduced</code>: Semantic motion only (e.g. layout shifts), disabling parallax/scaling. Respects <code>prefers-reduced-motion</code>.</li></ul>"
  },
  {
    "id": "density",
    "name": "Density Intent",
    "status": "concept",
    "summary": "Protocol defining spatial relationships, touch targets, and information quantity.",
    "dimensions": {
      "level": {
        "description": "The spatial density profile controlling touch targets, spacing, and information quantity",
        "values": ["comfortable", "compact", "wafer"]
      }
    },
    "description": "<h3>Spatial Contract</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>comfortable</code>: <strong>44px+</strong> touch targets. Whitespace is used for grouping (Consumer standard).</li><li><code>compact</code>: <strong>32px</strong> targets. High information density, border-based grouping (Enterprise standard).</li><li><code>wafer</code>: <strong>24px</strong> targets. Maximum data visibility for complex tooling/IDE interfaces.</li></ul>"
  },
  {
    "id": "interaction",
    "name": "Interaction Intent",
    "status": "concept",
    "summary": "Protocol defining the input modality and focus management behavior.",
    "dimensions": {
      "modality": {
        "description": "The primary input method determining interaction patterns",
        "values": ["pointer", "touch", "gamepad"]
      }
    },
    "description": "<h3>Input Contract</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>pointer</code>: Fine-grained control. Hover menus enabled, scrollbars visible.</li><li><code>touch</code>: Coarse control. <strong>Swipe actions</strong> replace hover menus. Hit testing padding applied.</li><li><code>gamepad</code>: Spatial Navigation (D-Pad). Focus is trapped within active regions (Sunsetting <code>tab-index</code> logic).</li></ul>"
  },
  {
    "id": "prefetch",
    "name": "Prefetch Intent",
    "status": "draft",
    "summary": "Controls the eagerness of resource prefetching based on connection and power.",
    "dimensions": {
      "eagerness": {
        "description": "How proactively the application anticipates the user's next action",
        "values": ["viewport", "hover", "interaction"]
      },
      "suppressWhen": {
        "description": "Conditions that suppress prefetching to respect the user's environment",
        "values": ["limited-data", "low-power", "slow-connection"]
      }
    },
    "description": "<h3>Eagerness</h3><p class=\"text-sm text-gray-600 mb-2\">How proactively the application anticipates the user's next action. Higher eagerness improves perceived speed but consumes more resources.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>viewport</code>: Prefetch when the link becomes visible to the user (Lazy).</li><li><code>hover</code>: Prefetch when the user shows interest (hovering or focusing) (Moderate).</li><li><code>interaction</code>: Prefetch at the moment the user begins to act (pointer down) (Eager).</li></ul><h3>User Conditions</h3><p class=\"text-sm text-gray-600 mb-2\">Conditions under which prefetching should be suppressed to respect the user's environment. The implementing block decides which APIs to use for detection.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>limited-data</code>: User has indicated a preference to conserve data (e.g., metered connection).</li><li><code>low-power</code>: Device is in a low-battery or power-saving state.</li><li><code>slow-connection</code>: Network quality is too poor for speculative loading to be beneficial.</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type PrefetchEagerness = 'viewport' | 'hover' | 'interaction';\ntype PrefetchConstraint = 'limited-data' | 'low-power' | 'slow-connection';\n\ninterface PrefetchIntent {\n    // How proactively to prefetch\n    eagerness: PrefetchEagerness;\n    // Conditions that suppress prefetching\n    suppressWhen: PrefetchConstraint[];\n}</code></pre>"
  },
  {
    "id": "loader",
    "name": "Loader Intent",
    "status": "draft",
    "summary": "Protocol defining how a pending state affects the user interface via Blocking, Scoping, and Timing profiles. Includes version token tracking and hierarchy aggregation for async lifecycle management.",
    "dimensions": {
      "strategy": {
        "description": "How the UI is blocked during a pending state",
        "values": ["optimistic", "soft", "hard", "replacement"]
      },
      "scope": {
        "description": "What part of the UI is affected by the pending state",
        "values": ["element", "region", "viewport"]
      },
      "progress": {
        "description": "Whether the user can see measurable progress",
        "values": ["determinate", "indeterminate"]
      },
      "context": {
        "description": "Whether prior content exists when loading begins",
        "values": ["initial", "transition"]
      },
      "timing": {
        "description": "When the loading indicator appears",
        "values": ["immediate", "debounced"]
      }
    },
    "description": "<h3>Blocking Strategy</h3><p class=\"text-sm text-gray-600 mb-2\">How the UI responds to a pending state. Each strategy represents a different trade-off between user freedom and data integrity.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>optimistic</code>: <strong>Non-blocking</strong>. UI remains fully interactive (background sync). The predicted outcome is shown immediately; reverted on failure.</li><li><code>soft</code>: <strong>Input-blocking</strong>. Applies <code>aria-disabled</code> and <code>pointer-events: none</code>. Content remains visible but non-interactive.</li><li><code>hard</code>: <strong>Visual-blocking</strong>. Content is obscured or overlaid (Backdrop). User must wait.</li><li><code>replacement</code>: <strong>Content-blocking</strong>. Placeholder content (Skeleton, Spinner) temporarily replaces the view.</li></ul><h3>Scope Definition</h3><p class=\"text-sm text-gray-600 mb-2\">What part of the UI is affected by the pending state.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>element</code>: Constrained to the triggering target (e.g., Button spinner).</li><li><code>region</code>: Constrained to the containing landmark (e.g., <code>aria-busy</code> on the Form).</li><li><code>viewport</code>: Global interruption (Glass pane).</li></ul><h3>Progress Indication</h3><p class=\"text-sm text-gray-600 mb-2\">Whether the user can see measurable progress or only that activity is happening.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>determinate</code>: Progress can be measured (0–100%). Rendered as a progress bar or percentage. Research suggests this is preferred for operations exceeding 10 seconds.</li><li><code>indeterminate</code>: Progress is happening but completion is unknown. Rendered as a spinner, pulse, or shimmer animation.</li></ul><h3>Loading Context</h3><p class=\"text-sm text-gray-600 mb-2\">Whether prior content exists. This changes user expectations for how the pending state should look.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>initial</code>: No prior content is available. The user expects a placeholder (skeleton, spinner, empty state).</li><li><code>transition</code>: Prior content is already visible. The user expects the existing content to remain visible (dimmed, stale) while new content loads. Replacing visible content with a loading indicator is perceived as regression.</li></ul><h3>Timing Contract</h3><p class=\"text-sm text-gray-600 mb-2\">Maps semantic timing profiles to configurable durations (ms). Applications can override defaults or register new profiles.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>immediate</code>: Defaults to 0ms (Configurable).</li><li><code>debounced</code>: Defaults to 400ms (Configurable). Prevents flicker for fast operations.</li><li><code>[custom]</code>: User-defined keys (e.g., <code>slow: 1000ms</code>).</li></ul><h3>Lifecycle States</h3><p class=\"text-sm text-gray-600 mb-2\">The full state machine for a loading operation. Components can react to any state transition.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>idle</code>: No operation in progress. Default state.</li><li><code>pending</code>: Operation started, no result yet. Loading indicator is active. <strong>Carries a version/generation token</strong> — an immutable snapshot of the input/request state at dispatch time. On resolution, if the current input version has changed, the result is dropped (stale-resolve prevention, cross-cutting req #1).</li><li><code>success</code>: Operation completed successfully. Indicator may briefly show confirmation before returning to idle.</li><li><code>error</code>: Operation failed. Error state may persist until dismissed or retried.</li><li><code>stale</code>: Showing previous data while a refresh is in progress. Combines with <code>transition</code> context.</li></ul><h3>Hierarchy Aggregation</h3><p class=\"text-sm text-gray-600 mb-2\"><strong>Validation and form hierarchies compose the loader lifecycle:</strong> a parent's pending state rolls up from its children — <code>pending</code> if any descendant is pending; otherwise <code>success</code>/<code>error</code>/<code>idle</code> per precedence. This aggregation surfaces the full async tree's readiness, enabling submission gates and progress indication. Validation composes this as its async-orchestration meta-intent (cross-cutting req #3: \"pending aggregation + submit awaits in-flight async\").</p><h3>Label Conventions</h3><p class=\"text-sm text-gray-600 mb-2\">Recommended conventions for loading indicator labels, drawn from design system research (Fluent, Spectrum).</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><strong>Casing</strong>: Sentence case (\"Loading data...\" not \"LOADING DATA\").</li><li><strong>Suffix</strong>: Trailing ellipsis to convey ongoing activity.</li><li><strong>Form</strong>: Gerund / \"-ing\" verbs (\"Loading\", \"Saving\", \"Submitting\").</li><li><strong>Length</strong>: Three words or fewer for conciseness.</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type BlockingStrategy = 'optimistic' | 'soft' | 'hard' | 'replacement';\ntype LoaderScope = 'element' | 'region' | 'viewport';\ntype LoaderProgress = 'determinate' | 'indeterminate';\ntype LoaderContext = 'initial' | 'transition';\ntype LoaderState = 'idle' | 'pending' | 'success' | 'error' | 'stale';\n\ninterface LoaderIntent {\n    // How the UI is blocked\n    strategy: BlockingStrategy;\n    // What part of the UI is affected\n    scope: LoaderScope;\n    // Whether progress is measurable\n    progress: LoaderProgress;\n    // Whether prior content exists\n    context: LoaderContext;\n    // When the loader appears (referencing Config.timings)\n    timing: 'immediate' | 'debounced' | string;\n    // Accessibility label\n    label?: string;\n    // Motion bindings per lifecycle state\n    animations?: Partial&lt;Record&lt;LoaderState, string&gt;&gt;;\n}</code></pre><h3>Provisioning Example</h3><pre><code class=\"language-typescript\">const LoaderConfig = {\n    timings: {\n        debounced: 400,\n        complex: 800\n    },\n    defaults: {\n        region: { strategy: 'soft', timing: 'debounced', context: 'transition' }\n    }\n};\n\ninjector Application {\n    provide { Loader } to '@web-intents' from './components/GlobalLoader';\n    provide { LoaderConfig } to '@web-intents/loader';\n}</code></pre><h3>Consumer Integration</h3><p class=\"text-sm text-gray-600 mb-2\">Blocks that consume the Loader Intent resolve it from the injector chain via <code>customContexts:loaderIntent</code> and select the matching <a href=\"/blocks/resource-loader/\">Resource Loader</a> trait for their loading behavior.</p><pre><code class=\"language-typescript\">// Consumer (e.g., Router) resolves the intent\nconst intent = InjectorRoot.getProviderOf(element, 'customContexts:loaderIntent');\n\n// Select trait based on declared strategy\nswitch (intent?.strategy) {\n    case 'replacement':\n        // Swap content with skeleton/spinner\n        return withReplacement(withSkeleton(BaseElement));\n    case 'hard':\n        // Overlay with backdrop, trap focus\n        return withHardBlocking(BaseElement);\n    case 'optimistic':\n        // No visual change, revert on error\n        return withOptimistic(BaseElement);\n    default:\n        // Soft: aria-busy + pointer-events:none\n        return withSoftBlocking(BaseElement);\n}</code></pre><p class=\"text-sm text-gray-600 mb-2\">Current consumers: <a href=\"/blocks/router/\">Router</a> (route transitions). The same pattern applies to forms, data grids, and any block with async operations.</p>",
    "events": [
      "idle",
      "pending",
      "success",
      "error",
      "stale"
    ],
    "designSystemResearch": [
      {
        "system": "Material Design 3",
        "components": ["Linear progress indicator", "Circular progress indicator"],
        "keyInsight": "Both support determinate and indeterminate. Naming uses 'progress indicators' (not 'spinner' or 'loader').",
        "reference": "https://m3.material.io/components/progress-indicators/overview"
      },
      {
        "system": "Carbon (IBM)",
        "components": ["Loading (full-page)", "Inline Loading (four-state: inactive → active → success → error)", "Skeleton states"],
        "keyInsight": "Four-state Inline Loading is the most complete lifecycle model. Progressive loading pattern: skeleton first, then primary content, then secondary. Never skeleton toasts, modals, or overflow menus.",
        "reference": "https://carbondesignsystem.com/patterns/loading-pattern/"
      },
      {
        "system": "Spectrum (Adobe)",
        "components": ["ProgressBar (linear)", "ProgressCircle (circular)"],
        "keyInsight": "Size scale (S/M/L/XL). Width constraints (48px min, 768px max for bars). Labels: sentence case with trailing ellipsis. RTL reverses bar fill; circles always spin clockwise.",
        "reference": "https://spectrum.adobe.com/page/progress-bar/"
      },
      {
        "system": "Fluent 2 (Microsoft)",
        "components": ["Spinner", "ProgressBar", "Skeleton"],
        "keyInsight": "Labels: short -ing verbs, ≤3 words, trailing ellipsis. Never rewind a progress bar. Switch from indeterminate to determinate once enough data is available.",
        "reference": "https://fluent2.microsoft.design/"
      },
      {
        "system": "Apple HIG",
        "components": ["Activity Indicator (indeterminate)", "Progress Bar (determinate)"],
        "keyInsight": "Never display inaccurate progress. Always allow cancellation for long operations. Activity indicators are non-interactive and must disappear on completion.",
        "reference": "https://developer.apple.com/design/human-interface-guidelines/progress-indicators"
      },
      {
        "system": "GOV.UK",
        "components": [],
        "keyInsight": "No dedicated loading components. Philosophy: progressive enhancement and server rendering avoid loading states by architecture. Relevant for the 'optimistic' strategy baseline.",
        "reference": "https://design-system.service.gov.uk/patterns/"
      }
    ],
    "uxResearch": [
      {
        "title": "Timing Thresholds",
        "source": "Nielsen Norman Group",
        "findings": [
          "< 1 second: Show nothing — any indicator would flash annoyingly",
          "1–10 seconds: Spinner or skeleton screen (skeleton for known layouts, spinner for unknown)",
          "> 10 seconds: Determinate progress bar — users need to see how much is left; spinners lose credibility"
        ],
        "reference": "https://www.nngroup.com/articles/skeleton-screens/"
      },
      {
        "title": "Skeleton Screen Types",
        "source": "Nielsen Norman Group",
        "findings": [
          "Animated shimmer: Best — wave/pulse animation conveys activity and outperforms static",
          "Static placeholder: Good — gray blocks matching layout structure",
          "Frame-only: Harmful — showing only chrome (header/footer) without content placeholders makes users assume the page is broken"
        ]
      },
      {
        "title": "Perceived Performance",
        "source": "Multiple (Facebook, Maister)",
        "findings": [
          "Skeleton screens led to ~300ms faster perceived load vs. spinners (Facebook study)",
          "Uncertain waits feel longer than known waits — favor determinate indicators",
          "Unexplained waits feel longer — provide labels and context",
          "Showing page structure (skeletons) reduces anxiety and cognitive load when content appears",
          "People overestimate passive waits by 36% (Maister's psychology of waiting)"
        ]
      },
      {
        "title": "Don't Hide Visible Content",
        "source": "Cross-industry convergence",
        "findings": [
          "When content is already visible, keep it visible (dimmed/stale) rather than replacing with a loading fallback",
          "Initial loads use fallbacks; transitions preserve existing content",
          "Replacing visible content with a spinner feels like regression"
        ]
      }
    ],
    "researchGaps": [
      "Skeleton exclusion rules not defined — Carbon research shows the 'replacement' strategy should never apply to toasts, modals, overflow menus, or loaders themselves. Consider an excludeFrom convention."
    ]
  },
  {
    "id": "validation",
    "name": "Validation Intent",
    "status": "draft",
    "summary": "Interop contract for form validation: observable states and events across control/group/form/nested levels. Defines per-severity commit policies and per-level vocabulary to enable swappable and combinable validation implementations. Composes Loader (async), unified severity vocabulary (Feedback/Message), and Reliability (mechanism failure) intents.",
    "dimensions": {
      "context": {
        "description": "Where validation messages are displayed / which element commits the state",
        "values": ["control", "group", "form", "root"]
      },
      "level": {
        "description": "Severity + commit policy. Part of unified vocabulary (error/warning/info/success) plus validation-specific caution. error→rejected/blocks-submit, warning/info→committed, caution→held-until-confirm.",
        "values": ["error", "warning", "caution", "info"]
      },
      "execution": {
        "description": "When validity is computed: on user input (change), blur, or form submission only",
        "values": ["change", "blur", "submit"]
      },
      "visibility": {
        "description": "When messages are displayed to user: immediate, when field dirty, after touch, or after submission attempt",
        "values": ["immediate", "dirty", "touched", "submitted"]
      }
    },
    "description": "<h3>Meta-Intent Composition</h3><p class=\"text-sm text-gray-600 mb-2\"><strong>Validation composes three shared meta-intents and is not a monolithic standard:</strong></p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><strong>Loader Intent (async lifecycle):</strong> Async validators emit <code>pending {version}</code>. The version token prevents stale-resolve races: results from superseded input versions are dropped. Pending aggregates up form hierarchy to gate submission.</li><li><strong>Unified severity/tone vocabulary:</strong> <code>level</code> shares the semantic axis (error/warning/info/success + caution) with Feedback (<code>severity</code>) and Message (<code>tone</code>). A single severity vocabulary across all messaging enables consistent tone.</li><li><strong>Reliability Intent (mechanism failure):</strong> Validation owns input-invalidity (expected outcome: user must fix). Reliability owns mechanism failure (async validator crash, server unreachable). The split prevents conflating input errors with operation failures.</li></ul><h3>Message Context (Dimension: context)</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>control</code>: Inline message anchored to a single input field.</li><li><code>group</code>: Message anchored to a fieldset, composite control, or field array. Error attaches to first errored member by path.</li><li><code>form</code>: Aggregate error summary at top of form, ordered by DOM. Links jump to first errored control. Focus moved to summary on submit-blocked.</li><li><code>root</code>: Global message (system-wide validation, not field-bound). Never sets <code>aria-invalid</code> on individual fields.</li></ul><h3>Severity & Commit Policy (Dimension: level)</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>error</code>: Substantive. <strong>Commit state: Rejected.</strong> Blocks form submission. Sets <code>aria-invalid=true</code>. Only <code>error</code>-level blocks.</li><li><code>warning</code>: Advisory. <strong>Commit state: Committed.</strong> Submission allowed. No <code>aria-invalid</code> (beyond-native).</li><li><code>caution</code>: Critical advisory. <strong>Commit state: Held.</strong> Submission paused until user confirms override (e.g., \"Continue anyway?\").</li><li><code>info</code>: Educational / helper text. <strong>Commit state: Committed.</strong> No visual emphasis. Complements the primary message.</li><li><code>[custom]</code>: App-defined levels (e.g., <code>audit</code>). App specifies commit behavior.</li></ul><h3>Per-Level Vocabulary: Control</h3><p class=\"text-sm text-gray-600 mb-2\"><strong>Observable states</strong> (four parallel regions):</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><strong>Validity:</strong> <code>unknown</code> | <code>pending {version}</code> | <code>invalid {severity, messages}</code> | <code>valid</code>. Reduced from independent sources (native/schema/async/manual) via declared precedence. Version token prevents stale-resolve.</li><li><strong>Interaction:</strong> <code>pristine</code>↔<code>dirty</code> (vs baseline), <code>untouched</code>↔<code>touched</code>, <code>blurred</code>↔<code>focused</code>, <code>pre-submit</code>↔<code>submit-attempted</code>.</li><li><strong>Commit:</strong> <code>committed</code> (warning/info) | <code>rejected</code> (error) | <code>held</code> (caution awaits confirm).</li><li><strong>Display:</strong> <code>silent</code> | <code>showing-error</code> | <code>showing-warning</code> | <code>showing-info</code> | <code>showing-pending</code>. Gated by <code>visibility</code> intent; subject to reward-early/punish-late policy.</li></ul><p class=\"text-sm text-gray-600 mb-2\"><strong>Observable events</strong> (stable-id scheme: <code>validation.control.*</code>):</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>value-input</code> (↑version): User entered / programmatic value change.</li><li><code>value-commit</code>: Value ready for persistence.</li><li><code>focus</code> | <code>blur</code>: Interaction state change.</li><li><code>validate-start {source, version}</code>: Validation run initiated (may be deferred/debounced).</li><li><code>validate-end {source, version, result}</code>: Validation completed. Stale results (old version) are dropped here.</li><li><code>validity-changed {merged}</code>: Merged validity state transitioned.</li><li><code>became-valid</code> | <code>became-invalid</code>: Crossing validity boundary.</li><li><code>message-enter</code> | <code>message-exit</code> | <code>message-pulse</code>: Message lifecycle (appears, resolves, re-triggers).</li><li><code>commit-state-changed</code>: Commit state transitioned (committed→rejected, etc.).</li><li><code>baseline-change</code>: Baseline updated (async defaults, reinitialize, reset-post-submit). Owns dirty/touched lifecycle.</li></ul><h3>Per-Level Vocabulary: Group</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><strong>Validity aggregation:</strong> <code>pending</code> if any member pending; <code>invalid</code> if any invalid; else <code>valid</code>. Max severity wins.</li><li><strong>Completion:</strong> <code>empty</code> | <code>partial</code> | <code>complete</code> (vs <code>visited</code> rollup).</li><li><strong>Anchor target:</strong> First errored member by DOM order (e.g., in composite date, target day/month/year sub-field).</li><li><strong>States:</strong> <code>dependsOn</code> reactive graph. Changing a dependency revalidates dependents (fixes requiredIf-latching).</li><li><strong>Events:</strong> <code>member-validity-change</code>, <code>group-became-invalid</code>, <code>dependents-revalidated</code>, message <code>enter</code>/<code>exit</code>.</li></ul><h3>Per-Level Vocabulary: Form</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><strong>Submission lifecycle:</strong> <code>idle</code> → <code>validating</code> (run all, await pending) → <code>blocked</code> (invalid; show summary, move focus) OR <code>submitting</code> (valid; in flight) → <code>submitted</code> | <code>submit-failed</code> (server errors injected).</li><li><strong>Rollup:</strong> Aggregate validity, dirty, visited, pending. <code>submittable</code> = not invalid AND not pending.</li><li><strong>Error summary (GOV.UK dual model):</strong> Summary ordered by DOM, <code>role=alert</code>, focus moved on blocked, entries link to first errored field/member, verbatim parity with inline messages.</li><li><strong>Server reconciliation:</strong> Server errors injected as <code>manual</code> validity source by path. Merge precedence ensures they are not wiped on blur.</li><li><strong>Events:</strong> <code>submit-attempt</code>, <code>submit-blocked {invalid-list}</code>, <code>submit-allowed</code>, <code>submit-start</code>, <code>submit-end {success|error}</code>, summary <code>enter</code>/<code>exit</code>, <code>focus-moved-to-summary</code>.</li></ul><h3>Per-Level Vocabulary: Nested-Form</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><strong>Bubbling policy:</strong> <code>participating</code> (child invalidity blocks parent submit) vs <code>isolated</code> (child validated independently).</li><li><strong>Field arrays:</strong> Errors track rows by stable key (not index). Reorder preserves error association. Array node has separate validity from items.</li><li><strong>Wizard steps:</strong> Per-step gate. <code>gated</code> state blocks advance; <code>step-advance-allowed</code> event fires when active step valid.</li><li><strong>Back-nav value retention:</strong> Stepping back preserves values + baseline (no unregister-on-leave).</li><li><strong>Events:</strong> <code>child-validity-change</code>, <code>step-gate-blocked</code>, <code>step-advance-allowed</code>, row <code>add</code>/<code>remove</code>/<code>reorder</code>.</li></ul><h3>Conformance Tiers</h3><p class=\"text-sm text-gray-600 mb-2\">Implementations conform at one of three tiers. Each builds on the previous:</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><strong>Level 0 — Intent-aware:</strong> Honours the UX intents (context/level/execution/visibility). Renders messages, timing, placement correctly. No state/event contract. (&quot;Does the right UX.&quot;)</li><li><strong>Level 1 — State &amp; event conformant:</strong> Presents observable states (merged validity via <code>aria-invalid</code>/<code>:user-invalid</code>/<code>validity</code> and documented attributes for beyond-native states: <code>data-pending</code>, <code>data-dirty</code>, <code>data-severity</code>, etc.) and emits stable-id events (<code>validation.control.*</code>) with documented <code>detail</code> shapes. Makes implementations <strong>swappable</strong> — swap one control for another, form behaves identically.</li><li><strong>Level 2 — Shape &amp; concern conformant:</strong> Canonical hand-off shapes (<code>ValueSnapshot</code>, <code>SourceResult</code>, <code>MergedValidity</code>, <code>InteractionState</code>, <code>DisplayDecision</code>) are normative. Decomposed concerns (<code>validation.concern.*</code>) are individually replaceable via DI. Makes concerns <strong>combinable</strong> across creators.</li></ul><h3>Features &amp; Capability Model</h3><p class=\"text-sm text-gray-600 mb-2\">Features are optional, trait-like capability bundles. Partial compliance is first-class if gaps are detectable and reported. All implementations declare a <strong>capability manifest:</strong> <code>{ specVersion, conformanceLevel, features: [...], concerns: {...} }</code>.</p><p class=\"text-sm text-gray-600 mb-2\"><strong>Core (mandatory for any conformance):</strong> control validity (error level only), interaction state, display decision, native-constraint source.</p><p class=\"text-sm text-gray-600 mb-2\"><strong>Optional features</strong> (<code>validation.feature.*</code>):</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>async</code>: Pending lifecycle, version tokens, debounce, cancellation, aggregation.</li><li><code>cross-field</code>: <code>dependsOn</code> graph, group-scoped rules, reactive revalidation.</li><li><code>conditional</code>: Dynamic required-if, conditional schema (discriminated union).</li><li><code>field-array</code>: Repeated sections, key-based identity, per-row validation, add/remove/reorder.</li><li><code>wizard</code>: Step gating, per-step validation, forward/back navigation.</li><li><code>error-summary</code>: Form-level summary, focus-on-blocked, GOV.UK dual model (inline + summary parity).</li><li><code>severity</code>: warning/caution/info beyond binary error (Level 1+).</li><li><code>schema</code>: Standard Schema adapter input (vendor-neutral rule definition).</li><li><code>server-reconciliation</code>: Manual validity source injection, server error merging.</li></ul><p class=\"text-sm text-gray-600 mb-2\"><strong>Out-of-capability usage detection:</strong> Build-time check (sibling to <code>check:standards</code>) diffs declared manifest vs atoms used. Runtime dev-mode guard warns/throws if undeclared feature atom is used. (See side-project brief: <code>reports/2026-05-30-validation-spec-versioning-adherence.md</code>.)</p><h3>Decomposed Concerns (DI Registry)</h3><p class=\"text-sm text-gray-600 mb-2\">Each concern is a swappable provider registered via <code>customValidation:&lt;concern&gt;</code>:</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>change-detection</code>: When a value \"changed\" (event listener, signal subscription, polling).</li><li><code>dirty-check</code>: Is current ≠ pristine (strict ===, deep equal, normalized, custom).</li><li><code>inter-element-comms</code>: Control↔group↔form messages (DOM events, injector bus, signals).</li><li><code>validity-computation</code>: Rule engine (native Constraint Validation, Standard Schema adapter, custom).</li><li><code>async-orchestration</code>: Pending, debounce, cancellation, aggregation, version tokens.</li><li><code>message-resolution</code>: Validity code → display string (native localized, i18n, schema issue.message).</li><li><code>display-placement</code>: Message rendering (inline, group summary, error summary, toast).</li><li><code>focus-management</code>: Focus on error (focus-summary-on-submit, focus-first-field, none).</li></ul><h3>Stable-Id Scheme</h3><p class=\"text-sm text-gray-600 mb-2\">Atoms are addressed by stable dot-paths rooted in <code>validation.</code> domain (not intent id), so re-categorization never breaks refs:</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>validation.control.validity.pending</code>, <code>validation.control.validity.invalid</code>, etc. (states)</li><li><code>validation.control.value-input</code>, <code>validation.control.became-valid</code>, etc. (events)</li><li><code>validation.group.*</code>, <code>validation.form.*</code>, <code>validation.nested.*</code> (per-level)</li><li><code>validation.source.native</code>, <code>validation.source.schema</code>, <code>validation.source.async</code>, <code>validation.source.manual</code> (sources)</li><li><code>validation.concern.dirty-check</code>, etc. (DI providers)</li><li><code>validation.feature.async</code>, <code>validation.feature.cross-field</code>, etc. (capabilities)</li></ul><h3>Interface Protocol (Provisional)</h3><pre><code class=\"language-typescript\">type ValidationLevel = 'error' | 'warning' | 'caution' | 'info' | string;\n\ninterface SourceResult {\n    source: 'native' | 'schema' | 'async' | 'manual' | string;\n    state: 'idle' | 'valid' | 'invalid' | 'pending';\n    version?: number; // for pending/async\n    messages?: Array&lt;{ code: string; level: ValidationLevel; path?: string }&gt;;\n}\n\ninterface MergedValidity {\n    state: 'unknown' | 'pending' | 'valid' | 'invalid';\n    blocking: boolean; // any source at level:error\n    severity: ValidationLevel;\n    messages: Array&lt;{ code: string; level: ValidationLevel; path?: string }&gt;;\n}\n\ninterface InteractionState {\n    dirty: boolean; // vs baseline\n    touched: boolean;\n    focused: boolean;\n    submitted: boolean;\n}\n\ninterface DisplayDecision {\n    show: boolean; // gated by visibility intent\n    message: string | null;\n    severity: ValidationLevel;\n    context: 'control' | 'group' | 'form' | 'root';\n}</code></pre>",
    "events": [
      "value-input",
      "value-commit",
      "focus",
      "blur",
      "validate-start",
      "validate-end",
      "validity-changed",
      "became-valid",
      "became-invalid",
      "message-enter",
      "message-exit",
      "message-pulse",
      "commit-state-changed",
      "baseline-change",
      "submit-attempt",
      "submit-blocked",
      "submit-allowed",
      "focus-moved-to-summary"
    ],
    "designSystemResearch": [
      {
        "system": "GOV.UK",
        "components": ["Error summary", "Inline error messages", "Form validation pattern"],
        "keyInsight": "Dual model: submit-only timing (discourages live/blur), error summary at top with focus-to-summary + anchor links, inline messages with verbatim parity. Gold standard for placement + a11y.",
        "reference": "https://design-system.service.gov.uk/patterns/validation/"
      },
      {
        "system": "Ant Design",
        "components": ["Form.Item", "validateStatus", "validateTrigger"],
        "keyInsight": "Comprehensive enum: validateStatus ∈ {success, warning, error, validating}. validateTrigger per-field override (change/blur/submit). validateFirst (short-circuit).",
        "reference": "https://ant.design/components/form/"
      },
      {
        "system": "IBM Carbon",
        "components": ["Form", "TextInput (invalid state)", "Inline loading"],
        "keyInsight": "On-blur default + helper-text→error-message swap. Progress-indicator step error state. Four-state Inline Loading (inactive→active→success→error).",
        "reference": "https://carbondesignsystem.com/patterns/forms/"
      },
      {
        "system": "Material Design 3",
        "components": ["TextField (error state)", "Form validation"],
        "keyInsight": "Error replaces helper text. Warm error hue. Native setValidity() support via custom elements.",
        "reference": "https://m3.material.io/components/text-fields/overview"
      },
      {
        "system": "Web Platform (HTML5 + ARIA)",
        "components": ["Constraint Validation API", "ElementInternals.setValidity()", ":invalid/:user-invalid"],
        "keyInsight": "Native binary validity (only error level). setValidity() carries an anchor element for focus. :user-invalid gates display to touched|submitted. Beyond-native: pending, severity, cross-field.",
        "reference": "https://developer.mozilla.org/en-US/docs/Web/HTML/Guides/Constraint_validation"
      }
    ],
    "uxResearch": [
      {
        "title": "Reward early, punish late",
        "source": "Smashing Magazine / Konjević",
        "findings": [
          "Once a field errors, switch from lazy to eager validation (on change instead of blur).",
          "On fix, clear the error message immediately.",
          "Revert to lazy (on blur) once the field is clean again.",
          "This dynamic flip prevents annoying re-validation while encouraging rapid correction."
        ],
        "reference": "https://medium.com/wdstack/inline-validation-in-forms-designing-the-experience-123fb34088ce"
      },
      {
        "title": "Live validation harm",
        "source": "USWDS (deprecated inline component)",
        "findings": [
          "Live/blur validation can raise error rates and user frustration.",
          "GOV.UK actively discourages it; ship submit-only as the safe default.",
          "If a design requires live, it must be opt-in and carefully tested for UX impact."
        ]
      },
      {
        "title": "Async generation/version token requirement",
        "source": "React Hook Form, Final Form, Vuelidate issue trackers",
        "findings": [
          "Every \"stale resolve clobbers newer error\" bug stems from missing generation tokens.",
          "Results from async validators dispatched for old input versions must be dropped.",
          "RHF #1688, Final Form #420, Vuelidate #364 all trace to this."
        ]
      },
      {
        "title": "Validity merge order (deterministic precedence)",
        "source": "Form library issue trackers",
        "findings": [
          "Concurrent sources (sync, async, manual, schema) must have a declared merge order.",
          "Without it: \"server error wiped on blur\", \"schema overwrites manual error\", \"root toggles isValid\" bugs.",
          "Formik #706, RHF #9982 trace to undefined precedence."
        ]
      }
    ],
    "researchGaps": [
      "Reward-early/punish-late is a dynamic flip (not a flat execution value). Interaction state gates display, not compute timing.",
      "Native validity is strictly binary (error only). Warning/caution/info are beyond-native, advisory-only, never block submission.",
      "visibility:dirty has no native pseudo-class. Must be tracked explicitly in interaction state.",
      "Async needs version token + aggregation + submit-await. Native CVAPI has none.",
      "Concurrent source merge order must be normalized (cross-cutting requirement across all libraries).",
      "Root/global error must block submit without corrupting per-field validity (the RHF \"isValid\" bug family).",
      "Capability/feature modularity + adherence detection is a separate meta-layer (see spec-versioning brief).",
      "DI concern interfaces and their hand-off contracts are normative only at Level 2; Level 1 needs only observable state/events."
    ]
  },
  {
    "id": "modal",
    "name": "Modal Intent",
    "status": "draft",
    "summary": "Protocol defining dialog behavior including stacking, scrolling, sizing, and dismissal strategies.",
    "dimensions": {
      "placement": {
        "description": "Visual position of the modal surface",
        "values": ["center", "top", "bottom", "start", "end", "fill"]
      },
      "mode": {
        "description": "Whether the modal blocks background interaction",
        "values": ["modal", "modeless"]
      },
      "backdrop": {
        "description": "Visual treatment behind the modal",
        "values": ["dimmed", "transparent", "blur"]
      },
      "dismissal": {
        "description": "How the modal can be closed",
        "values": ["light", "explicit", "none", "gesture"]
      },
      "scrolling": {
        "description": "How content scrolling is handled",
        "values": ["contained", "viewport"]
      }
    },
    "description": "<h3>Placement & Docking</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>center</code>: Standard dialog.</li><li><code>top/bottom</code>: Sheet/Drawer behavior (Mobile standard).</li><li><code>start/end</code>: Sidebar/Panel behavior.</li><li><code>fill</code>: Full-screen override.</li></ul><h3>Focus Management</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>trap</code>: Keeps focus within modal (Accessibility Requirement).</li><li><code>restore</code>: Returns focus to trigger when closed.</li><li><code>initial</code>: Default focus element (e.g., First input vs Close button).</li></ul><h3>Lifecycle Events</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>enter</code>: Modal opens and becomes active.</li><li><code>exit</code>: Modal closes.</li><li><code>recede</code>: Modal moves to background (when a new stacked modal opens).</li><li><code>advance</code>: Modal moves to foreground (when the top stacked modal closes).</li></ul><h3>Design System Alignment</h3><div class=\"comparison-table-wrapper\"><table class=\"comparison-table\"><thead><tr><th>System</th><th>Component</th><th>Placement</th><th>Backdrop</th><th>Dismissal</th></tr></thead><tbody><tr><td>Material 3</td><td>Basic Dialog</td><td><code>center</code></td><td><code>dimmed</code></td><td><code>light</code></td></tr><tr><td>Material 3</td><td>Full-screen</td><td><code>fill</code></td><td><code>transparent</code></td><td><code>explicit</code></td></tr><tr><td>Carbon</td><td>Modal</td><td><code>top</code></td><td><code>dimmed</code></td><td><code>explicit</code></td></tr><tr><td>Apple HIG</td><td>Sheet</td><td><code>bottom</code></td><td><code>dimmed</code></td><td><code>light</code></td></tr><tr><td>Radix UI</td><td>Dialog</td><td><code>center</code></td><td><code>dimmed</code></td><td><code>light</code></td></tr></tbody></table></div><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type Dismissal = 'light' | 'explicit' | 'none';\ntype Placement = 'center' | 'top' | 'bottom' | 'start' | 'end' | 'fill';\ntype Backdrop = 'dimmed' | 'transparent' | 'blur';\n\ninterface FocusStrategy {\n    trap: boolean;\n    restore: boolean;\n    initial?: string; // Selector\n}\n\ninterface ModalIntent {\n    // Role (ARIA)\n    role: 'dialog' | 'alertdialog';\n    // Visual placement\n    placement: Placement;\n    // Interaction Mode (Blocks background?)\n    mode: 'modal' | 'modeless';\n    // Mobile Snap Points (Sheet)\n    detents?: { initial: 'medium' | 'large' | 'full'; stops: string[] };\n    // Backdrop visual\n    backdrop: Backdrop;\n    // How the modal can be closed\n    dismissal: Dismissal | 'gesture';\n    // Focus Management\n    focus: FocusStrategy;\n    // Allow other modals on top?\n    stackable: boolean;\n    // Scroll strategy\n    scrolling: 'contained' | 'viewport';\n    // Responsive sizing map\n    size: 'auto' | 'fullscreen' | Record&lt;string, string&gt;;\n    // Motion bindings\n    animations?: Partial<Record<'enter' | 'exit' | 'recede' | 'advance', string>>;\n}</code></pre>",
    "events": [
      "enter",
      "exit",
      "recede",
      "advance"
    ]
  },
  {
    "id": "action",
    "name": "Action Intent",
    "status": "draft",
    "summary": "Protocol defining the semantic hierarchy (levels) of interactive elements to establish visual priority.",
    "dimensions": {
      "level": {
        "description": "The semantic weight determining visual prominence",
        "values": ["primary", "secondary", "tertiary", "destructive"]
      },
      "busy": {
        "description": "What the user experiences when the action is pending",
        "values": ["disabled", "interactive"]
      },
      "groupOrdering": {
        "description": "How actions are ordered in a group",
        "values": ["dom", "reverse", "platform"]
      },
      "groupSizing": {
        "description": "How actions are sized in a group",
        "values": ["auto", "stretch", "uniform"]
      }
    },
    "description": "<h3>Action Levels</h3><p class=\"text-sm text-gray-600 mb-2\">The semantic weight of an action, determining its visual prominence. Every design system converges on this hierarchy.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>primary</code>: Principal call to action. Highest visual weight (Material FAB, Fluent Solid).</li><li><code>secondary</code>: Alternative actions. Moderate visual weight (Outlined, Tonal).</li><li><code>tertiary</code>: Supplemental actions. Minimal visual weight (Text, Ghost).</li><li><code>destructive</code>: Negative actions. Implies danger semantics (typically red).</li></ul><h3>Grouping & Alignment</h3><p class=\"text-sm text-gray-600 mb-2\">Spatial relationships for collections of actions (Dialog footers, Toolbars, Form actions).</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>ordering</code>: <strong>Platform-aware</strong> (macOS: Primary Last, Windows: Primary First) vs <strong>DOM order</strong> (as authored).</li><li><code>sizing</code>: <strong>Stretch</strong> (full-width, common on mobile) vs <strong>Auto</strong> (content-sized, common on desktop).</li><li><code>placement</code>: <strong>End</strong> (Dialogs), <strong>Start</strong> (Forms), <strong>Split</strong> (primary and secondary at opposite ends).</li></ul><h3>Busy State</h3><p class=\"text-sm text-gray-600 mb-2\">What the user experiences when an action is pending. The implementing block handles the CSS and layout details.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>interaction</code>: Whether the action is <strong>disabled</strong> or remains <strong>interactive</strong> during the pending state.</li><li><code>indicator</code>: Whether a loading indicator appears, and its position relative to the label (<code>start</code>, <code>end</code>, <code>center</code>, <code>overlay</code>).</li><li><code>label</code>: Whether the label changes (e.g., \"Save\" → \"Saving...\"), hides, or remains as-is.</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type ActionLevel = 'primary' | 'secondary' | 'tertiary' | 'destructive' | string;\ntype IndicatorPosition = 'start' | 'end' | 'center' | 'overlay';\n\ninterface ActionBusy {\n    // Is the action disabled while busy?\n    interaction: 'disabled' | 'interactive';\n    // Where to show a loading indicator (if any)\n    indicator?: IndicatorPosition;\n    // Temporary label during pending state\n    label?: string;\n}\n\ninterface ActionGroupIntent {\n    alignment: 'start' | 'end' | 'center' | 'justify' | 'split';\n    ordering: 'dom' | 'reverse' | 'platform';\n    sizing: 'auto' | 'stretch' | 'uniform';\n}\n\ninterface ActionIntent {\n    // The semantic weight\n    level: ActionLevel;\n    // What the user experiences when the action is pending\n    busy?: ActionBusy;\n    // Icon configuration (references Icon Intent)\n    icon?: { position?: 'start' | 'end' };\n    // Grouping Context (for Collections)\n    group?: ActionGroupIntent;\n}</code></pre>",
    "events": []
  },
  {
    "id": "icon",
    "name": "Icon Intent",
    "status": "draft",
    "summary": "Protocol defining iconography standards including sizing scales, weight, and positioning strategies.",
    "dimensions": {
      "size": {
        "description": "The visual size of the icon",
        "values": ["sm", "md", "lg", "xl"]
      },
      "weight": {
        "description": "Stroke weight for variable icons",
        "values": ["light", "regular", "bold"]
      },
      "style": {
        "description": "Render style of the icon",
        "values": ["outlined", "filled"]
      }
    },
    "description": "<h3>Sizing Scale</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>sm</code>: Small (e.g., 16px). For inline text or tight density.</li><li><code>md</code>: Medium (e.g., 20px). Standard usage.</li><li><code>lg</code>: Large (e.g., 24px). Feature icons or comfortable buttons.</li><li><code>xl</code>: Extra Large (e.g., 32px+). Hero or empty states.</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type IconSize = 'sm' | 'md' | 'lg' | 'xl' | string;\n\ninterface IconIntent {\n    // The visual size identifier\n    size: IconSize;\n    // Name of the icon asset/glyph\n    name: string;\n    // Stroke weight (if variable)\n    weight?: 'light' | 'regular' | 'bold';\n    // Render style\n    style?: 'outlined' | 'filled';\n}</code></pre>"
  },
  {
    "id": "breakpoint",
    "name": "Breakpoint Intent",
    "status": "draft",
    "summary": "Protocol defining display adaptation strategies, abstracting viewport tiers and container queries.",
    "dimensions": {
      "strategy": {
        "description": "How layout rules accumulate as the viewport changes",
        "values": ["mobile-first", "desktop-first", "strict-range"]
      },
      "steps": {
        "description": "Named tiers describing the user's display context",
        "values": ["compact", "medium", "expanded"]
      }
    },
    "description": "<h3>Adaptation Strategy</h3><p class=\"text-sm text-gray-600 mb-2\">How layout rules accumulate or subtract as the viewport changes. This determines the mental model for authoring responsive layouts.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>mobile-first</code>: Start small, add complexity as space grows. Standard approach (Tailwind, Bootstrap).</li><li><code>desktop-first</code>: Start large, remove features as space shrinks. Legacy approach.</li><li><code>strict-range</code>: Each size tier is an isolated bucket — no accumulation. Used by Material Design 3.</li></ul><h3>Semantic Steps</h3><p class=\"text-sm text-gray-600 mb-2\">Named tiers that describe the user's display context, independent of specific pixel values. The implementing block maps these to concrete media/container queries.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>compact</code>: Phones in portrait. Single-column layouts, stacked navigation.</li><li><code>medium</code>: Tablets or phones in landscape. Two-column layouts, rail navigation.</li><li><code>expanded</code>: Desktops or large tablets. Multi-column layouts, persistent navigation.</li><li><code>[custom]</code>: Application-defined tiers (e.g., <code>ultrawide</code> for multi-pane dashboards).</li></ul><h3>Logical Groups</h3><p class=\"text-sm text-gray-600 mb-2\">Aliases that combine steps for convenience. Allows components to query \"am I in a small context?\" without knowing specific tier names.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>small</code>: Groups <code>compact</code> (equivalent to \"mobile\").</li><li><code>large</code>: Groups <code>expanded</code> and above (equivalent to \"desktop\").</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type AdaptationStrategy = 'mobile-first' | 'desktop-first' | 'strict-range';\n\ninterface BreakpointIntent {\n    // How layout rules accumulate\n    strategy: AdaptationStrategy;\n    // Ordered list of semantic step names (smallest to largest)\n    steps: string[];\n    // Logical groups that alias one or more steps\n    aliases?: Record&lt;string, string[]&gt;;\n}</code></pre>",
    "events": [
      "change"
    ]
  },
  {
    "id": "typography",
    "name": "Typography Intent",
    "status": "draft",
    "summary": "Protocol abstracting visual hierarchy from semantic HTML, normalizing roles across Design Systems.",
    "dimensions": {
      "role": {
        "description": "The semantic purpose of the text",
        "values": ["display", "heading", "body", "label", "code"]
      },
      "scale": {
        "description": "Relative size/emphasis within the role",
        "values": ["xs", "sm", "md", "lg", "xl"]
      }
    },
    "description": "<h3>Role Definition</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>display</code>: Expressive/Decorative (Hero, Splash). Low density.</li><li><code>heading</code>: Structural hierarchy (H1-H6 markers).</li><li><code>body</code>: focused on Readability (Long-form content).</li><li><code>label</code>: focused on Legibility (UI Components, Buttons).</li><li><code>code</code>: Monospace technical data.</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type TypographyRole = 'display' | 'heading' | 'body' | 'label' | 'code';\n// T-shirt scale relative to the Role base size\ntype TypographyScale = 'xs' | 'sm' | 'md' | 'lg' | 'xl';\n\ninterface ToneFeatures {\n    // Force monospace numbers (Data Grids)\n    tabular?: boolean;\n    // Overflow behavior\n    truncation?: 'clip' | 'ellipsis' | 'fade';\n    // Disable definition-based optical sizing\n    opticalSizing?: 'auto' | 'none';\n}\n\ninterface TypographyIntent {\n    // The semantic purpose\n    role: TypographyRole;\n    // Relative size/emphasis within that role\n    scale?: TypographyScale;\n    // Rendering constraints\n    features?: ToneFeatures;\n}</code></pre>"
  },
  {
    "id": "surface",
    "name": "Surface Intent",
    "status": "draft",
    "summary": "Protocol defining Z-Axis depth, material texture (Glass/Solid), and interactions across lighting modes.",
    "dimensions": {
      "texture": {
        "description": "Physical material of the surface",
        "values": ["solid", "glass", "transparent"]
      },
      "interaction": {
        "description": "Visual response to user interaction",
        "values": ["static", "lift", "scale"]
      }
    },
    "description": "<h3>Elevation Semantics (Z-Axis)</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>0</code>: Base. Application background.</li><li><code>1-2</code>: Grouping content. (Cards, Lists).</li><li><code>3-5</code>: Floating content. (Sticky Headers, Modals, Tooltips).</li></ul><h3>Material Texture</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>solid</code>: Opaque. Standard for Material Design 3 (Surface Tones).</li><li><code>glass</code>: Blurred background. Standard for macOS/iOS (Vibrancy) and Windows (Mica/Acrylic).</li><li><code>transparent</code>: Layout-only containment.</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type SurfaceMaterial = 'solid' | 'glass' | 'transparent';\ntype SurfaceInteraction = 'static' | 'lift' | 'scale';\n\ninterface SurfaceIntent {\n    // Z-axis position relative to parent (0-5)\n    elevation: number;\n    \n    // Physical texture of the surface\n    texture: SurfaceMaterial;\n    \n    // Visual variation (Default vs High Contrast)\n    variant: 'default' | 'alt';\n    \n    // Interaction physics\n    interaction: SurfaceInteraction;\n}</code></pre>"
  },
  {
    "id": "feedback",
    "name": "Feedback Intent",
    "status": "draft",
    "summary": "Protocol defining transient, non-blocking messaging (Toasts/Snackbars) and their queuing logic. Severity dimension participates in the unified severity/tone vocabulary shared with Message and Validation intents.",
    "dimensions": {
      "placement": {
        "description": "Positioning strategy for the notification",
        "values": ["top-start", "top-end", "bottom-start", "bottom-end", "top-center", "bottom-center"]
      },
      "severity": {
        "description": "Semantic weight of the message. Part of unified severity vocabulary: info (informational), success (confirmation), warning (advisory), error (failure).",
        "values": ["info", "success", "warning", "error"]
      },
      "duration": {
        "description": "How long the notification persists",
        "values": ["short", "long", "indefinite"]
      }
    },
    "description": "<h3>Placement Logic</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>top-end</code>: Standard for Desktop notifications (Toasts).</li><li><code>bottom-start</code>: Standard for Material Design (Snackbars). Avoids FAB interaction.</li><li><code>top-center</code>: Mobile alerts.</li></ul><h3>Severity Semantics</h3><p class=\"text-sm text-gray-600 mb-2\"><code>severity</code> is part of a <strong>unified severity/tone vocabulary</strong> shared across Feedback (transient), Message (structural), and Validation (input states). All three use the same semantic set:</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>info</code>: Informational. Neutral tone. No action required.</li><li><code>success</code>: Confirmation. Positive outcome achieved.</li><li><code>warning</code>: Advisory. Issue detected but not blocking. User may act.</li><li><code>error</code>: Failure. Problem occurred. User action required.</li></ul><h3>Timing & Duration</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>short</code>: ~4000ms. For simple confirmations (\"Saved\").</li><li><code>long</code>: ~10000ms. If text > 60 chars.</li><li><code>indefinite</code>: Remains until dismissed. Required if an Action is present (Accessibility).</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type FeedbackPlacement = 'top-start' | 'top-end' | 'bottom-start' | 'bottom-end' | 'top-center' | 'bottom-center';\ntype FeedbackSeverity = 'info' | 'success' | 'warning' | 'error';\n\ninterface FeedbackAction {\n    label: string;\n    intent: string;\n}\n\ninterface FeedbackIntent {\n    // Positioning strategy\n    placement: FeedbackPlacement;\n    // Semantic weight (shared severity vocabulary)\n    severity: FeedbackSeverity;\n    // Persistence\n    duration: 'short' | 'long' | 'indefinite';\n    // Queue Management (1 = Single/Replace, >1 = Stack)\n    stackLimit: number;\n    // Optional interactive element (e.g. \"Undo\")\n    action?: FeedbackAction;\n}</code></pre>"
  },
  {
    "id": "selection",
    "name": "Selection Intent",
    "status": "draft",
    "summary": "Protocol defining choice behaviors (Single vs Multi) and state urgency, distinguishing interactions from form data.",
    "dimensions": {
      "model": {
        "description": "How many items can be selected",
        "values": ["single", "multiple"]
      },
      "immediacy": {
        "description": "When the selection state is committed",
        "values": ["live", "buffered"]
      },
      "variant": {
        "description": "Visual metaphor for the selection",
        "values": ["boolean", "item", "range"]
      }
    },
    "description": "<h3>Choice Models</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>single</code>: Mutually exclusive. Renders as <strong>Radio</strong> (Forms), <strong>Switch</strong> (Settings), or <strong>Select</strong> (Compact).</li><li><code>multiple</code>: Accumulative. Renders as <strong>Checkbox</strong> (List) or <strong>Filter Chips</strong> (Toolbar).</li></ul><h3>Immediacy Profile</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>live</code>: State changes immediately (Settings toggle).</li><li><code>buffered</code>: State changes on submission (Form field).</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type SelectionModel = 'single' | 'multiple';\ntype SelectionVariant = 'boolean' | 'item' | 'range';\n\ninterface SelectionIntent {\n    // How many can be selected\n    model: SelectionModel;\n    // When the state is committed\n    immediacy: 'live' | 'buffered';\n    // Visual metaphor\n    variant: SelectionVariant;\n    // Constraints\n    constraints?: { min?: number; max?: number };\n}</code></pre>"
  },
  {
    "id": "input",
    "name": "Input Intent",
    "status": "draft",
    "summary": "Protocol for text entry standardization including affordances, privacy masking, and visual variants.",
    "dimensions": {
      "variant": {
        "description": "Visual style of the input",
        "values": ["outlined", "filled", "borderless"]
      },
      "affordances": {
        "description": "Interaction helpers shown with the input",
        "values": ["clear", "reveal", "copy"]
      },
      "status": {
        "description": "Interaction state of the input",
        "values": ["enabled", "disabled", "readonly"]
      }
    },
    "description": "<h3>Visual Variants</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>outlined</code>: High contrast (Material default). Defined boundary.</li><li><code>filled</code>: High emphasis background (Fluent/Material).</li><li><code>borderless</code>: Minimalist (iOS List style).</li></ul><h3>Affordances</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>clear</code>: 'X' button to reset value.</li><li><code>reveal</code>: Eye icon for passwords.</li><li><code>copy</code>: Clipboard action.</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type InputVariant = 'outlined' | 'filled' | 'borderless';\ntype InputAffordance = 'clear' | 'reveal' | 'copy';\n\ninterface InputIntent {\n    // Visual style\n    variant: InputVariant;\n    // Privacy masking (dots)\n    masking: boolean;\n    // Interaction helpers\n    affordances: InputAffordance[];\n    // State\n    status: 'enabled' | 'disabled' | 'readonly';\n}</code></pre>"
  },
  {
    "id": "navigation",
    "name": "Navigation Intent",
    "status": "concept",
    "summary": "Protocol defining application structure (Hierarchical, Lateral, Linear) and view transition logic.",
    "dimensions": {
      "structure": {
        "description": "The navigation model governing view transitions and history",
        "values": ["hierarchical", "lateral", "linear"]
      }
    },
    "description": "<h3>Structure Model</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>hierarchical</code>: Parent/Child stack. Managing history and back-navigation.</li><li><code>lateral</code>: Peer switching (Tabs, Bottom Nav). No history stack.</li><li><code>linear</code>: Sequential steps (Wizard). Locked progression.</li></ul>"
  },
  {
    "id": "anchor",
    "name": "Anchor Intent",
    "status": "draft",
    "summary": "Protocol for tethered surfaces (Tooltips, Popovers, Menus) defining placement, collision handling, and tracking. Position computation is delegated to a shared positioning provider; top-layer escape composes with Web Portals.",
    "dimensions": {
      "type": {
        "description": "The role of the anchored surface",
        "values": ["tooltip", "popover", "menu"]
      },
      "placement": {
        "description": "Preferred position of the anchored element relative to its anchor (12-way grid + auto)",
        "values": ["top", "top-start", "top-end", "right", "right-start", "right-end", "bottom", "bottom-start", "bottom-end", "left", "left-start", "left-end", "auto"]
      },
      "collision": {
        "description": "Ordered fallback pipeline applied when the preferred placement overflows the viewport or a clipping ancestor",
        "values": ["flip", "shift", "hide", "none"]
      },
      "strategy": {
        "description": "How the anchored element stacks. inline stays in normal flow; escape lifts it to the top layer (composes Web Portals)",
        "values": ["inline", "escape"]
      },
      "tracking": {
        "description": "Whether the position is recomputed as layout changes",
        "values": ["static", "follow"]
      }
    },
    "description": "<h3>Tether Contract</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>tooltip</code>: Informational. Hover/Focus trigger. Non-interactive.</li><li><code>popover</code>: Interactive content. Click trigger. Traps focus if modal.</li><li><code>menu</code>: Action list. Click/Context trigger. Keyboard navigable.</li></ul><h3>Placement</h3><p class=\"text-sm text-gray-600 mb-2\">The preferred side and alignment of the <strong>anchored element</strong> relative to its <strong>anchor element</strong>, using the platform 12-way grid (side &times; <code>start</code>/center/<code>end</code>). <code>auto</code> defers the choice to the provider (most-available-space).</p><h3>Collision</h3><p class=\"text-sm text-gray-600 mb-2\">An ordered fallback pipeline applied when the preferred placement would overflow.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>flip</code>: Move to the opposite side (CSS <code>position-try</code> / Floating UI <code>flip</code>).</li><li><code>shift</code>: Slide along the axis to stay in view (<code>shift</code>).</li><li><code>hide</code>: Hide the anchored element when the anchor is clipped or scrolled off-screen (CSS <code>position-visibility</code>).</li><li><code>none</code>: No fallback; honor the requested placement verbatim.</li></ul><h3>Strategy</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>inline</code>: The anchored element stays in normal flow / stacking context (CSS <code>anchor()</code>, absolute).</li><li><code>escape</code>: The anchored element lifts to the top layer to escape clipping ancestors &mdash; composed via <a href=\"/projects/webportals/\">Web Portals</a> (DOM/context escape) and the Surface Intent (elevation), not re-implemented here.</li></ul><h3>Delegation</h3><p class=\"text-sm text-gray-600 mb-2\">This intent declares the <em>what</em>; the <em>how</em> (position math, collision, tracking) is delegated to a registered <a href=\"/plugs/custompositioner/\">positioning provider</a> resolved through <a href=\"/plugs/custompositioningregistry/\">CustomPositioningRegistry</a> &mdash; native-first (CSS Anchor Positioning) by default, or a library adapter (Floating UI). See <a href=\"/projects/webpositioning/\">Web Positioning</a>.</p><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type SurfaceRole = 'tooltip' | 'popover' | 'menu';\ntype Placement = 'top' | 'top-start' | 'top-end'\n  | 'right' | 'right-start' | 'right-end'\n  | 'bottom' | 'bottom-start' | 'bottom-end'\n  | 'left' | 'left-start' | 'left-end' | 'auto';\ntype CollisionStrategy = 'flip' | 'shift' | 'hide' | 'none';\ntype AnchorStrategy = 'inline' | 'escape';   // 'escape' composes Web Portals (top layer)\n\ninterface AnchorIntent {\n    // The role of the anchored surface\n    role: SurfaceRole;\n    // Preferred position relative to the anchor\n    placement: Placement;\n    // Ordered fallback pipeline on overflow\n    collision: CollisionStrategy[];\n    // Normal flow vs. top-layer escape\n    strategy: AnchorStrategy;\n    // Gap from the anchor edge (px)\n    offset?: number;\n    // Recompute on scroll / resize\n    tracking?: 'static' | 'follow';\n    // Render a pointer toward the anchor\n    arrow?: boolean;\n}</code></pre>",
    "designSystemResearch": [
      {
        "system": "CSS Anchor Positioning",
        "components": ["anchor-name", "position-anchor", "anchor()", "position-area", "position-try / @position-try", "position-visibility"],
        "keyInsight": "Native, Baseline 2026. Unifies placement + collision fallback in CSS while leaving layering (top layer) to the Popover API. We adopt this separation: the Anchor Intent owns placement, Web Portals owns escape.",
        "reference": "https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Anchor_positioning"
      },
      {
        "system": "Popover API",
        "components": ["popover", "popovertarget", "top layer", "light dismiss"],
        "keyInsight": "Handles top-layer stacking and light dismiss, but NOT positioning — it delegates placement to CSS Anchor Positioning via the implicit anchor. Confirms positioning and layering are orthogonal concerns.",
        "reference": "https://developer.mozilla.org/en-US/docs/Web/API/Popover_API"
      },
      {
        "system": "Floating UI",
        "components": ["placement (12-way)", "middleware: offset/flip/shift/size/arrow/hide/autoPlacement", "autoUpdate", "strategy"],
        "keyInsight": "Combines anchoring + collision + escape in one JS engine. Our dimension and option vocabulary mirrors it 1:1 so the adapter maps directly; Tier-3 middleware (autoPlacement, size, inline) stay adapter-only.",
        "reference": "https://floating-ui.com/docs/middleware"
      },
      {
        "system": "Radix UI",
        "components": ["Popper primitive", "side / align", "avoidCollisions", "collisionPadding", "sticky"],
        "keyInsight": "Headless side/align maps to our placement; avoidCollisions/collisionPadding map to the collision pipeline. Validates exposing collision as a first-class, configurable axis.",
        "reference": "https://www.radix-ui.com/primitives/docs/utilities/popper"
      },
      {
        "system": "Material Design 3",
        "components": ["Menus", "Tooltips (plain / rich)"],
        "keyInsight": "Menus reposition to remain on-screen and align to the anchor edge; rich tooltips persist and are interactive. Informs the tooltip/popover/menu role split.",
        "reference": "https://m3.material.io/components/menus/overview"
      }
    ],
    "researchGaps": [
      "Escape delegation depth — should strategy:'escape' drive the native Popover top layer directly, or always route through Web Portals for context preservation? Define the handoff.",
      "Arrow placement math — ship a basic built-in arrow in the native provider, or always defer to an adapter (Floating UI arrow())?",
      "RTL and safe-area handling (notch, virtual keyboard) in the native-first default provider."
    ]
  },
  {
    "id": "temporal",
    "name": "Temporal Intent",
    "status": "concept",
    "summary": "Protocol abstracting date/time selection paradigms across mouse, touch, and keyboard modalities.",
    "dimensions": {
      "presentation": {
        "description": "The visual paradigm for date/time selection",
        "values": ["media", "linear", "input"]
      },
      "granularity": {
        "description": "The selection scope",
        "values": ["point", "range", "multi"]
      }
    },
    "description": "<h3>Presentation Paradigms</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>media</code>: Spatial surfaces. Wall calendars (Grid) or Analog Clocks.</li><li><code>linear</code>: Sequential lists. Scrolling Wheels (iOS) or Listboxes.</li><li><code>input</code>: Text-based. Masked inputs or simple native fallbacks.</li></ul><h3>Granularity</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>point</code>: Single instance (Date/Time).</li><li><code>range</code>: Span between two points.</li><li><code>multi</code>: Disconnected sets of points.</li></ul>"
  },
  {
    "id": "layout",
    "name": "Layout Intent",
    "status": "concept",
    "summary": "Protocol defining the application shell structure and region mechanics (Drawers, Rails, Panels).",
    "dimensions": {
      "behavior": {
        "description": "How regions interact with the main content area",
        "values": ["push", "overlay", "rail"]
      }
    },
    "description": "<h3>Region Behavior</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>push</code>: Region consumes space, shifting the main content (Desktop).</li><li><code>overlay</code>: Region floats above content with a backdrop (Mobile).</li><li><code>rail</code>: Region collapses to a thin strip (Icons) rather than disappearing.</li></ul><h3>Anatomy</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>shell</code>: The global viewport container.</li><li><code>pane</code>: A resizable subdivision of the shell.</li><li><code>dock</code>: A sticky region (Header/Footer).</li></ul>"
  },
  {
    "id": "message",
    "name": "Message Intent",
    "status": "concept",
    "summary": "Protocol defining communication priority (Attention) and structural anatomy across alerts, banners, and dialogs. Tone dimension participates in the unified severity/tone vocabulary shared with Feedback and Validation intents.",
    "dimensions": {
      "attention": {
        "description": "Priority level determining how the message interrupts the user",
        "values": ["ambient", "routine", "urgent", "critical"]
      },
      "tone": {
        "description": "Semantic meaning conveyed by the message. Part of unified severity vocabulary: neutral↔info, positive↔success, caution↔warning, negative↔error.",
        "values": ["neutral", "positive", "caution", "negative"]
      }
    },
    "description": "<h3>Attention Hierarchy</h3><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>ambient</code>: Low priority. Status bars or helper text.</li><li><code>routine</code>: Standard priority. Toasts or inline notes.</li><li><code>urgent</code>: High priority. Banners or System Alerts.</li><li><code>critical</code>: Blocking priority. Dialogs or Screen Takeovers.</li></ul><h3>Tone Semantics</h3><p class=\"text-sm text-gray-600 mb-2\"><code>tone</code> is part of a <strong>unified severity/tone vocabulary</strong> shared across Message (structural), Feedback (transient), and Validation (input states). The semantic mapping aligns all three:</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>neutral</code> / info: Informational. No action required.</li><li><code>positive</code> / success: Confirmation. Positive outcome achieved.</li><li><code>caution</code> / warning: Advisory. Issue detected but not blocking. User may act.</li><li><code>negative</code> / error: Failure. Problem occurred. User action required.</li></ul>"
  },
  {
    "id": "reliability",
    "name": "Reliability Intent",
    "status": "draft",
    "summary": "Protocol defining error recovery strategies, retry policies, and outcome handling for mechanism failures (system/operation errors). Distinct from input-invalidity (validation domain): Reliability handles operation failures (timeout, crash, network); Validation handles input errors (field wrong, value out of range).",
    "dimensions": {
      "tolerance": {
        "description": "How severely the user is affected by failure",
        "values": ["forgivable", "degraded", "terminal"]
      },
      "recovery": {
        "description": "What the user experiences during system recovery",
        "values": ["automatic", "manual", "none"]
      },
      "fallback": {
        "description": "What replaces the failed content",
        "values": ["skeleton", "empty", "previous"]
      }
    },
    "description": "<h3>Scope: Mechanism Failure (not Input Invalidity)</h3><p class=\"text-sm text-gray-600 mb-2\"><strong>Reliability is strictly for operation/system failure,</strong> not validation errors:</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><strong>Input-invalidity</strong> (Validation domain): \"The user's email is malformed\" or \"These passwords don't match\". This is an expected outcome; the field is wrong, and the user must fix it. Validation owns this.</li><li><strong>Mechanism failure</strong> (Reliability domain): \"The uniqueness check timed out\" or \"The server is unreachable\" or \"The async validator crashed\". This is an unexpected system problem. Reliability owns this.</li></ul><p class=\"text-sm text-gray-600 mb-2\">A validation scenario that includes async (e.g., checking if an email is already registered) may emit a Reliability error if the check itself fails (server down), but a Validation error if the email is taken. The distinction prevents conflating the two error families.</p><h3>Failure Tolerance</h3><p class=\"text-sm text-gray-600 mb-2\">How severely the user is affected when an operation fails. This determines the visual response and the user's ability to continue.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>forgivable</code>: Operation failed but the user can continue. Communicated silently (logged) or with minimal disruption (toast). App state remains valid.</li><li><code>degraded</code>: Partial failure. The UI renders with missing data or fallback content. The user can still interact but the experience is incomplete.</li><li><code>terminal</code>: Broken state. The component cannot render meaningfully. Requires replacement with an error boundary or a full-page error state.</li></ul><h3>Recovery Experience</h3><p class=\"text-sm text-gray-600 mb-2\">What the user experiences while the system attempts to recover from a failure. The implementing block decides the technical retry strategy (timing, backoff algorithm).</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>automatic</code>: The system retries silently. The user sees a loading state but takes no action.</li><li><code>manual</code>: The user is presented with a \"Retry\" affordance and decides when to try again.</li><li><code>none</code>: No recovery is attempted. The failure state is final.</li></ul><h3>Fallback Content</h3><p class=\"text-sm text-gray-600 mb-2\">What the user sees in place of the failed content.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>skeleton</code>: Structural placeholder, implying the content may load later.</li><li><code>empty</code>: Explicit empty state with messaging (e.g., \"No data available\").</li><li><code>previous</code>: Stale data from the last successful load, marked as outdated.</li></ul><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type FailureTolerance = 'forgivable' | 'degraded' | 'terminal';\ntype RecoveryExperience = 'automatic' | 'manual' | 'none';\ntype FallbackContent = 'skeleton' | 'empty' | 'previous';\n\ninterface ReliabilityIntent {\n    // How severely the user is affected\n    tolerance: FailureTolerance;\n    // What the user experiences during recovery\n    recovery: RecoveryExperience;\n    // What replaces the failed content\n    fallback?: FallbackContent;\n}</code></pre>"
  },
  {
    "id": "type-ahead",
    "name": "Type-Ahead Intent",
    "status": "draft",
    "summary": "Protocol for keyboard type-ahead navigation: typed characters move focus to the matching item, with a buffer that resets after an idle timeout. Distinct from filtering.",
    "dimensions": {
      "matching": {
        "description": "How typed characters map to items",
        "values": ["prefix", "cycle", "prefix-then-cycle"]
      },
      "reset": {
        "description": "When the accumulated character buffer clears",
        "values": ["debounced", "custom"]
      },
      "wrap": {
        "description": "Focus behavior at the ends of the collection",
        "values": ["wrap", "clamp"]
      }
    },
    "description": "<h3>Matching Strategy</h3><p class=\"text-sm text-gray-600 mb-2\">How typed characters are mapped to an item. Type-ahead <strong>moves focus</strong> to an existing item — it never removes or hides items (that is filtering, see <code>aria-autocomplete=\"list\"</code> on a combobox).</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>prefix</code>: Characters accumulate into a buffer; focus moves to the first item whose label starts with the buffer.</li><li><code>cycle</code>: A repeated single character cycles focus through items whose label starts with that character.</li><li><code>prefix-then-cycle</code>: <strong>Recommended default</strong>. Matches WAI-ARIA APG and native <code>&lt;select&gt;</code> — characters typed in rapid succession accumulate into a prefix, while a lone repeated key cycles among same-initial items.</li></ul><h3>Reset Timing</h3><p class=\"text-sm text-gray-600 mb-2\">When the character buffer is discarded so the next keystroke starts a fresh search.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>debounced</code>: Buffer clears after an idle timeout (defaults to <strong>500ms</strong>, the APG / native <code>&lt;select&gt;</code> convention).</li><li><code>custom</code>: Application-defined timeout via <code>resetMs</code>.</li></ul><h3>Wrap Behavior</h3><p class=\"text-sm text-gray-600 mb-2\">What happens when matching passes the last item.</p><ul class=\"text-sm text-gray-600 list-disc pl-5\"><li><code>wrap</code>: Search wraps around to the start of the collection (default).</li><li><code>clamp</code>: Search stops at the ends without wrapping.</li></ul><h3>Label Source</h3><p class=\"text-sm text-gray-600 mb-2\">The text each item is matched against. Defaults, in order: <code>aria-label</code>, the element referenced by <code>aria-labelledby</code>, then <code>textContent</code>. Applications may override via a <code>label</code> resolver.</p><h3>Interface Protocol</h3><pre><code class=\"language-typescript\">type TypeAheadMatching = 'prefix' | 'cycle' | 'prefix-then-cycle';\n\ninterface TypeAheadIntent {\n    // How typed characters map to items\n    matching: TypeAheadMatching;\n    // Idle ms before the buffer resets (default 500)\n    resetMs: number;\n    // Wrap focus past the ends of the collection (default true)\n    wrap: boolean;\n    // Override how an item's searchable label is derived\n    label?: (item: Element) =&gt; string;\n}</code></pre>",
    "events": ["typeahead-match", "typeahead-nomatch", "typeahead-reset"]
  }
]
