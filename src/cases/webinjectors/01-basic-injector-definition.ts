<!-- WEB CASE 1: Basic Injector Definition -->
// definitions.ts
export injector definitions {
    // Define resources inside the injector
    const Resource = 100;
    // Expose them to the outside world
    provide { Resource };
}
