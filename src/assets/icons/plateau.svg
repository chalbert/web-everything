<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128" width="128" height="128">
  <defs>
    <!-- Amber for Plateau -->
    <linearGradient id="plateauGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#f59e0b" />
      <stop offset="100%" stop-color="#d97706" />
    </linearGradient>
    <filter id="floatShadow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur in="SourceAlpha" stdDeviation="4" />
      <feOffset dx="2" dy="4" result="offsetblur" />
      <feComponentTransfer><feFuncA type="linear" slope="0.2"/></feComponentTransfer>
      <feMerge><feMergeNode/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>
  </defs>

  <!-- Background -->
  <rect x="14" y="14" width="100" height="100" rx="30" fill="#f8fafc" />

  <!-- Stacked plateau layers (flat-topped mesa) -->
  <g filter="url(#floatShadow)">
    <!-- Top tier -->
    <path d="M52 44 H84 L92 56 H44 Z" fill="url(#plateauGrad)" />
    <!-- Middle tier -->
    <path d="M44 62 H92 L100 74 H36 Z" fill="url(#plateauGrad)" fill-opacity="0.85" />
    <!-- Base tier -->
    <path d="M36 80 H100 L108 92 H28 Z" fill="#fcd34d" fill-opacity="0.9" />
  </g>
</svg>
