<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128" width="128" height="128">
  <defs>
    <linearGradient id="frameGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#c084fc" /> <!-- Purple-400 -->
      <stop offset="100%" stop-color="#9333ea" /> <!-- Purple-600 -->
    </linearGradient>
    
    <linearGradient id="behaviorGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#2dd4bf" /> <!-- Teal-400 -->
      <stop offset="100%" stop-color="#0d9488" /> <!-- Teal-600 -->
    </linearGradient>
    
    <linearGradient id="elementGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#e2e8f0" /> <!-- Slate-200 -->
      <stop offset="100%" stop-color="#94a3b8" /> <!-- Slate-400 -->
    </linearGradient>
    
    <filter id="floatShadow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur in="SourceAlpha" stdDeviation="4" />
      <feOffset dx="2" dy="4" result="offsetblur" />
      <feComponentTransfer><feFuncA type="linear" slope="0.2"/></feComponentTransfer>
      <feMerge><feMergeNode/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>
  </defs>

  <rect x="14" y="14" width="100" height="100" rx="30" fill="#f8fafc" />

  <g filter="url(#floatShadow)">
    
    <!-- FRAME: Double Quotes "" -->
    <rect x="28" y="44" width="8" height="28" rx="4" fill="url(#frameGrad)" />
    <rect x="40" y="44" width="8" height="28" rx="4" fill="url(#frameGrad)" />

    <rect x="80" y="44" width="8" height="28" rx="4" fill="url(#frameGrad)" />
    <rect x="92" y="44" width="8" height="28" rx="4" fill="url(#frameGrad)" />

    <!-- OPTION 2: The "Socket/Plug" Metaphor -->
    
    <!-- Evolution: A "Chip" (Behavior) plugging into the side of the Element -->
    
    <!-- Base Element (Vertical Rounded Rect) -->
    <!-- Has a "notch" or "socket" on the side -->
    <path d="M 54 38
             L 66 38
             A 4 4 0 0 1 70 42
             L 70 56
             L 64 56
             A 2 2 0 0 0 64 68
             L 70 68
             L 70 86
             A 4 4 0 0 1 66 90
             L 54 90
             A 4 4 0 0 1 50 86
             L 50 42
             A 4 4 0 0 1 54 38 Z"
             fill="url(#elementGrad)" />

    <!-- The Behavior (The Plug) -->
    <!-- Fits into the socket -->
    <path d="M 74 58
             L 86 58
             A 2 2 0 0 1 88 60
             L 88 64
             A 2 2 0 0 1 86 66
             L 74 66
             A 2 2 0 0 1 72 64
             L 72 60
             A 2 2 0 0 1 74 58 Z"
             fill="url(#behaviorGrad)" />

    <!-- A larger "controller" box attached to the plug to give it weight? -->
    <!-- Or just let the small chip be the attribute attribute? -->
    <!-- Let's make the behavior component look like a "tag" attached -->
    <rect x="72" y="58" width="12" height="8" rx="2" fill="url(#behaviorGrad)" />
    
  </g>
</svg>