<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128" width="128" height="128">
  <defs>
    <!-- Rich Body Gradient: Deep Blue depth -->
    <linearGradient id="bodyGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#4dabf7" /> <!-- Sky Blue Highlight -->
      <stop offset="40%" stop-color="#0056b3" /> <!-- Brand Blue -->
      <stop offset="100%" stop-color="#003264" /> <!-- Deep Shadow -->
    </linearGradient>
    
    <!-- Metallic Gradient for Prongs (Chrome effect) -->
    <linearGradient id="metalGrad" x1="10%" y1="0%" x2="90%" y2="0%">
      <stop offset="0%" stop-color="#868e96" />
      <stop offset="20%" stop-color="#e9ecef" />
      <stop offset="50%" stop-color="#ffffff" />
      <stop offset="80%" stop-color="#e9ecef" />
      <stop offset="100%" stop-color="#868e96" />
    </linearGradient>
    
    <!-- Subtle Shadow Filter -->
    <filter id="softShadow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur in="SourceAlpha" stdDeviation="3" />
      <feOffset dx="2" dy="4" result="offsetblur" />
      <feComponentTransfer>
        <feFuncA type="linear" slope="0.2"/>
      </feComponentTransfer>
      <feMerge> 
        <feMergeNode/>
        <feMergeNode in="SourceGraphic"/> 
      </feMerge>
    </filter>

    <!-- Inner Glow for LED -->
    <filter id="glow">
        <feGaussianBlur stdDeviation="2.5" result="coloredBlur"/>
        <feMerge>
            <feMergeNode in="coloredBlur"/>
            <feMergeNode in="SourceGraphic"/>
        </feMerge>
    </filter>
  </defs>

  <!-- Standard Background: Clear Circle Container -->
  <circle cx="64" cy="64" r="62" fill="#ffffff" stroke="#adb5bd" stroke-width="2"/>
  
  <!-- Main Group Centered -->
  <g filter="url(#softShadow)" transform="translate(64, 64) scale(0.75) translate(-64, -64) translate(0, 4)">
      
      <!-- Prongs (Back Layer) -->
      <!-- Left Prong -->
      <path d="M42 20 L42 45 L52 45 L52 20 C52 18 50 16 47 16 C44 16 42 18 42 20 Z" fill="url(#metalGrad)" stroke="#666" stroke-width="0.5"/>
      <!-- Right Prong -->
      <path d="M76 20 L76 45 L86 45 L86 20 C86 18 84 16 81 16 C78 16 76 18 76 20 Z" fill="url(#metalGrad)" stroke="#666" stroke-width="0.5"/>

      <!-- Plug Body -->
      <!-- Main Housing Shape -->
      <rect x="28" y="40" width="72" height="60" rx="12" fill="url(#bodyGrad)" />
      
      <!-- Defined Highlight Edge (Top) -->
      <path d="M30 41 H98 C102 41 103 44 103 44 L25 44 C25 44 26 41 30 41 Z" fill="rgba(255,255,255,0.3)" />

      <!-- Grip Texture (Ribs) -->
      <g stroke="rgba(0,0,0,0.15)" stroke-width="2" stroke-linecap="round">
          <line x1="38" y1="90" x2="90" y2="90" />
          <line x1="42" y1="84" x2="86" y2="84" />
          <line x1="46" y1="78" x2="82" y2="78" />
      </g>

      <!-- Connection Interface Symbol (Like a network or port icon) -->
      <circle cx="64" cy="60" r="14" fill="rgba(0,0,0,0.2)" />
      <path d="M58 60 L62 64 L70 56" stroke="#00ff88" stroke-width="3" fill="none" stroke-linecap="round" stroke-linejoin="round" filter="url(#glow)"/>

      <!-- Cord Strain Relief -->
      <path d="M54 100 L74 100 L70 115 L58 115 Z" fill="#343a40" />
      
      <!-- Cord -->
      <path d="M64 114 Q 64 125 74 125" stroke="#343a40" stroke-width="10" fill="none" stroke-linecap="round"/>
  </g>
</svg>
