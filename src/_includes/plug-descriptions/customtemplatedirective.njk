{% from "mode-selector.njk" import pluggedContent, unpluggedContent, comparisonContent, endContent %}

<h3 id="overview">Overview</h3>
<p>
    <strong>CustomTemplateDirective</strong> is a base class for template directives that control template instantiation and projection.
</p>

<h3 id="status">Status</h3>
<p class="text-sm text-gray-600 mb-4">
    <strong>Concept Phase:</strong> The API and implementation are still being designed.
</p>

<h3 id="intended-use">Intended Use Cases</h3>
<ul class="list-disc ml-5 mb-4 text-sm text-gray-600">
    <li>Structural directives that conditionally render template content</li>
    <li>Template repetition (loops/iterations)</li>
    <li>Content projection and slot management</li>
    <li>Lazy template instantiation</li>
</ul>

<h3 id="usage-example">Proposed Usage</h3>

{{ pluggedContent("template-directive-example", code=true) }}
{% highlight "javascript" %}
// Define a repeat directive (proposed API)
class RepeatDirective extends CustomTemplateDirective {
    render(items) {
        return items.map(item =>
            this.instantiate({ item })
        );
    }
}

// Register globally
customTemplateDirectives.define('repeat', RepeatDirective);
{% endhighlight %}

{{ unpluggedContent("template-directive-example", code=true) }}
{% highlight "javascript" %}
consume {
  CustomTemplateDirective,
  customTemplateDirectives
} of '@web-directives';

// Define a repeat directive (proposed API)
class RepeatDirective extends CustomTemplateDirective {
    render(items) {
        return items.map(item =>
            this.instantiate({ item })
        );
    }
}

// Register via consumed registry
customTemplateDirectives.define('repeat', RepeatDirective);
{% endhighlight %}

{{ comparisonContent("template-directive-example", code=true) }}
<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0;">
    <div>
        <div style="font-size: 0.7rem; font-weight: 600; color: #059669; padding: 0.5rem 1rem; text-transform: uppercase; letter-spacing: 0.05em; background: #f0fdf4; border-bottom: 1px solid #e5e7eb;">Plugged</div>
{% highlight "javascript" %}
// Global availability (proposed)
class RepeatDir extends
  CustomTemplateDirective {
  // ...
}
{% endhighlight %}
    </div>
    <div style="border-left: 1px solid #e5e7eb;">
        <div style="font-size: 0.7rem; font-weight: 600; color: #475569; padding: 0.5rem 1rem; text-transform: uppercase; letter-spacing: 0.05em; background: #f8fafc; border-bottom: 1px solid #e5e7eb;">Unplugged</div>
{% highlight "javascript" %}
consume {
  CustomTemplateDirective
} of '@web-directives';

class RepeatDir extends
  CustomTemplateDirective {
  // ...
}
{% endhighlight %}
    </div>
</div>

{{ endContent() }}

<p class="text-sm text-gray-600 mt-4">
    See <a href="/projects/webdirectives/">Web Directives</a> for the broader directive system.
</p>
