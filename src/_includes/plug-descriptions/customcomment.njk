{% from "mode-selector.njk" import pluggedContent, unpluggedContent, comparisonContent, endContent %}

<h3 id="overview">Overview</h3>
<p>Base class for enhanced comments with lifecycle callbacks for comment-based directives.</p>

<h3 id="interface">Interface</h3>
{% highlight "typescript" %}
class CustomComment extends Comment {
    connectedCallback?(): void;
    disconnectedCallback?(): void;
    optionsChangedCallback?(oldValue: unknown, newValue: unknown): void;
}
{% endhighlight %}

<h3 id="use-cases">Use Cases</h3>
<ul class="list-disc ml-5 mb-4 text-sm text-gray-600">
    <li>Structural directives (*if, *for)</li>
    <li>Virtual elements that don't pollute DOM structure</li>
    <li>Template projection markers</li>
</ul>

<h3 id="usage-example">Usage Example</h3>

{{ pluggedContent("comment-example", code=true) }}
{% highlight "javascript" %}
// Define an if directive as a comment
class IfDirective extends CustomComment {
    connectedCallback() {
        // Evaluate condition and render content
    }
}

// Register globally
customComments.define('if', IfDirective);
{% endhighlight %}

{{ unpluggedContent("comment-example", code=true) }}
{% highlight "javascript" %}
consume {
  CustomComment,
  customComments
} of '@web-directives';

// Define an if directive as a comment
class IfDirective extends CustomComment {
    connectedCallback() {
        // Evaluate condition and render content
    }
}

// Register via consumed registry
customComments.define('if', IfDirective);
{% endhighlight %}

{{ comparisonContent("comment-example", code=true) }}
<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 0;">
    <div>
        <div style="font-size: 0.7rem; font-weight: 600; color: #059669; padding: 0.5rem 1rem; text-transform: uppercase; letter-spacing: 0.05em; background: #f0fdf4; border-bottom: 1px solid #e5e7eb;">Plugged</div>
{% highlight "javascript" %}
// Global availability
class IfDir extends CustomComment {
  connectedCallback() {
    // ...
  }
}

customComments.define('if', IfDir);
{% endhighlight %}
    </div>
    <div style="border-left: 1px solid #e5e7eb;">
        <div style="font-size: 0.7rem; font-weight: 600; color: #475569; padding: 0.5rem 1rem; text-transform: uppercase; letter-spacing: 0.05em; background: #f8fafc; border-bottom: 1px solid #e5e7eb;">Unplugged</div>
{% highlight "javascript" %}
consume {
  CustomComment,
  customComments
} of '@web-directives';

class IfDir extends CustomComment {
  connectedCallback() {
    // ...
  }
}

customComments.define('if', IfDir);
{% endhighlight %}
    </div>
</div>

{{ endContent() }}

<p class="text-sm text-gray-600 mt-4">
    See <a href="/projects/webdirectives/">Web Directives</a> for implementation patterns.
</p>
