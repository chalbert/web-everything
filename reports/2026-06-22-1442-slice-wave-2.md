# #1442 Block-model conversion — slice wave 2 (2026-06-22)

Carves the next batchable wave off the [#1442](../backlog/1442-block-model-conversion-register-remaining-blocks-as-custom-e.md)
block-model-conversion epic. Supersedes the wave-1 stance ("file the tail on pickup") for the
clearly-in-scope, fork-free presentational + form-control blocks: #1442 is a finite burndown epic
(not a program), so its remaining scope should sit in the batch pool as open `tracking` slices rather
than be hidden behind a `childlessReason: program` flag (the flag is dropped as part of this slicing).

Both per-block forks the wave-1 analysis de-buried are now **resolved**, so the form-control mechanism
is settled and no slice buries a fork:

- **#1456** (resolved) — grouped form-control packaging → **B, persistent light-DOM group element**.
- **#1457** (resolved) — behavior blocks (stepper/deck/tabs) → support-both; already built via #1489/1490/1491.

## Could split — wave 2 (4 slices)

All four are flat applications of an already-shipping reference pattern, so there is no DAG root to
sequence — a clean fan-out, fully independent, each `size` ≤ 3, each leaving a valid demoable state.

| # | Block → element | Mechanism | Reference pattern | Size |
|---|---|---|---|---|
| 1 | meter → `we-meter` | transient (A) | fui:blocks/transient/TransientElement.ts | 2 |
| 2 | progress → `we-progress` | transient (A) | fui:blocks/transient/TransientElement.ts | 2 |
| 3 | checkbox → `we-checkbox` + `we-checkbox-group` | persistent light-DOM (B), #1456 | fui:blocks/wizard/WizardElement.ts | 3 |
| 4 | radio → `we-radio` + `we-radio-group` | persistent light-DOM (B), #1456 | fui:blocks/wizard/WizardElement.ts | 3 |

DAG: none — `{1,2,3,4}` are mutually independent and batchable in one lane.

## Could not split (this wave) — deliberately deferred

Not carved now; they remain incremental-on-pickup under the open epic (it reads `tracking` once the
above land, so it never again looks falsely "done"):

- **Single-input form controls** (text-field, number-input) — confirm #1456's single-input arm before
  carving so the slice doesn't bury a mechanism choice.
- **Stateful/interactive blocks** (droplist, type-ahead, tree-select, navigation, sectioned-nav,
  disclosure-nav, edit-in-place, master-detail, marquee-select, data-grid, pan-zoom-surface) — each is a
  larger persistent-B conversion; carve as its own wave when picked up.
- **Headless behaviors** (selection, for-each, guard, lifecycle, stores, view, resource-loader,
  draft-persistence, workflow-engine, traits, parsers, renderers, text-nodes, audit, bulk-action) — a
  "can-do" verb stays a CustomAttribute behavior, not a `we-` element (the is-a/can-do rule), so out of
  scope for this epic.
