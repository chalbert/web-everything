# #1442 slice analysis — Wave 5 (dockable, the last block)

**Date:** 2026-06-24 · **Focus:** `/slice 1442` (block-model conversion epic) · **Locus:** frontierui

## Verdict

#1442 is a **finite-burndown epic**, already decomposed across Waves 1–4 (25 children, **all
resolved**). It is not a classic oversize-story split candidate — the should-we-decompose question was
settled at the parent. The job here is to drain its remaining catalog: verify the real tree, find what
is left, and carve it.

**One block remains: `dockable`.** It was explicitly deferred in the epic body — *"dockable is excluded
— its packaging is downstream of #1653 … carve its mechanism decision under #1442 once #1653 resolves."*
That gate is now open, so the deferred slice is carve-able as **Wave 5**.

## Tree verification (not the body's framing)

Enumerated all 60 `fui:blocks/*` dirs and checked which register a `we-*` custom element. Every
styled-noun block now ships an element **except dockable**:

- Already elements (born or converted): button, badge, card, checkbox, radio, meter, progress,
  text-field, number-input, tree-select, type-ahead, data-grid, stepper, deck, tabs, disclosure-nav,
  sectioned-nav, app-shell, **pan-zoom-surface** (#1707), **temporal** (#1706), plus styled blocks that
  were born as elements (signature-pad, rich-text-editor, filter-chip, tag, notification, code-view,
  droplist, story-canvas, background-task-surface, props-table).
- The six #1676 blocks (navigation, master-detail, marquee-select, edit-in-place, annotation,
  bulk-action) classified **can-do → exited scope** (stay `CustomAttribute`/behaviors).
- **dockable** — still a light-DOM **renderer** (`renderDockable`), no custom element registered.
  Citable: fui:blocks/dockable/index.ts (barrel exports `renderDockable`, no element), the root scopes
  under a class not a tag — `const BASE_CLASS = 'we-dockable'` at fui:blocks/dockable/renderDockable.ts:37
  with `WE_DOCKABLE_CSS` keyed under `.we-dockable` (#1349 **S1, no shadow**, per the header comment).

Block is fully built: #1485's realization slices (#1510 contract, #1511 render, #1512 drag-to-dock,
#1513 serialize/restore, #1514 popout) and the #1627 dockview adapter are **all resolved**. Protocol
owner #1653 and protocol mint #1486 are **resolved**. So the packaging-mechanism decision is ready — the
surface exists and its upstream gates are clear.

## Could split — Wave 5 (1 slice)

| Slice | kind | parent | blockedBy | Mechanism fork |
|---|---|---|---|---|
| dockable packaging mechanism | `decision` | 1442 | — | **B** (persistent light-DOM) vs **C** (shadow, #1349-S2) |

**Why a decision, not a conversion task.** dockable buries a genuine per-block mechanism fork — identical
shape to #1674 (pan-zoom-surface): a complex, stateful layout surface (drag/serialize/popout) that
**cannot** be transient (A), leaving B vs C. Per the Wave-4 convention, the **conversion `task` files on
pickup once the decision lands** (mirroring #1674 → #1707, #1675 → #1706); it is not scaffolded now.

**The fork, sketched for the decision turn (not ruled here):**
- **B — persistent light-DOM.** Matches what ships today: the tree scopes under `.we-dockable` (S1, no
  shadow), and a docking workspace hosts the **author's own panes** as content that must keep inheriting
  page styles — light DOM is the natural fit. Likely default (it is the as-built state).
- **C — shadow (#1349-S2).** Only if the dock *chrome* (dividers, tab bars, drag affordances) must
  survive **hostile/unknown host CSS** — the S2 opt-in case. Adds slotting complexity for author panes.

DAG: single node under #1442, no edges. Independent, batchable on resolution.

## Could not split

| Item | Failed condition | Unblocking action |
|---|---|---|
| dockable **conversion task** | (not a failure) deferred by convention behind its mechanism decision | resolve the Wave-5 decision; the task files on pickup, per Waves 1–4 |
| rest of #1442 catalog | nothing left | every other block converted or test-excluded (#1676) |

## Net

- Epic #1442 stays open with one new child (Wave 5). On the decision's resolution + the conversion task
  it spawns landing, the catalog is fully drained and **#1442 resolves**.
- This is the epic's final wave.
