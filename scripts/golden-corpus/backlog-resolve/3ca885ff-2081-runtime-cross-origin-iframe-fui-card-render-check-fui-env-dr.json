{
  "sha": "3ca885ff992ff8d54b94aa0aec3a13b7aef69804",
  "date": "2026-07-03",
  "file": "backlog/2081-runtime-cross-origin-iframe-fui-card-render-check-fui-env-dr.md",
  "verb": "resolve",
  "opts": {
    "today": "2026-07-03",
    "graduatedTo": "none"
  },
  "before": "---\nkind: story\nsize: 5\nstatus: active\ndateOpened: \"2026-07-02\"\nblockedBy: [\"2142\"]\ndateStarted: \"2026-07-03\"\ntags: []\n---\n\n# Runtime cross-origin iframe .fui-card render check (FUI env-driven ports)\n\nExtend the #2000 render-check harness (we:scripts/dev/render-check.mjs) to the RUNTIME cross-origin path: a .fui-card rendered INSIDE a live FUI demo iframe (the #1895-proper case), not only WE's own home-grid tiles. Requires FUI to expose env-driven dev-server ports (its 6002/6080 are hardcoded, unlike WE's #1997 per-lane `WE_*_PORT`) so the harness can boot a WE+FUI pair on collision-free lane ports and assert the iframe frame from the CLI. The FUI port work is now filed as #2142 (hold-model fix 2026-07-02: `platform-gated` was the wrong reason — this waits on a tracked repo item, not a browser feature — so the park became a real `blockedBy` edge). #2000 delivered the build-time-coupled home-tile fixture (the leak class that shipped); this is the deferred runtime-iframe extension.\n",
  "after": "---\nkind: story\nsize: 5\nstatus: resolved\ndateOpened: \"2026-07-02\"\nblockedBy: [\"2142\"]\ndateStarted: \"2026-07-03\"\ndateResolved: \"2026-07-03\"\ngraduatedTo: none\ntags: []\n---\n\n# Runtime cross-origin iframe .fui-card render check (FUI env-driven ports)\n\nExtend the #2000 render-check harness (we:scripts/dev/render-check.mjs) to the RUNTIME cross-origin path: a .fui-card rendered INSIDE a live FUI demo iframe (the #1895-proper case), not only WE's own home-grid tiles. Requires FUI to expose env-driven dev-server ports (its 6002/6080 are hardcoded, unlike WE's #1997 per-lane `WE_*_PORT`) so the harness can boot a WE+FUI pair on collision-free lane ports and assert the iframe frame from the CLI. The FUI port work is now filed as #2142 (hold-model fix 2026-07-02: `platform-gated` was the wrong reason — this waits on a tracked repo item, not a browser feature — so the park became a real `blockedBy` edge). #2000 delivered the build-time-coupled home-tile fixture (the leak class that shipped); this is the deferred runtime-iframe extension.\n"
}
