// @webeverything/contracts/validity-merge — the validity-merge plane's pure-contract surface (#873).
// Type-only re-export (zero runtime emit) of the canonical contract module; the runtime impl is FUI's.
export type * from '../validity-merge/contract';
