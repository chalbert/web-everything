// @webeverything/contracts/lifecycle — the lifecycle family's pure-contract surface (#694/#879).
// Type-only re-export (zero runtime emit) of the canonical contract module; the runtime impl is FUI's.
export type * from '../blocks/lifecycle/contract';
