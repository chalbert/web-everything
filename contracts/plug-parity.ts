// @webeverything/contracts/plug-parity — the per-plug unplugged-parity manifest's pure-contract surface (#1888).
// Type-only re-export (zero runtime emit) of the canonical contract module; the measured verdict *values*
// are a FUI-runtime fact and live FUI-side (`fui:plugs/<plug>/parity.json`, #1887) per #1282/#1839.
export type * from '../plug-parity/contract';
