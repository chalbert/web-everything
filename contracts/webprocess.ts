// @webeverything/contracts/webprocess — the Self-Driven Project Artefact protocol's pure-contract surface
// (#1026/#1070 — the autonomy/value-risk-ODD/gate/step meta-schemas and the artefact structure that binds
// the SDLC). Type-only re-export (zero runtime emit) of the canonical contract module; the runtime impl —
// the driving loop, meta-schema registries, default-recipe resolution, and the Plateau recipe configurator
// — is FUI/plateau-app's (statute #1282, relocated by the #1294 cascade). This is the FUI→WE arrow the
// relocated webprocess runtime imports, exactly like `./webcompliance` and `./webpolicy`.
export type * from '../process/contract';
