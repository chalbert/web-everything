// @webeverything/contracts/tree-select — the tree-select family's pure-contract surface (#694/#879).
// Type-only re-export (zero runtime emit) of the canonical contract module; the runtime impl is FUI's.
export type * from '../blocks/tree-select/contract';
