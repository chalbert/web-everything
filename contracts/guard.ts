// @webeverything/contracts/guard — the Guard protocol's pure-contract surface (#288/#873).
// Type-only re-export (zero runtime emit) of the canonical contract module; the runtime impl is FUI's.
export type * from '../guard/contract';
