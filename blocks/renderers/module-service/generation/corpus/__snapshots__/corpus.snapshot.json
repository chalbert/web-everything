{
  "canonical/csharp/GeneratedMaaSOrigin.cs": "// @generated by the MaaS generation adapter (backlog #548) — framework-agnostic HTTP shell.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\n//\n// The IR specifies the wire contract — URL grammar, cache policy, status table, header vocabulary. It\n// deliberately does NOT specify content-hash IDENTITY, definition RESOLUTION, or the TRANSFORM; those are\n// INJECTED here as delegates, exactly as the JS reference origin (fetchHandler.ts) injects resolve/resolver.\n// Plain POCOs in/out so the handler has no ASP.NET dependency (host it under any .NET HTTP stack).\nusing System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace FrontierUI.Maas.Generated;\n\npublic sealed record OriginRequest(string Url, IReadOnlyDictionary<string, string> Headers);\n\npublic sealed class OriginResponse\n{\n    public int Status { get; init; }\n    public string? Body { get; init; }\n    public Dictionary<string, string> Headers { get; } = new();\n}\n\n/// <summary>The transform's result: served bytes + language + diagnostics (#081).</summary>\npublic sealed record TransformResult(string Code, string Language, bool Lossy, IReadOnlyList<string> Diagnostics);\n\n/// <summary>Content-addressed identity for a served artifact (#088 §1/§3).</summary>\npublic sealed record ArtifactIdentity(string Id, string Integrity);\n\n/// <summary>A deterministic, framework-agnostic MaaS origin built from injected seams.</summary>\npublic sealed class GeneratedMaaSOrigin\n{\n    private readonly Func<string, string?> _resolveDefinition;\n    private readonly Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<TransformResult>> _transform;\n    private readonly Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<ArtifactIdentity>> _identity;\n    private readonly string _producer;\n    private readonly string _basePath;\n\n    public GeneratedMaaSOrigin(\n        Func<string, string?> resolveDefinition,\n        Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<TransformResult>> transform,\n        Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<ArtifactIdentity>> identity,\n        string producer = \"webadapters/0.0.0\",\n        string? basePath = null)\n    {\n        _resolveDefinition = resolveDefinition;\n        _transform = transform;\n        _identity = identity;\n        _producer = producer;\n        _basePath = basePath ?? OriginCore.BasePath;\n    }\n\n    private static OriginResponse JsonError(int status, string message) => new()\n    {\n        Status = status,\n        Body = \"{\\\"error\\\":\" + System.Text.Json.JsonSerializer.Serialize(message) + \"}\",\n        Headers = { [\"Content-Type\"] = OriginCore.MediaTypes.Error },\n    };\n\n    public async Task<OriginResponse> HandleAsync(OriginRequest request)\n    {\n        var uri = new Uri(request.Url);\n        if (!uri.AbsolutePath.StartsWith(_basePath, StringComparison.Ordinal))\n            return JsonError(OriginCore.Status.NotFound, $\"Not a MaaS request (expected {_basePath}<name>.js).\");\n\n        var spec = Uri.UnescapeDataString(uri.AbsolutePath.Substring(_basePath.Length));\n        if (spec.EndsWith(\".js\", StringComparison.Ordinal)) spec = spec[..^3];\n        if (spec.Length == 0) return JsonError(OriginCore.Status.NotFound, \"Empty component spec.\");\n        var at = spec.LastIndexOf('@');\n        var name = at >= 0 ? spec[..at] : spec;\n        var pin = at >= 0 ? spec[(at + 1)..] : null;\n        if (name.Length == 0) return JsonError(OriginCore.Status.NotFound, \"Missing component name.\");\n\n        var query = System.Web.HttpUtility.ParseQueryString(uri.Query);\n        var form = query[\"form\"];\n        var target = query[\"target\"];\n        var strategy = query[\"strategy\"];\n\n        var definition = _resolveDefinition(name);\n        if (definition == null) return JsonError(OriginCore.Status.NotFound, $\"No component \\\"{name}\\\".\");\n\n        var paramMap = (IReadOnlyDictionary<string, string?>)\n            new Dictionary<string, string?>\n            {\n                [\"form\"] = form,\n                [\"target\"] = target,\n                [\"strategy\"] = strategy,\n            };\n\n        TransformResult result;\n        try { result = await _transform(definition, default!, paramMap, _producer); }\n        catch (Exception e) { return JsonError(OriginCore.Status.ServerError, $\"serve failed: {e.Message}\"); }\n\n        var id = await _identity(definition, result, paramMap, _producer);\n\n        void Base(OriginResponse r)\n        {\n            r.Headers[OriginCore.Headers.Producer] = _producer;\n            if (result.Lossy) r.Headers[OriginCore.Headers.Lossy] = \"1\";\n            if (result.Diagnostics is { Count: > 0 })\n                r.Headers[OriginCore.Headers.Diagnostic] = Uri.EscapeDataString(string.Join(\" | \", result.Diagnostics));\n        }\n\n        // A floating tag / exact semver redirects (short-TTL) to the terminal content-hash URL (#088 §2).\n        if (!OriginCore.IsHashPin(pin))\n        {\n            var r = new OriginResponse { Status = OriginCore.Status.Redirect };\n            Base(r);\n            r.Headers[\"Location\"] = $\"{_basePath}{Uri.EscapeDataString(name)}@{id.Id}.js{uri.Query}\";\n            r.Headers[\"Cache-Control\"] = OriginCore.CachePolicy.Floating;\n            return r;\n        }\n\n        if (pin != id.Id)\n            return JsonError(OriginCore.Status.NotFound, $\"No artifact \\\"{name}@{pin}\\\" — current id is {id.Id}.\");\n\n        var etag = $\"\\\"{id.Id}\\\"\";\n        if (request.Headers.TryGetValue(\"If-None-Match\", out var inm) && inm == etag)\n        {\n            var r = new OriginResponse { Status = OriginCore.Status.NotModified };\n            Base(r);\n            r.Headers[\"ETag\"] = etag;\n            r.Headers[\"Cache-Control\"] = OriginCore.CachePolicy.Immutable;\n            return r;\n        }\n\n        var ok = new OriginResponse { Status = OriginCore.Status.Ok, Body = result.Code };\n        Base(ok);\n        ok.Headers[\"Content-Type\"] = OriginCore.MediaTypes.Javascript;\n        ok.Headers[\"Cache-Control\"] = OriginCore.CachePolicy.Immutable;\n        ok.Headers[\"ETag\"] = etag;\n        ok.Headers[OriginCore.Headers.Integrity] = id.Integrity;\n        return ok;\n    }\n}\n",
  "canonical/csharp/OriginCore.cs": "// @generated by the MaaS generation adapter (backlog #548) — language-neutral serve-path core.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\nnamespace FrontierUI.Maas.Generated;\n\n/// <summary>One query parameter of the serve path (name, required, role).</summary>\npublic readonly record struct ServePathParam(string Name, bool Required, string Description);\n\n/// <summary>One response the serve path can produce, tied to its HTTP status.</summary>\npublic readonly record struct ServePathResponse(int Status, string When, string[] Headers, string? MediaType);\n\n/// <summary>The neutral MaaS serve-path contract, rendered from servePathIR.ts (#505).</summary>\npublic static class OriginCore\n{\n    public const string Version = \"1.0.0\";\n    public const string HashAlgorithm = \"sha256\";\n    public const string HashPinPattern = \"^sha256-[A-Za-z0-9_-]+$\";\n    public const string BasePath = \"/_maas/\";\n    public const string Route = \"<name>[@<pin>].js\";\n    public const string Method = \"GET\";\n\n    public static class CachePolicy\n    {\n        public const string Immutable = \"public, max-age=31536000, immutable\";\n        public const string Floating = \"public, max-age=60, must-revalidate\";\n    }\n\n    public static class Headers\n    {\n        public const string Producer = \"X-MaaS-Producer\";\n        public const string Lossy = \"X-MaaS-Lossy\";\n        public const string Diagnostic = \"X-MaaS-Diagnostic\";\n        public const string Integrity = \"X-MaaS-Integrity\";\n    }\n\n    public static class Status\n    {\n        public const int Ok = 200;\n        public const int Redirect = 302;\n        public const int NotModified = 304;\n        public const int BadRequest = 400;\n        public const int NotFound = 404;\n        public const int ServerError = 500;\n    }\n\n    public static class MediaTypes\n    {\n        public const string Javascript = \"text/javascript; charset=utf-8\";\n        public const string Html = \"text/html; charset=utf-8\";\n        public const string Error = \"application/json; charset=utf-8\";\n    }\n\n    public static readonly ServePathParam[] Params = new[]\n    {\n        new ServePathParam(\"form\", false, \"The served form (e.g. wc-class). Defaults to the origin's default form. The value set is an implementation catalog, not part of the neutral contract.\"),\n        new ServePathParam(\"target\", false, \"The transpile target passed to the transform (e.g. an ES version). Byte-determining.\"),\n        new ServePathParam(\"strategy\", false, \"The define/delivery strategy passed to the transform. Byte-determining.\"),\n    };\n\n    public static readonly ServePathResponse[] Responses = new[]\n    {\n        new ServePathResponse(200, \"A terminal content-hash pin matching the current artifact id.\", new[] { \"Content-Type\", \"Cache-Control\", \"ETag\", \"X-MaaS-Integrity\", \"X-MaaS-Producer\" }, \"text/javascript; charset=utf-8\"),\n        new ServePathResponse(302, \"A floating tag or exact semver — redirected to the terminal content-hash URL.\", new[] { \"Location\", \"Cache-Control\", \"X-MaaS-Producer\" }, null),\n        new ServePathResponse(304, \"A hash-pinned request whose If-None-Match equals the current ETag.\", new[] { \"ETag\", \"Cache-Control\", \"X-MaaS-Producer\" }, null),\n        new ServePathResponse(400, \"An unknown form query value.\", System.Array.Empty<string>(), \"application/json; charset=utf-8\"),\n        new ServePathResponse(404, \"No such component, a non-MaaS path, or a hash pin that does not match current state.\", System.Array.Empty<string>(), \"application/json; charset=utf-8\"),\n        new ServePathResponse(500, \"The injected transform threw.\", System.Array.Empty<string>(), \"application/json; charset=utf-8\"),\n    };\n\n    /// <summary>A pin that is itself a terminal content hash — served immutable, never redirected (#088 §2).</summary>\n    public static bool IsHashPin(string? pin) =>\n        pin != null && System.Text.RegularExpressions.Regex.IsMatch(pin, HashPinPattern);\n}\n",
  "canonical/javascript/origin.core.js": "// @generated by the MaaS generation adapter (backlog #547) — language-neutral serve-path core.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\n\nexport const VERSION = '1.0.0';\nexport const HASH_ALGORITHM = 'sha256';\nexport const HASH_PIN_PATTERN = '^sha256-[A-Za-z0-9_-]+$';\nexport const BASE_PATH = '/_maas/';\nexport const ROUTE = '<name>[@<pin>].js';\nexport const METHOD = 'GET';\n\nexport const CACHE_POLICY = {\n  immutable: 'public, max-age=31536000, immutable',\n  floating: 'public, max-age=60, must-revalidate',\n};\n\nexport const HEADERS = {\n  producer: 'X-MaaS-Producer',\n  lossy: 'X-MaaS-Lossy',\n  diagnostic: 'X-MaaS-Diagnostic',\n  integrity: 'X-MaaS-Integrity',\n};\n\nexport const STATUS = {\n  ok: 200,\n  redirect: 302,\n  notModified: 304,\n  badRequest: 400,\n  notFound: 404,\n  serverError: 500,\n};\n\nexport const MEDIA_TYPES = {\n  javascript: 'text/javascript; charset=utf-8',\n  html: 'text/html; charset=utf-8',\n  error: 'application/json; charset=utf-8',\n};\n\nexport const PARAMS = [\n  { name: 'form', required: false, description: 'The served form (e.g. wc-class). Defaults to the origin\\'s default form. The value set is an implementation catalog, not part of the neutral contract.' },\n  { name: 'target', required: false, description: 'The transpile target passed to the transform (e.g. an ES version). Byte-determining.' },\n  { name: 'strategy', required: false, description: 'The define/delivery strategy passed to the transform. Byte-determining.' },\n];\n\nexport const RESPONSES = [\n  { status: 200, when: 'A terminal content-hash pin matching the current artifact id.', headers: ['Content-Type', 'Cache-Control', 'ETag', 'X-MaaS-Integrity', 'X-MaaS-Producer'], mediaType: 'text/javascript; charset=utf-8' },\n  { status: 302, when: 'A floating tag or exact semver — redirected to the terminal content-hash URL.', headers: ['Location', 'Cache-Control', 'X-MaaS-Producer'], mediaType: null },\n  { status: 304, when: 'A hash-pinned request whose If-None-Match equals the current ETag.', headers: ['ETag', 'Cache-Control', 'X-MaaS-Producer'], mediaType: null },\n  { status: 400, when: 'An unknown form query value.', headers: [], mediaType: 'application/json; charset=utf-8' },\n  { status: 404, when: 'No such component, a non-MaaS path, or a hash pin that does not match current state.', headers: [], mediaType: 'application/json; charset=utf-8' },\n  { status: 500, when: 'The injected transform threw.', headers: [], mediaType: 'application/json; charset=utf-8' },\n];\n\n/** A pin that is itself a terminal content hash — served immutable, never redirected (#088 §2). */\nexport const isHashPin = (pin) => pin != null && new RegExp(HASH_PIN_PATTERN).test(pin);\n",
  "canonical/javascript/origin.shell.js": "// @generated by the MaaS generation adapter (backlog #547) — Web-Fetch HTTP shell.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\n//\n// The IR specifies the wire contract — URL grammar, cache policy, status table, header vocabulary. It\n// deliberately does NOT specify content-hash IDENTITY, definition RESOLUTION, or the TRANSFORM; those are\n// INJECTED here, exactly as the JS reference origin (fetchHandler.ts) injects resolve/resolver. This keeps\n// the shell framework-free: it runs anywhere the Web Fetch API (Request/Response/URL) exists.\nimport { BASE_PATH, CACHE_POLICY, HEADERS, STATUS, MEDIA_TYPES, isHashPin } from './origin.core.js';\n\nconst jsonError = (status, message) =>\n  new Response(JSON.stringify({ error: message }), {\n    status,\n    headers: { 'Content-Type': MEDIA_TYPES.error },\n  });\n\n/**\n * Build a deterministic, framework-agnostic MaaS origin: (Request) => Promise<Response>.\n * Inject the non-IR-derivable seams:\n *   - identity(definition, result, params, producer) => { id, integrity }  (content-hash, #088 §1/§3)\n *   - resolveDefinition(name) => string | null                            (definition registry)\n *   - transform(definition, params) => { code, language }                 (the served-form transform)\n */\nexport function createGeneratedMaaSOrigin(deps) {\n  const { identity, resolveDefinition, transform } = deps;\n  const producer = deps.producer ?? 'webadapters/0.0.0';\n  const basePath = deps.basePath ?? BASE_PATH;\n\n  return async function handle(request) {\n    const url = new URL(request.url);\n    if (!url.pathname.startsWith(basePath))\n      return jsonError(STATUS.notFound, `Not a MaaS request (expected ${basePath}<name>.js).`);\n\n    let spec = decodeURIComponent(url.pathname.slice(basePath.length)).replace(/\\.js$/, '');\n    if (!spec) return jsonError(STATUS.notFound, 'Empty component spec.');\n    const at = spec.lastIndexOf('@');\n    const name = at >= 0 ? spec.slice(0, at) : spec;\n    const pin = at >= 0 ? spec.slice(at + 1) : null;\n    if (!name) return jsonError(STATUS.notFound, 'Missing component name.');\n\n    const form = url.searchParams.get('form') ?? undefined;\n    const target = url.searchParams.get('target') ?? undefined;\n    const strategy = url.searchParams.get('strategy') ?? undefined;\n\n    const definition = resolveDefinition(name);\n    if (definition == null) return jsonError(STATUS.notFound, `No component \"${name}\".`);\n\n    let result;\n    try {\n      result = await transform(definition, { form, target, strategy });\n    } catch (e) {\n      return jsonError(STATUS.serverError, `serve failed: ${e.message}`);\n    }\n\n    const id = await identity(definition, result, { form, target, strategy }, producer);\n\n    const baseHeaders = () => {\n      const h = new Headers();\n      h.set(HEADERS.producer, producer);\n      if (result.lossy) h.set(HEADERS.lossy, '1');\n      if (result.diagnostics && result.diagnostics.length)\n        h.set(HEADERS.diagnostic, encodeURIComponent(result.diagnostics.join(' | ')));\n      return h;\n    };\n\n    // A floating tag / exact semver redirects (short-TTL) to the terminal content-hash URL (#088 §2).\n    if (!isHashPin(pin)) {\n      const h = baseHeaders();\n      h.set('Location', `${basePath}${encodeURIComponent(name)}@${id.id}.js${url.search}`);\n      h.set('Cache-Control', CACHE_POLICY.floating);\n      return new Response(null, { status: STATUS.redirect, headers: h });\n    }\n\n    // Terminal content-hash pin that doesn't match current state — this origin serves only current bytes.\n    if (pin !== id.id)\n      return jsonError(STATUS.notFound, `No artifact \"${name}@${pin}\" — current id is ${id.id}.`);\n\n    const etag = `\"${id.id}\"`;\n    if (request.headers.get('If-None-Match') === etag) {\n      const h = baseHeaders();\n      h.set('ETag', etag);\n      h.set('Cache-Control', CACHE_POLICY.immutable);\n      return new Response(null, { status: STATUS.notModified, headers: h });\n    }\n\n    const h = baseHeaders();\n    h.set('Content-Type', MEDIA_TYPES.javascript);\n    h.set('Cache-Control', CACHE_POLICY.immutable);\n    h.set('ETag', etag);\n    h.set(HEADERS.integrity, id.integrity);\n    return new Response(result.code, { status: STATUS.ok, headers: h });\n  };\n}\n",
  "edge-shapes/csharp/GeneratedMaaSOrigin.cs": "// @generated by the MaaS generation adapter (backlog #548) — framework-agnostic HTTP shell.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\n//\n// The IR specifies the wire contract — URL grammar, cache policy, status table, header vocabulary. It\n// deliberately does NOT specify content-hash IDENTITY, definition RESOLUTION, or the TRANSFORM; those are\n// INJECTED here as delegates, exactly as the JS reference origin (fetchHandler.ts) injects resolve/resolver.\n// Plain POCOs in/out so the handler has no ASP.NET dependency (host it under any .NET HTTP stack).\nusing System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace FrontierUI.Maas.Generated;\n\npublic sealed record OriginRequest(string Url, IReadOnlyDictionary<string, string> Headers);\n\npublic sealed class OriginResponse\n{\n    public int Status { get; init; }\n    public string? Body { get; init; }\n    public Dictionary<string, string> Headers { get; } = new();\n}\n\n/// <summary>The transform's result: served bytes + language + diagnostics (#081).</summary>\npublic sealed record TransformResult(string Code, string Language, bool Lossy, IReadOnlyList<string> Diagnostics);\n\n/// <summary>Content-addressed identity for a served artifact (#088 §1/§3).</summary>\npublic sealed record ArtifactIdentity(string Id, string Integrity);\n\n/// <summary>A deterministic, framework-agnostic MaaS origin built from injected seams.</summary>\npublic sealed class GeneratedMaaSOrigin\n{\n    private readonly Func<string, string?> _resolveDefinition;\n    private readonly Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<TransformResult>> _transform;\n    private readonly Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<ArtifactIdentity>> _identity;\n    private readonly string _producer;\n    private readonly string _basePath;\n\n    public GeneratedMaaSOrigin(\n        Func<string, string?> resolveDefinition,\n        Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<TransformResult>> transform,\n        Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<ArtifactIdentity>> identity,\n        string producer = \"webadapters/0.0.0\",\n        string? basePath = null)\n    {\n        _resolveDefinition = resolveDefinition;\n        _transform = transform;\n        _identity = identity;\n        _producer = producer;\n        _basePath = basePath ?? OriginCore.BasePath;\n    }\n\n    private static OriginResponse JsonError(int status, string message) => new()\n    {\n        Status = status,\n        Body = \"{\\\"error\\\":\" + System.Text.Json.JsonSerializer.Serialize(message) + \"}\",\n        Headers = { [\"Content-Type\"] = OriginCore.MediaTypes.Error },\n    };\n\n    public async Task<OriginResponse> HandleAsync(OriginRequest request)\n    {\n        var uri = new Uri(request.Url);\n        if (!uri.AbsolutePath.StartsWith(_basePath, StringComparison.Ordinal))\n            return JsonError(OriginCore.Status.NotFound, $\"Not a MaaS request (expected {_basePath}<name>.js).\");\n\n        var spec = Uri.UnescapeDataString(uri.AbsolutePath.Substring(_basePath.Length));\n        if (spec.EndsWith(\".js\", StringComparison.Ordinal)) spec = spec[..^3];\n        if (spec.Length == 0) return JsonError(OriginCore.Status.NotFound, \"Empty component spec.\");\n        var at = spec.LastIndexOf('@');\n        var name = at >= 0 ? spec[..at] : spec;\n        var pin = at >= 0 ? spec[(at + 1)..] : null;\n        if (name.Length == 0) return JsonError(OriginCore.Status.NotFound, \"Missing component name.\");\n\n        var query = System.Web.HttpUtility.ParseQueryString(uri.Query);\n        var form = query[\"form\"];\n        var locale = query[\"locale\"];\n\n        var definition = _resolveDefinition(name);\n        if (definition == null) return JsonError(OriginCore.Status.NotFound, $\"No component \\\"{name}\\\".\");\n\n        var paramMap = (IReadOnlyDictionary<string, string?>)\n            new Dictionary<string, string?>\n            {\n                [\"form\"] = form,\n                [\"locale\"] = locale,\n            };\n\n        TransformResult result;\n        try { result = await _transform(definition, default!, paramMap, _producer); }\n        catch (Exception e) { return JsonError(OriginCore.Status.ServerError, $\"serve failed: {e.Message}\"); }\n\n        var id = await _identity(definition, result, paramMap, _producer);\n\n        void Base(OriginResponse r)\n        {\n            r.Headers[OriginCore.Headers.Producer] = _producer;\n            if (result.Lossy) r.Headers[OriginCore.Headers.Lossy] = \"1\";\n            if (result.Diagnostics is { Count: > 0 })\n                r.Headers[OriginCore.Headers.Diagnostic] = Uri.EscapeDataString(string.Join(\" | \", result.Diagnostics));\n        }\n\n        // A floating tag / exact semver redirects (short-TTL) to the terminal content-hash URL (#088 §2).\n        if (!OriginCore.IsHashPin(pin))\n        {\n            var r = new OriginResponse { Status = OriginCore.Status.Redirect };\n            Base(r);\n            r.Headers[\"Location\"] = $\"{_basePath}{Uri.EscapeDataString(name)}@{id.Id}.js{uri.Query}\";\n            r.Headers[\"Cache-Control\"] = OriginCore.CachePolicy.Floating;\n            return r;\n        }\n\n        if (pin != id.Id)\n            return JsonError(OriginCore.Status.NotFound, $\"No artifact \\\"{name}@{pin}\\\" — current id is {id.Id}.\");\n\n        var etag = $\"\\\"{id.Id}\\\"\";\n        if (request.Headers.TryGetValue(\"If-None-Match\", out var inm) && inm == etag)\n        {\n            var r = new OriginResponse { Status = OriginCore.Status.NotModified };\n            Base(r);\n            r.Headers[\"ETag\"] = etag;\n            r.Headers[\"Cache-Control\"] = OriginCore.CachePolicy.Immutable;\n            return r;\n        }\n\n        var ok = new OriginResponse { Status = OriginCore.Status.Ok, Body = result.Code };\n        Base(ok);\n        ok.Headers[\"Content-Type\"] = OriginCore.MediaTypes.Javascript;\n        ok.Headers[\"Cache-Control\"] = OriginCore.CachePolicy.Immutable;\n        ok.Headers[\"ETag\"] = etag;\n        ok.Headers[OriginCore.Headers.Integrity] = id.Integrity;\n        return ok;\n    }\n}\n",
  "edge-shapes/csharp/OriginCore.cs": "// @generated by the MaaS generation adapter (backlog #548) — language-neutral serve-path core.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\nnamespace FrontierUI.Maas.Generated;\n\n/// <summary>One query parameter of the serve path (name, required, role).</summary>\npublic readonly record struct ServePathParam(string Name, bool Required, string Description);\n\n/// <summary>One response the serve path can produce, tied to its HTTP status.</summary>\npublic readonly record struct ServePathResponse(int Status, string When, string[] Headers, string? MediaType);\n\n/// <summary>The neutral MaaS serve-path contract, rendered from servePathIR.ts (#505).</summary>\npublic static class OriginCore\n{\n    public const string Version = \"2.0.0\";\n    public const string HashAlgorithm = \"sha256\";\n    public const string HashPinPattern = \"^sha256-[A-Za-z0-9_-]+$\";\n    public const string BasePath = \"/edge/\";\n    public const string Route = \"<name>[@<pin>].mjs\";\n    public const string Method = \"GET\";\n\n    public static class CachePolicy\n    {\n        public const string Immutable = \"public, max-age=31536000, immutable\";\n        public const string Floating = \"public, max-age=60, must-revalidate\";\n    }\n\n    public static class Headers\n    {\n        public const string Producer = \"X-MaaS-Producer\";\n        public const string Lossy = \"X-MaaS-Lossy\";\n        public const string Diagnostic = \"X-MaaS-Diagnostic\";\n        public const string Integrity = \"X-MaaS-Integrity\";\n    }\n\n    public static class Status\n    {\n        public const int Ok = 200;\n        public const int Redirect = 302;\n        public const int NotModified = 304;\n        public const int BadRequest = 400;\n        public const int NotFound = 404;\n        public const int ServerError = 500;\n    }\n\n    public static class MediaTypes\n    {\n        public const string Javascript = \"text/javascript; charset=utf-8\";\n        public const string Html = \"text/html; charset=utf-8\";\n        public const string Error = \"application/json; charset=utf-8\";\n    }\n\n    public static readonly ServePathParam[] Params = new[]\n    {\n        new ServePathParam(\"form\", false, \"Uses an apostrophe ' and a \\\"quote\\\" to test escaping.\"),\n        new ServePathParam(\"locale\", true, \"A required param.\"),\n    };\n\n    public static readonly ServePathResponse[] Responses = new[]\n    {\n        new ServePathResponse(200, \"served\", new[] { \"Content-Type\", \"ETag\" }, \"text/javascript; charset=utf-8\"),\n        new ServePathResponse(304, \"not modified\", System.Array.Empty<string>(), null),\n        new ServePathResponse(404, \"absent\", System.Array.Empty<string>(), \"application/json; charset=utf-8\"),\n    };\n\n    /// <summary>A pin that is itself a terminal content hash — served immutable, never redirected (#088 §2).</summary>\n    public static bool IsHashPin(string? pin) =>\n        pin != null && System.Text.RegularExpressions.Regex.IsMatch(pin, HashPinPattern);\n}\n",
  "edge-shapes/javascript/origin.core.js": "// @generated by the MaaS generation adapter (backlog #547) — language-neutral serve-path core.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\n\nexport const VERSION = '2.0.0';\nexport const HASH_ALGORITHM = 'sha256';\nexport const HASH_PIN_PATTERN = '^sha256-[A-Za-z0-9_-]+$';\nexport const BASE_PATH = '/edge/';\nexport const ROUTE = '<name>[@<pin>].mjs';\nexport const METHOD = 'GET';\n\nexport const CACHE_POLICY = {\n  immutable: 'public, max-age=31536000, immutable',\n  floating: 'public, max-age=60, must-revalidate',\n};\n\nexport const HEADERS = {\n  producer: 'X-MaaS-Producer',\n  lossy: 'X-MaaS-Lossy',\n  diagnostic: 'X-MaaS-Diagnostic',\n  integrity: 'X-MaaS-Integrity',\n};\n\nexport const STATUS = {\n  ok: 200,\n  redirect: 302,\n  notModified: 304,\n  badRequest: 400,\n  notFound: 404,\n  serverError: 500,\n};\n\nexport const MEDIA_TYPES = {\n  javascript: 'text/javascript; charset=utf-8',\n  html: 'text/html; charset=utf-8',\n  error: 'application/json; charset=utf-8',\n};\n\nexport const PARAMS = [\n  { name: 'form', required: false, description: 'Uses an apostrophe \\' and a \"quote\" to test escaping.' },\n  { name: 'locale', required: true, description: 'A required param.' },\n];\n\nexport const RESPONSES = [\n  { status: 200, when: 'served', headers: ['Content-Type', 'ETag'], mediaType: 'text/javascript; charset=utf-8' },\n  { status: 304, when: 'not modified', headers: [], mediaType: null },\n  { status: 404, when: 'absent', headers: [], mediaType: 'application/json; charset=utf-8' },\n];\n\n/** A pin that is itself a terminal content hash — served immutable, never redirected (#088 §2). */\nexport const isHashPin = (pin) => pin != null && new RegExp(HASH_PIN_PATTERN).test(pin);\n",
  "edge-shapes/javascript/origin.shell.js": "// @generated by the MaaS generation adapter (backlog #547) — Web-Fetch HTTP shell.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\n//\n// The IR specifies the wire contract — URL grammar, cache policy, status table, header vocabulary. It\n// deliberately does NOT specify content-hash IDENTITY, definition RESOLUTION, or the TRANSFORM; those are\n// INJECTED here, exactly as the JS reference origin (fetchHandler.ts) injects resolve/resolver. This keeps\n// the shell framework-free: it runs anywhere the Web Fetch API (Request/Response/URL) exists.\nimport { BASE_PATH, CACHE_POLICY, HEADERS, STATUS, MEDIA_TYPES, isHashPin } from './origin.core.js';\n\nconst jsonError = (status, message) =>\n  new Response(JSON.stringify({ error: message }), {\n    status,\n    headers: { 'Content-Type': MEDIA_TYPES.error },\n  });\n\n/**\n * Build a deterministic, framework-agnostic MaaS origin: (Request) => Promise<Response>.\n * Inject the non-IR-derivable seams:\n *   - identity(definition, result, params, producer) => { id, integrity }  (content-hash, #088 §1/§3)\n *   - resolveDefinition(name) => string | null                            (definition registry)\n *   - transform(definition, params) => { code, language }                 (the served-form transform)\n */\nexport function createGeneratedMaaSOrigin(deps) {\n  const { identity, resolveDefinition, transform } = deps;\n  const producer = deps.producer ?? 'webadapters/0.0.0';\n  const basePath = deps.basePath ?? BASE_PATH;\n\n  return async function handle(request) {\n    const url = new URL(request.url);\n    if (!url.pathname.startsWith(basePath))\n      return jsonError(STATUS.notFound, `Not a MaaS request (expected ${basePath}<name>.js).`);\n\n    let spec = decodeURIComponent(url.pathname.slice(basePath.length)).replace(/\\.js$/, '');\n    if (!spec) return jsonError(STATUS.notFound, 'Empty component spec.');\n    const at = spec.lastIndexOf('@');\n    const name = at >= 0 ? spec.slice(0, at) : spec;\n    const pin = at >= 0 ? spec.slice(at + 1) : null;\n    if (!name) return jsonError(STATUS.notFound, 'Missing component name.');\n\n    const form = url.searchParams.get('form') ?? undefined;\n    const locale = url.searchParams.get('locale') ?? undefined;\n\n    const definition = resolveDefinition(name);\n    if (definition == null) return jsonError(STATUS.notFound, `No component \"${name}\".`);\n\n    let result;\n    try {\n      result = await transform(definition, { form, locale });\n    } catch (e) {\n      return jsonError(STATUS.serverError, `serve failed: ${e.message}`);\n    }\n\n    const id = await identity(definition, result, { form, locale }, producer);\n\n    const baseHeaders = () => {\n      const h = new Headers();\n      h.set(HEADERS.producer, producer);\n      if (result.lossy) h.set(HEADERS.lossy, '1');\n      if (result.diagnostics && result.diagnostics.length)\n        h.set(HEADERS.diagnostic, encodeURIComponent(result.diagnostics.join(' | ')));\n      return h;\n    };\n\n    // A floating tag / exact semver redirects (short-TTL) to the terminal content-hash URL (#088 §2).\n    if (!isHashPin(pin)) {\n      const h = baseHeaders();\n      h.set('Location', `${basePath}${encodeURIComponent(name)}@${id.id}.js${url.search}`);\n      h.set('Cache-Control', CACHE_POLICY.floating);\n      return new Response(null, { status: STATUS.redirect, headers: h });\n    }\n\n    // Terminal content-hash pin that doesn't match current state — this origin serves only current bytes.\n    if (pin !== id.id)\n      return jsonError(STATUS.notFound, `No artifact \"${name}@${pin}\" — current id is ${id.id}.`);\n\n    const etag = `\"${id.id}\"`;\n    if (request.headers.get('If-None-Match') === etag) {\n      const h = baseHeaders();\n      h.set('ETag', etag);\n      h.set('Cache-Control', CACHE_POLICY.immutable);\n      return new Response(null, { status: STATUS.notModified, headers: h });\n    }\n\n    const h = baseHeaders();\n    h.set('Content-Type', MEDIA_TYPES.javascript);\n    h.set('Cache-Control', CACHE_POLICY.immutable);\n    h.set('ETag', etag);\n    h.set(HEADERS.integrity, id.integrity);\n    return new Response(result.code, { status: STATUS.ok, headers: h });\n  };\n}\n",
  "minimal/csharp/GeneratedMaaSOrigin.cs": "// @generated by the MaaS generation adapter (backlog #548) — framework-agnostic HTTP shell.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\n//\n// The IR specifies the wire contract — URL grammar, cache policy, status table, header vocabulary. It\n// deliberately does NOT specify content-hash IDENTITY, definition RESOLUTION, or the TRANSFORM; those are\n// INJECTED here as delegates, exactly as the JS reference origin (fetchHandler.ts) injects resolve/resolver.\n// Plain POCOs in/out so the handler has no ASP.NET dependency (host it under any .NET HTTP stack).\nusing System;\nusing System.Collections.Generic;\nusing System.Threading.Tasks;\n\nnamespace FrontierUI.Maas.Generated;\n\npublic sealed record OriginRequest(string Url, IReadOnlyDictionary<string, string> Headers);\n\npublic sealed class OriginResponse\n{\n    public int Status { get; init; }\n    public string? Body { get; init; }\n    public Dictionary<string, string> Headers { get; } = new();\n}\n\n/// <summary>The transform's result: served bytes + language + diagnostics (#081).</summary>\npublic sealed record TransformResult(string Code, string Language, bool Lossy, IReadOnlyList<string> Diagnostics);\n\n/// <summary>Content-addressed identity for a served artifact (#088 §1/§3).</summary>\npublic sealed record ArtifactIdentity(string Id, string Integrity);\n\n/// <summary>A deterministic, framework-agnostic MaaS origin built from injected seams.</summary>\npublic sealed class GeneratedMaaSOrigin\n{\n    private readonly Func<string, string?> _resolveDefinition;\n    private readonly Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<TransformResult>> _transform;\n    private readonly Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<ArtifactIdentity>> _identity;\n    private readonly string _producer;\n    private readonly string _basePath;\n\n    public GeneratedMaaSOrigin(\n        Func<string, string?> resolveDefinition,\n        Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<TransformResult>> transform,\n        Func<string, TransformResult, IReadOnlyDictionary<string, string?>, string, Task<ArtifactIdentity>> identity,\n        string producer = \"webadapters/0.0.0\",\n        string? basePath = null)\n    {\n        _resolveDefinition = resolveDefinition;\n        _transform = transform;\n        _identity = identity;\n        _producer = producer;\n        _basePath = basePath ?? OriginCore.BasePath;\n    }\n\n    private static OriginResponse JsonError(int status, string message) => new()\n    {\n        Status = status,\n        Body = \"{\\\"error\\\":\" + System.Text.Json.JsonSerializer.Serialize(message) + \"}\",\n        Headers = { [\"Content-Type\"] = OriginCore.MediaTypes.Error },\n    };\n\n    public async Task<OriginResponse> HandleAsync(OriginRequest request)\n    {\n        var uri = new Uri(request.Url);\n        if (!uri.AbsolutePath.StartsWith(_basePath, StringComparison.Ordinal))\n            return JsonError(OriginCore.Status.NotFound, $\"Not a MaaS request (expected {_basePath}<name>.js).\");\n\n        var spec = Uri.UnescapeDataString(uri.AbsolutePath.Substring(_basePath.Length));\n        if (spec.EndsWith(\".js\", StringComparison.Ordinal)) spec = spec[..^3];\n        if (spec.Length == 0) return JsonError(OriginCore.Status.NotFound, \"Empty component spec.\");\n        var at = spec.LastIndexOf('@');\n        var name = at >= 0 ? spec[..at] : spec;\n        var pin = at >= 0 ? spec[(at + 1)..] : null;\n        if (name.Length == 0) return JsonError(OriginCore.Status.NotFound, \"Missing component name.\");\n\n        var query = System.Web.HttpUtility.ParseQueryString(uri.Query);\n        var form = query[\"form\"];\n\n        var definition = _resolveDefinition(name);\n        if (definition == null) return JsonError(OriginCore.Status.NotFound, $\"No component \\\"{name}\\\".\");\n\n        var paramMap = (IReadOnlyDictionary<string, string?>)\n            new Dictionary<string, string?>\n            {\n                [\"form\"] = form,\n            };\n\n        TransformResult result;\n        try { result = await _transform(definition, default!, paramMap, _producer); }\n        catch (Exception e) { return JsonError(OriginCore.Status.ServerError, $\"serve failed: {e.Message}\"); }\n\n        var id = await _identity(definition, result, paramMap, _producer);\n\n        void Base(OriginResponse r)\n        {\n            r.Headers[OriginCore.Headers.Producer] = _producer;\n            if (result.Lossy) r.Headers[OriginCore.Headers.Lossy] = \"1\";\n            if (result.Diagnostics is { Count: > 0 })\n                r.Headers[OriginCore.Headers.Diagnostic] = Uri.EscapeDataString(string.Join(\" | \", result.Diagnostics));\n        }\n\n        // A floating tag / exact semver redirects (short-TTL) to the terminal content-hash URL (#088 §2).\n        if (!OriginCore.IsHashPin(pin))\n        {\n            var r = new OriginResponse { Status = OriginCore.Status.Redirect };\n            Base(r);\n            r.Headers[\"Location\"] = $\"{_basePath}{Uri.EscapeDataString(name)}@{id.Id}.js{uri.Query}\";\n            r.Headers[\"Cache-Control\"] = OriginCore.CachePolicy.Floating;\n            return r;\n        }\n\n        if (pin != id.Id)\n            return JsonError(OriginCore.Status.NotFound, $\"No artifact \\\"{name}@{pin}\\\" — current id is {id.Id}.\");\n\n        var etag = $\"\\\"{id.Id}\\\"\";\n        if (request.Headers.TryGetValue(\"If-None-Match\", out var inm) && inm == etag)\n        {\n            var r = new OriginResponse { Status = OriginCore.Status.NotModified };\n            Base(r);\n            r.Headers[\"ETag\"] = etag;\n            r.Headers[\"Cache-Control\"] = OriginCore.CachePolicy.Immutable;\n            return r;\n        }\n\n        var ok = new OriginResponse { Status = OriginCore.Status.Ok, Body = result.Code };\n        Base(ok);\n        ok.Headers[\"Content-Type\"] = OriginCore.MediaTypes.Javascript;\n        ok.Headers[\"Cache-Control\"] = OriginCore.CachePolicy.Immutable;\n        ok.Headers[\"ETag\"] = etag;\n        ok.Headers[OriginCore.Headers.Integrity] = id.Integrity;\n        return ok;\n    }\n}\n",
  "minimal/csharp/OriginCore.cs": "// @generated by the MaaS generation adapter (backlog #548) — language-neutral serve-path core.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\nnamespace FrontierUI.Maas.Generated;\n\n/// <summary>One query parameter of the serve path (name, required, role).</summary>\npublic readonly record struct ServePathParam(string Name, bool Required, string Description);\n\n/// <summary>One response the serve path can produce, tied to its HTTP status.</summary>\npublic readonly record struct ServePathResponse(int Status, string When, string[] Headers, string? MediaType);\n\n/// <summary>The neutral MaaS serve-path contract, rendered from servePathIR.ts (#505).</summary>\npublic static class OriginCore\n{\n    public const string Version = \"0.0.1\";\n    public const string HashAlgorithm = \"sha256\";\n    public const string HashPinPattern = \"^sha256-[A-Za-z0-9_-]+$\";\n    public const string BasePath = \"/m/\";\n    public const string Route = \"<name>.js\";\n    public const string Method = \"GET\";\n\n    public static class CachePolicy\n    {\n        public const string Immutable = \"public, max-age=31536000, immutable\";\n        public const string Floating = \"public, max-age=60, must-revalidate\";\n    }\n\n    public static class Headers\n    {\n        public const string Producer = \"X-MaaS-Producer\";\n        public const string Lossy = \"X-MaaS-Lossy\";\n        public const string Diagnostic = \"X-MaaS-Diagnostic\";\n        public const string Integrity = \"X-MaaS-Integrity\";\n    }\n\n    public static class Status\n    {\n        public const int Ok = 200;\n        public const int Redirect = 302;\n        public const int NotModified = 304;\n        public const int BadRequest = 400;\n        public const int NotFound = 404;\n        public const int ServerError = 500;\n    }\n\n    public static class MediaTypes\n    {\n        public const string Javascript = \"text/javascript; charset=utf-8\";\n        public const string Html = \"text/html; charset=utf-8\";\n        public const string Error = \"application/json; charset=utf-8\";\n    }\n\n    public static readonly ServePathParam[] Params = new[]\n    {\n        new ServePathParam(\"form\", false, \"The served form.\"),\n    };\n\n    public static readonly ServePathResponse[] Responses = new[]\n    {\n        new ServePathResponse(200, \"ok\", new[] { \"Content-Type\" }, \"text/javascript; charset=utf-8\"),\n    };\n\n    /// <summary>A pin that is itself a terminal content hash — served immutable, never redirected (#088 §2).</summary>\n    public static bool IsHashPin(string? pin) =>\n        pin != null && System.Text.RegularExpressions.Regex.IsMatch(pin, HashPinPattern);\n}\n",
  "minimal/javascript/origin.core.js": "// @generated by the MaaS generation adapter (backlog #547) — language-neutral serve-path core.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\n\nexport const VERSION = '0.0.1';\nexport const HASH_ALGORITHM = 'sha256';\nexport const HASH_PIN_PATTERN = '^sha256-[A-Za-z0-9_-]+$';\nexport const BASE_PATH = '/m/';\nexport const ROUTE = '<name>.js';\nexport const METHOD = 'GET';\n\nexport const CACHE_POLICY = {\n  immutable: 'public, max-age=31536000, immutable',\n  floating: 'public, max-age=60, must-revalidate',\n};\n\nexport const HEADERS = {\n  producer: 'X-MaaS-Producer',\n  lossy: 'X-MaaS-Lossy',\n  diagnostic: 'X-MaaS-Diagnostic',\n  integrity: 'X-MaaS-Integrity',\n};\n\nexport const STATUS = {\n  ok: 200,\n  redirect: 302,\n  notModified: 304,\n  badRequest: 400,\n  notFound: 404,\n  serverError: 500,\n};\n\nexport const MEDIA_TYPES = {\n  javascript: 'text/javascript; charset=utf-8',\n  html: 'text/html; charset=utf-8',\n  error: 'application/json; charset=utf-8',\n};\n\nexport const PARAMS = [\n  { name: 'form', required: false, description: 'The served form.' },\n];\n\nexport const RESPONSES = [\n  { status: 200, when: 'ok', headers: ['Content-Type'], mediaType: 'text/javascript; charset=utf-8' },\n];\n\n/** A pin that is itself a terminal content hash — served immutable, never redirected (#088 §2). */\nexport const isHashPin = (pin) => pin != null && new RegExp(HASH_PIN_PATTERN).test(pin);\n",
  "minimal/javascript/origin.shell.js": "// @generated by the MaaS generation adapter (backlog #547) — Web-Fetch HTTP shell.\n// Source of truth: the neutral serve-path IR (servePathIR.ts, #505). DO NOT EDIT BY HAND;\n// regenerate with `npm run gen:maas-origin`. Deterministic: same IR in, byte-identical out.\n//\n// The IR specifies the wire contract — URL grammar, cache policy, status table, header vocabulary. It\n// deliberately does NOT specify content-hash IDENTITY, definition RESOLUTION, or the TRANSFORM; those are\n// INJECTED here, exactly as the JS reference origin (fetchHandler.ts) injects resolve/resolver. This keeps\n// the shell framework-free: it runs anywhere the Web Fetch API (Request/Response/URL) exists.\nimport { BASE_PATH, CACHE_POLICY, HEADERS, STATUS, MEDIA_TYPES, isHashPin } from './origin.core.js';\n\nconst jsonError = (status, message) =>\n  new Response(JSON.stringify({ error: message }), {\n    status,\n    headers: { 'Content-Type': MEDIA_TYPES.error },\n  });\n\n/**\n * Build a deterministic, framework-agnostic MaaS origin: (Request) => Promise<Response>.\n * Inject the non-IR-derivable seams:\n *   - identity(definition, result, params, producer) => { id, integrity }  (content-hash, #088 §1/§3)\n *   - resolveDefinition(name) => string | null                            (definition registry)\n *   - transform(definition, params) => { code, language }                 (the served-form transform)\n */\nexport function createGeneratedMaaSOrigin(deps) {\n  const { identity, resolveDefinition, transform } = deps;\n  const producer = deps.producer ?? 'webadapters/0.0.0';\n  const basePath = deps.basePath ?? BASE_PATH;\n\n  return async function handle(request) {\n    const url = new URL(request.url);\n    if (!url.pathname.startsWith(basePath))\n      return jsonError(STATUS.notFound, `Not a MaaS request (expected ${basePath}<name>.js).`);\n\n    let spec = decodeURIComponent(url.pathname.slice(basePath.length)).replace(/\\.js$/, '');\n    if (!spec) return jsonError(STATUS.notFound, 'Empty component spec.');\n    const at = spec.lastIndexOf('@');\n    const name = at >= 0 ? spec.slice(0, at) : spec;\n    const pin = at >= 0 ? spec.slice(at + 1) : null;\n    if (!name) return jsonError(STATUS.notFound, 'Missing component name.');\n\n    const form = url.searchParams.get('form') ?? undefined;\n\n    const definition = resolveDefinition(name);\n    if (definition == null) return jsonError(STATUS.notFound, `No component \"${name}\".`);\n\n    let result;\n    try {\n      result = await transform(definition, { form });\n    } catch (e) {\n      return jsonError(STATUS.serverError, `serve failed: ${e.message}`);\n    }\n\n    const id = await identity(definition, result, { form }, producer);\n\n    const baseHeaders = () => {\n      const h = new Headers();\n      h.set(HEADERS.producer, producer);\n      if (result.lossy) h.set(HEADERS.lossy, '1');\n      if (result.diagnostics && result.diagnostics.length)\n        h.set(HEADERS.diagnostic, encodeURIComponent(result.diagnostics.join(' | ')));\n      return h;\n    };\n\n    // A floating tag / exact semver redirects (short-TTL) to the terminal content-hash URL (#088 §2).\n    if (!isHashPin(pin)) {\n      const h = baseHeaders();\n      h.set('Location', `${basePath}${encodeURIComponent(name)}@${id.id}.js${url.search}`);\n      h.set('Cache-Control', CACHE_POLICY.floating);\n      return new Response(null, { status: STATUS.redirect, headers: h });\n    }\n\n    // Terminal content-hash pin that doesn't match current state — this origin serves only current bytes.\n    if (pin !== id.id)\n      return jsonError(STATUS.notFound, `No artifact \"${name}@${pin}\" — current id is ${id.id}.`);\n\n    const etag = `\"${id.id}\"`;\n    if (request.headers.get('If-None-Match') === etag) {\n      const h = baseHeaders();\n      h.set('ETag', etag);\n      h.set('Cache-Control', CACHE_POLICY.immutable);\n      return new Response(null, { status: STATUS.notModified, headers: h });\n    }\n\n    const h = baseHeaders();\n    h.set('Content-Type', MEDIA_TYPES.javascript);\n    h.set('Cache-Control', CACHE_POLICY.immutable);\n    h.set('ETag', etag);\n    h.set(HEADERS.integrity, id.integrity);\n    return new Response(result.code, { status: STATUS.ok, headers: h });\n  };\n}\n"
}
