{
  "body": "Follow-up to #1957. Two things, both consequences of that PR rather than new work.\n\n## 1. The carve-out the #1957 review raised\n\nThe G8 gate shipped matching fork citations with `/#(\\d{3,4})\\b/g` — a bound fitted to the id shape of every open decision at the time. `norm` strips leading zeros, so `we:backlog/039-*.md` has id `39`, and **98 items** carry a 1–2 digit normalized id the bound could never see. A fork leaning on one of them read as clear.\n\nWidened to `/#(\\d+)\\b/g`.\n\n**Measured, not assumed:** the live G8 count is **9 before, 9 after** across 3,496 items — no currently-open decision has a short id, so this is inert on today's corpus and correct going forward. No false positives appeared either.\n\nTwo tests pin **both** ends of the range, because the defect was a bound nobody was watching:\n\n- a 1–2 digit id (`#39`) — the case that was blind;\n- a 4-digit id (`#3512`) — so widening the low end can't silently drop the high end.\n\n**Mutation-verified, both directions:** restoring `\\d{3,4}` reddens the 1–2 digit test; `\\d{1,3}` reddens the 4-digit test. Neither assertion is decorative.\n\nThe juror's disposition on this finding was `carve-out` (`introduced: true`, `worseThanBase: false`, `parallelizable: true`), which is why it lands here as its own diff rather than as a late push onto the reviewed head of #1957.\n\n## 2. Nine stranded hashes, numbered\n\n#1957 was opened through the credential-less fallback rather than `pr-land`, so the drain's JIT numbering (#2288) never ran and nine cards landed on `main` still carrying their hash ids. `npm run check:standards` was **red on `main`** as a result — nine `#2319` stranded-hash errors.\n\n`node we:scripts/backlog.mjs number-stranded` healed it:\n\n| was | now |\n| --- | --- |\n| `x2xrboe` | **#3513** |\n| `x5c8zep` | **#3514** |\n| `x6z6z2j` | **#3515** |\n| `xarvjgf` | **#3516** |\n| `xayr3dc` | **#3517** |\n| `xblcmkc` | **#3518** |\n| `xbwup67` | **#3519** |\n| `xjktyol` | **#3520** |\n| `xzdzg3j` | **#3521** |\n\nEach keeps a `bornAs:` lineage stamp, and the cross-references were rewritten with the rename (`#3519`'s `crossRef` now points at `/backlog/3513-…/`, not the dead hash path). Verified by grep: the only surviving hash mentions are the `bornAs:` stamps themselves.\n\n## 3. Filed, not fixed: `xs10j09`\n\n`G1` and `D2` carry the **same defect class, worse**. Both walk citations with `\\d{1,3}\\b`:\n\n```js\n// we:scripts/audit-backlog-health.mjs lines 129–130\nconst PROSE_PREREQ = /\\b(gated on|blocked on|…)\\s+#?(\\d{1,3})\\b/gi;   // G1\nconst ANY_REF      = /(?:#|\\/backlog\\/)(\\d{1,3})\\b/g;                 // D2, and two other walks\n```\n\n`\\d{1,3}\\b` cannot match `#2209` **at all** — the engine takes `220`, finds no word boundary before `9`, backtracks to `22` and `2`, fails each time, and reports no match. It misses; it does not truncate. The board runs to **#3521**, so the bound is blind to the majority of the corpus. Consistent with what the counts show: **D2 = 1** across 3,496 items reads as a clean board and is instead a gate that stopped seeing.\n\nConfirmed by running both literals against a case table, not by reading the patterns:\n\n| citation | `ANY_REF` (G1/D2) | G8 after this PR |\n| --- | --- | --- |\n| `#39` | `39` | `39` |\n| `#607` | `607` | `607` |\n| `#2209` | *(nothing)* | `2209` |\n| `#3512` | *(nothing)* | `3512` |\n\nBoth regexes are live — `ANY_REF` at lines 336, 365, 522; `PROSE_PREREQ` at line 425 — so this is not a dead definition. **Filed rather than fixed here** because it is pre-existing, sits in gates this diff does not touch, and widening it changes what G1 and D2 report board-wide. That is a measurement change owed its own before/after count, not a rider on this one. `xs10j09` carries the evidence and the trap: unlike G8, these will **not** be inert when widened.\n\n## Verification\n\n- `npx vitest run` — **409 files, 10,766 passed / 7 skipped, 0 failed** (the two new G8 cases are the delta).\n- `we:scripts/__tests__/audit-backlog-health.test.mjs` — 12/12.\n- `npm run check:standards` with `WE_SKIP_HAND_NUMBERED_GATE=1` (the sanctioned renumbering path the gate's own message names, #2548) — the 9 hand-numbered errors clear. The 9 remaining *\"is on main with a NON-NUMERIC leading id\"* errors are the transitional state by construction: those files are still on `origin/main` until this PR lands, which is what clears them.\n- `node we:scripts/check-backlog-item.mjs xs10j09` — clean.\n\n## Known limit\n\nSame as #1957: no `gh` credential on this host, so the PR was opened through the API rather than `pr-land`. The **decision** still came from the operation — `scoreEscalation` returns `escalate: true`, `humanRequired: false` (blast-radius on the two `scripts/` files) → `producerReviewLabel` = **`review:pending`**, applied by hand at open. The `authored-by-actor` stamp is absent and no independence guard runs at open (#3267). This PR is the second instance of that cost; the first one's is what section 2 cleans up.\n\n---\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01PgZw8NXgV4DxWSTQ2cFSaM\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01PgZw8NXgV4DxWSTQ2cFSaM)_",
  "comments": [
    {
      "id": "IC_kwDORBt1-c8AAAABS32B3A",
      "author": {
        "login": "chalbert"
      },
      "authorAssociation": "OWNER",
      "body": "## Human review verdict — chalbert/web-everything#1958\n\n**Verdict:** ✅ pass — no blocking findings\n\n### Panel verdicts\n\n| lens | weight | verdict |\n| --- | --- | --- |\n| correctness | mandatory | accept |\n| security | mandatory | accept |\n\n### Findings (0)\n\n_No findings._\n\n---\n\n**Decision:** `accept` — recorded by the authoring agent, not by a human. Per #3434, a clean independent verdict on a `review:pending` PR is recorded mechanically. The two jurors below are the independent actors: each ran headless under its own derived session id.\n**Lenses:** `correctness` + `security` — 2 juror(s), one per lens, each a separate `judge` step spawned with its own derived session id (#3028) and its own tools (#3319). They ran SEQUENTIALLY and neither saw the other's findings; this is not a `judgePanel` fan-out (#3050). The other 3 panel lens(es) (simplicity, standards-conformance, claim-accuracy) did NOT run and are not reported as unjudged.\n**Earned vs seated:** this PR's code touch-set scores care `elevated` (blast-radius (scripts/__tests__/audit-backlog-health.test.mjs, scripts/audit-backlog-health.mjs)), for which the care dial asks for 5 lens(es) × 1 juror(s)/lens × 2 round(s). This run seated 2 lens(es) (correctness, security), 1 juror each, in 1 round. SHORTFALL: 3 earned lens(es) (simplicity, standards-conformance, claim-accuracy) did not sit. The caller declared `--careLevel=elevated`. The shortfall is structural — the step list is fixed at registration (#3319) — so it is RECORDED here rather than implied away: do not read the seats above as the whole review this PR earned.\nNet basis: `dcbba7e1646ffd67236d10f315280a867da98ab6..f0627091b59d500c76252198203f33b60addc3f8` (rev `origin/claude/token-optimization-research-tx4ntm` at review time) — 15 net changed file(s) vs current main (#2450), not `gh pr diff`'s three-dot list.\n\n_Recorded through the declared `review-pr` operation (#3035)._\n\n### What the correctness juror actually checked\n\nNot just \"no findings\" — the summary names the probes: the regex widen itself, the two mutation-verified tests, the nine hash→number renames *with* their `bornAs` stamps and cross-reference rewrites (grep-confirmed no stray hash references remain), and the `xs10j09` card's `\\b`-boundary claim, which it verified against how JS regex backtracking actually behaves rather than taking the card's word for it. The security juror confirmed the extracted ids only feed in-memory `Set` membership and that `/#(\\d+)\\b/g` is not ReDoS-prone.\n\nJuror spend: **$0.56** (correctness $0.41, security $0.15).\n\n### How this verdict reached the PR (#3267)\n\nSame as #1957: no `gh` credential on this host, so the operation's `record` step failed at\n`we:scripts/review-set-label.mjs` with `spawnSync gh ENOENT` and its outcome is UNKNOWN to the run. This\ncomment and the `review:pending` → `review:accepted` swap were applied **by hand through the GitHub API**,\nso the single home (#2644) was not in play: **no independence guard ran**, and the `reviewed-sha` /\n`reviewed-diff` / `reviewed-contribution` markers are **absent**.\n\n`test` is green on the reviewed head `f0627091b` — 4 shards + smoke — from a `workflow_dispatch` run. Worth\nrecording why that was needed: GitHub dispatched **no** `pull_request` CI run for this PR at all (zero check\nruns on the sha for ~10 minutes after open). The dispatch is a run that never started, not a re-run of a\nfailure, and no empty commit was pushed to provoke it.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_",
      "createdAt": "2026-09-06T19:08:34Z",
      "includesCreatedEdit": false,
      "isMinimized": false,
      "minimizedReason": "",
      "reactionGroups": [],
      "url": "https://github.com/chalbert/web-everything/pull/1958#issuecomment-5561483740",
      "viewerDidAuthor": false
    },
    {
      "id": "IC_kwDORBt1-c8AAAABS32kCA",
      "author": {
        "login": "chalbert"
      },
      "authorAssociation": "OWNER",
      "body": "<!-- drain-skip-reason -->\n· **Skipped by the drain**\n\nrequired check \"test\" is not green",
      "createdAt": "2026-09-06T19:10:04Z",
      "includesCreatedEdit": false,
      "isMinimized": false,
      "minimizedReason": "",
      "reactionGroups": [],
      "url": "https://github.com/chalbert/web-everything/pull/1958#issuecomment-5561492488",
      "viewerDidAuthor": false
    }
  ],
  "createdAt": "2026-09-06T18:52:46Z",
  "files": [
    {
      "path": "backlog/xs10j09-g1-and-d2-cannot-see-a-four-digit-backlog-id-at-all.md",
      "additions": 64,
      "deletions": 0,
      "changeType": "ADDED"
    },
    {
      "path": "scripts/__tests__/audit-backlog-health.test.mjs",
      "additions": 14,
      "deletions": 0,
      "changeType": "MODIFIED"
    },
    {
      "path": "scripts/audit-backlog-health.mjs",
      "additions": 1,
      "deletions": 1,
      "changeType": "MODIFIED"
    }
  ],
  "headRefName": "claude/token-optimization-research-tx4ntm",
  "headRefOid": "484de847a021dbe44802d5ea57870792d0e03a5c",
  "labels": [
    {
      "id": "LA_kwDORBt1-c8AAAACp_g2vA",
      "name": "ready-to-merge",
      "description": "Producer-complete; a drain may land this PR",
      "color": "0E8A16"
    },
    {
      "id": "LA_kwDORBt1-c8AAAACqLiMoQ",
      "name": "review:accepted",
      "description": "reviewer accepted → the drain may merge",
      "color": "0e8a16"
    },
    {
      "id": "LA_kwDORBt1-c8AAAACrQ29dQ",
      "name": "checking",
      "description": "Required checks are still running — CI truth not yet known (#2281)",
      "color": "BFD4F2"
    }
  ],
  "number": 1958,
  "state": "OPEN",
  "title": "G8 sees a backlog id of any length, and the 9 stranded hashes get their numbers",
  "url": "https://github.com/chalbert/web-everything/pull/1958",
  "repo": "chalbert/web-everything"
}
