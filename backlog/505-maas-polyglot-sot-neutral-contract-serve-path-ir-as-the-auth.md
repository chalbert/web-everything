---
type: issue
workItem: story
size: 5
parent: "081"
status: resolved
dateOpened: "2026-06-13"
dateStarted: "2026-06-13"
dateResolved: "2026-06-13"
graduatedTo: blocks/renderers/module-service/servePathIR.ts
tags: []
---

# MaaS polyglot SoT — neutral contract + serve-path IR as the authority (project to OpenAPI)

Ratified in #463 (fork b): elevate the language-neutral contract to the source of truth for the polyglot MaaS origin. Author a serve-path IR/spec over we:protocols.json#maas-versioning (content-hash identity, SRI, immutable-artifact + short-TTL-pointer cache semantics) and project it to OpenAPI for the HTTP-GET shape (verbs, Cache-Control, ETag/SRI). The #461 JS Fetch origin (we:fetchHandler.ts) becomes the reference implementation, NOT the definition — when the contract and #461 disagree, the contract wins and #461 is fixed. This neutral authority is what every per-language target and the generation adapter derive from; no language (including JS) is privileged.

## Progress — resolved 2026-06-13

Shipped the neutral SoT + projection + reference-impl derivation:

- **`we:blocks/renderers/module-service/servePathIR.ts`** — the language-neutral serve-path IR (frozen `SERVE_PATH`): URL grammar, query params (named, not value-enumerated, for neutrality), hash-pin pattern, cache policy (`immutable` vs `floating`), the `X-MaaS-*` header vocabulary, status-code set, media types. Pure data + types, zero imports (no Node/DOM/Fetch) so a generator with no TS runtime can read it.
- **`we:servePathOpenAPI.ts`** — `servePathToOpenAPI(SERVE_PATH)`, a deterministic projection to an OpenAPI 3.1 document for the HTTP-GET shape (path + query params, `Cache-Control`/`ETag`/SRI response headers, the structured `MaaSError` 4xx/5xx schema).
- **`we:maas-servepath.openapi.json`** — the committed projection #506/#507 read; regenerated by **`we:scripts/gen-maas-openapi.mjs`** (`npm run gen:maas-openapi`, esbuild-bundles the projector), drift-gated by a vitest test.
- **`we:fetchHandler.ts` (#461) → reference implementation, not definition** — every byte-determining constant (base path, cache strings, hash-pin regex, header names, status codes, media types) now imports from `servePathIR`, so "the IR wins and #461 is fixed" is enforced by the conformance test rather than prose.
- **`we:servePathIR.test.ts`** — 10 tests: IR shape, the reference handler emits exactly the IR-declared cache/header/status values, OpenAPI projection fidelity + determinism + committed-file drift gate. All 22 (10 new + 12 existing handler) green.
- **`we:protocols.json#maas-versioning`** summary updated to point at the IR as the polyglot SoT.

Unblocks **#506** (conformance vectors assert against this IR) and **#507** (generation adapter reads it). No leftovers.

**Graduated to** `we:blocks/renderers/module-service/servePathIR.ts` — serve-path IR + OpenAPI projection (we:servePathOpenAPI.ts, we:maas-servepath.openapi.json); we:fetchHandler.ts derives from it.
