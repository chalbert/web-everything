---
bornAs: xjsr7pa
kind: story
size: 3
parent: "3383"
status: open
scope: ["we:scripts/operations/wip-report.mjs", "we:scripts/operations/wip-report-io.mjs", "we:scripts/operations/wip-report-cli.mjs", "we:scripts/operations/__tests__/wip-report-readable.test.mjs", "we:scripts/operations/wip-report-next.mjs", "we:scripts/operations/__tests__/wip-report-next.test.mjs"]
dateOpened: "2026-09-21"
preparedDate: "2026-09-21"
preparedAgainstSha: "afd7d5118eeee0afa4c4a2cac0f1259eb3fda9ae"
relatedTo: ["3819"]
tags: []
---

# /wip report is readable on a phone: full-word descriptions, no truncation, Needs you first, Next from the Priority order

Operator (2026-09-21, from a phone; recorded as rule 4 in the operator rules file): the /wip report is too long, is not focused on what the operator must see, gives no description of a work item, and cuts titles with an ellipsis, which makes a cut title useless on a phone. This card makes the report readable on a phone. REQUIREMENTS. (1) Every work item row carries a description in plain words, in full, wrapped onto more lines, never cut with an ellipsis. (2) The default view is four parts, in this order. (a) NEEDS YOU: the operator-queue script NEEDS YOU section, verbatim, unchanged. (b) RUNNING: a compact table with ID, description, supervisor agent, and its tasks with the agent on each task; the supervisor and executor data already exists in the `--sessions` view (we:scripts/operations/wip-agents.mjs). (c) QUEUE: an ordered list of the same rows (ID, description, tasks) with NO agent column, because the queue is routed mechanically. (d) NEXT, not yet queued: a similar table (ID, description, planned tasks, Priority-order list line number) for the operator to review and approve into the queue. (3) NEXT is taken from the first dispatchable lines of the `## Priority order` in epic #3383 card (read from the prototype branch), skipping claimed, blocked, operator-decision and already-in-flight lines and anything already in QUEUE; each row shows its list line number and a one-line why. (4) The long Done-since table and the internal Attention findings leave the default view and stay available behind a flag such as `--full`, the way `--bullets` and `--sessions` exist. (5) A test renders the report over a fixture and fails if any line ends in an ellipsis, any row lacks a description, the four parts are out of order, a part has the wrong columns, or the QUEUE part has an agent column. Amendment 2026-09-21 (operator, relayed by the continuation session): the RUNNING, QUEUE and NEXT split replaces the first wording, "then what is running, then Next"; the no-ellipsis, full-description and flag-hidden-Done-since rules are unchanged. RELATION TO OTHER CARDS (read 2026-09-21). #3736 (open on main; its build is in commit c952d29fe on the prototype branch, graduation owed) specified work item titles cut to about 18 characters and three short columns, plus compact Done-since and Attention tables. This card SUPERSEDES that one clause (the 18-character cut, `TITLE_MAX` in we:scripts/operations/wip-report.mjs) and the default Done-since table. It does not reopen the rest of #3736: the queued, gap and overdue handling of Attention findings stays as built, only hidden from the default view. #3767 (the deployed /wip command is hand-edited and its source is stale) is a separate concern and is not folded in; the command text is not touched here. PROTOTYPE WORK. The code (we:scripts/operations/wip-report.mjs, we:scripts/operations/wip-report-io.mjs, we:scripts/operations/wip-report-cli.mjs) exists only on lane/mechanical-dispatcher, so this build is prototype work under epic #3383: commit straight to lane/mechanical-dispatcher, no PR, one tracker note per push. Not blocked: the code it changes already exists there.

## Ruled design (#3819, ratified 2026-09-21)

The design forks that were prepared here were ruled in decision #3819, all eight as prepared; its `## Ruling` is the source of truth and the options and rejection reasons stay there. The build follows these choices. Code references are to the prototype branch `lane/mechanical-dispatcher`.

- **Description** (Fork 1): a fixed deterministic chain per row kind: Priority-order why, else full title (card); the named card, else PR title (PR); run-record item, else name-derived card, else bound PR, else brief TASK line (session); last resort `no description: <why>`.
- **Supervisor and task agents** (Fork 2): the best source present, in a fixed order; `none (works alone)` on a complete transcript scan, `not known` on an incomplete one.
- **QUEUE** (Fork 3): land-advance owed dispatches, then the conveyor sidecar's cleared items in Priority-order line order; read the runner's sidecar file, not the report's own checkout's.
- **Planned tasks** (Fork 4): the fixed step list of the dispatch outcome (`build, review, land`; `prepare, build, review, land`; `prepare forks, you rule`; `slice first`; `review` / `fix` / `ci-heal`; `no plan yet`).
- **NEXT** (Fork 5): mechanical checks plus `blockedBy` as the one gate record, the first 5 dispatchable Priority-order lines (`--full` shows 10), each with its line number and why. The gate edges are a separate backlog change, not part of this build.
- **Default view versus `--full`** (Fork 6): one `Since HH:MM: N PRs landed, M sessions finished (--full lists them)` line replaces Done-since and Attention; a not-live runner adds one QUEUE line; `--full` prints everything the report prints today.
- **Build home** (Fork 7): `lane/mechanical-dispatcher`, no PR, one tracker note per push; graduation rides #3443.
- **Row layout** (Fork 8): one short block per row, bold ID line, description wrapped at 42 columns, then labelled field lines (`supervisor:` and `tasks:` in RUNNING, `tasks:` in QUEUE, `planned:` and `line N` in NEXT).

## Done when

1. **Executable** — vitest run (`npx vitest run`, run on `lane/mechanical-dispatcher`) on we:scripts/operations/__tests__/wip-report-readable.test.mjs passes. The test renders the default report over a fixture and asserts: (a) no output line ends in an ellipsis (`…` or `...`); (b) every row in RUNNING, QUEUE and NEXT has a non-empty description, and the description comes from the #3819 Fork 1 chain: the fixture has a Priority-order card (its why), an unlisted card (its title), a PR with no card (its PR title) and a brief-file session (its run record's item), and the test asserts each row's text; (c) the four parts appear in the order NEEDS YOU, RUNNING, QUEUE, NEXT; (d) each row has the labelled fields: RUNNING `supervisor:` and `tasks:`, QUEUE `tasks:` and no `supervisor:` or agent field, NEXT `planned:` and `line N`; (e) a RUNNING row whose transcript scan is incomplete says `not known`, and one with a complete scan and no delegation says `none (works alone)`. Before: the file does not exist, and the current compact report cuts titles at `TITLE_MAX = 18` with `…`, so (a) fails on the same fixture.
2. **Executable** — NEEDS YOU is verbatim: a test feeds the operator-queue script's NEEDS YOU section (the fixture text) in and asserts each NEEDS YOU line of the report equals the operator-queue line, with no prefix added (the renderer's `- ` prefix at we:scripts/operations/wip-report.mjs:581 is removed for this part).
3. **Executable** — NEXT comes from the Priority order: a test gives the new pure parser (we:scripts/operations/wip-report-next.mjs, with its own test file) a fixture of epic #3383's `## Priority order` with claimed, blocked, operator-decision, already-queued, band-B and epic lines mixed in, plus a card with a branch build commit and a card with an open `blockedBy` decision, and asserts NEXT holds exactly the first 5 dispatchable lines, each with its correct list line number and why.
3b. **Executable** — QUEUE lists the land-advance owed PR dispatches first, then the conveyor sidecar's items in Priority-order line order, each with its planned steps (#3819 Forks 3 and 4); with both empty it prints `Empty.`.
4. **Executable** — the default view drops the long tables: with the same fixture, the default output has no `## Done since` heading and no `## Attention` heading, and has one `Since HH:MM:` summary line. The report CLI we:scripts/operations/wip-report-cli.mjs run with `--full` (on `lane/mechanical-dispatcher`) prints both headings, plus the header lines, the land-advance Next lines and the docket count line. `--full` is listed in the CLI's `KNOWN` flags beside `--bullets` and `--sessions`.
5. **Executable** — the existing report tests still pass: `npx vitest run` on all `wip-report*` tests under we:scripts/operations/__tests__ exits 0, with the `TITLE_MAX` cut assertions of #3736 updated in the same commit, and `--bullets` output is byte-for-byte unchanged on the existing fixture.
6. **Human verify** — the operator opens `/wip` on a phone and reads it top to bottom without an ellipsis, with a description on every row, Needs you first.
