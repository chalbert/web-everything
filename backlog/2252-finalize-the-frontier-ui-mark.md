---
kind: story
size: 5
status: open
parent: "2256"
blockedBy: ["2249"]
locus: frontierui
dateOpened: "2026-07-04"
tags: [branding, frontier-ui, favicon, logo]
---

# Finalize the Frontier UI mark

Run the sighted `brand-mark-loop` on the FUI Round-3 shortlist — **FUI-12 (advancing ridge), FUI-18
(ground-edge), FUI-21 (edge-post)** (`plateau:branding-proposals/journey.json`, all mesa-family grammar).
None have been through the per-mark loop yet. Render at 220/64/16 on light+dark next to the mesa (family
test); red-team the double-reads (no sailboat / image-placeholder); converge to a winner in the chosen
visual language; then replace `fui:src/assets/logo.svg` + `fui:src/assets/favicon.svg`. The current
"FUI" letterforms are the weakest constellation mark — this is a real upgrade.
