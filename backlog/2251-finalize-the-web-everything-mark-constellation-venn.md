---
kind: story
size: 5
status: open
parent: "2256"
blockedBy: ["2249", "2250"]
locus: webeverything
dateOpened: "2026-07-04"
tags: [branding, web-everything, favicon, logo]
---

# Finalize the Web Everything mark — constellation Venn

Take the user-favored constellation Venn (`plateau:branding-proposals/loop/weB-venn-constellation-v2.svg`)
to a shippable mark via the `brand-mark-loop`. Known execution gap: `mix-blend-mode:screen` renders
muddy — hand-composite the overlapping lens-regions with the true blended colors (or an SVG filter) for
a luminous additive Venn with a unified core. Render-and-look each iteration; validate at 16px on-gradient
and on dark. Then replace `we:src/assets/logo.svg` + `we:src/assets/favicon.svg` (and any small-size
variant). Gated by the visual-language (#2249) and multi-color (#2250) calls. If the sighted loop stalls
below pro-grade, escalate: hand the corpus+rubric brief to a designer (honest ceiling clause).
