---
type: decision
workItem: story
size: 5
status: open
parent: "746"
blockedBy: ["747"]
dateOpened: "2026-06-16"
relatedProject: webdocs
crossRef: { url: /backlog/751-embedded-theme-design-system-creator-plateau/, label: "Plateau-embed creator — the full tier (#751)" }
tags: [webdocs, block-explorer, design-system, theme-creator, open-core, plateau-embed, fui, monetization]
---

# Design-system creator/assembler — open-core layering: simple FUI-native version + full Plateau offering

The #747 design-system manifest needs an authoring tool. Per the constellation (#091) the capability decomposes by tier: a SIMPLE assembler embedded natively in FUI (free/open — pick tokens + set intent defaults + presentational trait defaults, emit a #747 manifest, no sign-in) and the FULL-featured creator offered as a Plateau product (#751's embedded-Plateau seam — DTCG/Figma ingest, screenshot→theme, persist/share). Open-core split: the FUI version is the zero-friction floor; Plateau is the upsell. Output of both is a #747 manifest that feeds the live switcher (#749). Open question: exactly where the simple/full line sits (which knobs are free).
