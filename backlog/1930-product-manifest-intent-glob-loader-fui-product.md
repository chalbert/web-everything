---
kind: story
size: 3
status: resolved
dateOpened: "2026-06-28"
dateStarted: "2026-07-01"
dateResolved: "2026-07-01"
tags: []
---

# Product-manifest intent glob-loader (FUI/product)

Runtime impl side of #1913 (FUI/product, not WE): a per-product custom-intent manifest globbed in exactly like src/_data/intents/ (loadIntents readdir), feeding owner:intent custom intents into the resolver alongside the standard catalog. Build-time declarative seam only (the existing glob pattern); the runtime register-API is the separate demand-gated follow-up. Blocked on the WE meta-schema definition (#1929).

## Progress

- 2026-06-28 (batch-2026-06-28-1946-1945, parallel/serial lane): carried — lane agent flagged blocked-in-fact and reverted its edits, but the precise reason wasn't captured in the run ledger. Note for the next attempt: #1929 resolved as **definition + validate-script only, explicitly "no runtime loader"** (graduatedTo we:scripts/check-standards-rules.mjs), and frontierui has no `src/_data/intents/` glob or `loadIntents` substrate to mirror — so this slice likely has to stand up the FUI/product runtime intent-resolution substrate first, not just add a manifest glob. Re-investigate that substrate gap before re-claiming (may warrant a `blockedBy` prerequisite or a re-size).
- 2026-06-28 (batch-2026-06-28-parallel pre-flight): **substrate gap VERIFIED, re-blocked.** Grepped frontierui — **zero** references to WE's `intentProfileResolver`/`resolveTraits`/`bundlePlan`, no `src/_data/intents/` glob, no `loadIntents`, no product-manifest concept anywhere. There is no FUI-side intent-catalog-assembly + resolver-invocation pipeline for a product manifest to feed into; the seam #1913 says to "mirror" (`src/_data/intents/`) exists only in WE. Filed the prerequisite **#1948** (FUI/product intent-resolution substrate) and re-pointed `blockedBy: ["1948"]` (the #1929 edge is resolved/satisfied). Dropped from this batch as `blocked-in-fact`, not a gut stop.
- 2026-07-01: **#1948 ratified → unblocked.** Ruling: the reusable build-time substrate lives in **FUI** (`fui:tools/intent-resolver/`, twin of `fui:tools/trait-enforcer/vite-plugin.ts`) — glob `{WE standard catalog + product manifest}`, invoke the #776 WE resolver, emit a virtual module; plateau-app imports + wires it, supplying only its `owner:intent` manifest dir. So this story now builds **both** the FUI substrate module *and* the product-manifest glob wiring (no longer just a mirror of a missing seam). Cleared `blockedBy`. See #1948 Ruling + `we:docs/agent/platform-decisions.md#custom-intents-namespace-by-ownership` (Disambiguation).
- 2026-07-01 (batch-2026-07-01-1930-1982): **RESOLVED — built + gated in both loci.** Shipped the FUI substrate `fui:tools/intent-resolver/` (`fui:tools/intent-resolver/catalog.ts` pure assembler — glob a manifest dir, merge standard+custom sorted-by-id, throw on an `owner:intent` id colliding with a standard; `fui:tools/intent-resolver/vite-plugin.ts` `intentResolver({ standardDir, manifestDir, profile? })` serving `virtual:product-intents` = the merged catalog `intents` + optional resolved `plan` via the #776 `bundlePlan`; `fui:tools/intent-resolver/virtual.d.ts`; 6 vitest cases). Wired into **plateau-app**: `src/_data/intents/` manifest dir (+ README + a sample namespaced custom `@plateau/product-emphasis`), `intentResolver(...)` in `vite.config.mts`, and the intent-configurator's catalog source swapped from the WE-only runtime glob to `virtual:product-intents` (so product customs render alongside standards). **Design note vs #1948's code shape:** the substrate takes `standardDir` as an option instead of importing WE's CJS `loadIntents` — the CJS `require('node:fs')` breaks when esbuild bundles a product's `vite.config` to ESM ("Dynamic require not supported"); the product already resolves `weRoot`, so it passes the dir, keeping the plugin location-agnostic + cleanly ESM-bundleable (the resolver `.ts` import is kept). Gate: FUI `check:standards` 0 errors + 6/6 tests; plateau `vite build` ✓ + full suite 602/602; verified the built bundle carries the custom intent *and* the standards (merged, not replaced). Bundle-plan consumer (a product wiring an active `profile`) stays the demand-gated follow-up alongside the runtime register-API #1931.
